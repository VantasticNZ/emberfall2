# EMBERFALL 2 — PLAYTEST LOG (Van's feel-judgments per slice iteration)

A running record of OWNER-JUDGED feel (Part 2.5 P6) — movement, camera, interaction,
combat — so we can see whether feel improves over iterations. Self-checks (text/
tiles/licence) live in the verify gate + commit reports; THIS log is for the things
only Van's hands can judge. One row per playtest.

Format: `date | iteration | what was tested | Van's verdict | follow-ups`
Verdict scale: ✅ good · ⚠ needs work · ❌ bad · ⏳ awaiting Van.

| date | iteration | tested | verdict | follow-ups |
|---|---|---|---|---|
| 2026-06-11 | **REBUILD: ASSET-OWNED doorways — every building asset declares its portal (universal fix) + bug sweep** ✅ | Van's governing principle: the doorway is a PROPERTY OF THE ASSET so a fix applies to ALL buildings. BUILT: each building PROP in assets.js declares `doorway.px` (the painted door's offset, read off the sprite art — store=+36 bottom-right, paneled=+40 porch, manor=-46 left, forge=0); the ONE door system derives visible==walkable==trigger from it. Removed the per-sprite DOOR_OFFSET hack. Gate #21 now FAILS if any building asset omits `doorway` + asserts every interior has a 'back' exit. Eyes-on FRESH save: STORE now enters dead-on its bottom-right door (store-asset-doorway.png — the long-standing 'can't enter' fixed); ALL GH buildings enter + EXIT lands walkable (no void); tavern up→f2/down→f1 (stairs via the stack); avatar VISIBLE on knock (knock-avatar-visible.png); interiors isolated (store-interior-isolated.png — one room, black around). 21 gates GREEN, 0 errors. | 👁️ **VAN (fresh load):** every building (incl. the store) enters dead-on the door you SEE; exit never drops you in black; tavern upstairs is stairs. The universal fix: adding/fixing a building now needs only its `doorway` in the asset def. ⚑ stairs could use a stairs SPRITE (cosmetic); varied generics still pending. |
| 2026-06-11 | **FIX: Van's numbered door bugs — interior isolation · GH dungeon-rocks · approach · chapel align** ✅ | (1) INTERIOR ISOLATION: inside a building you saw other rooms in the black (interiors share a far-band, all streamed) → FIX: only the ACTIVE interior renders when _inInterior (interior-isolated.png — one room, pure black around). (2) GH DUNGEON ROCKS: cave_f1 + 2 generated dev-warps sat as rocks in the plaza → cave_f1 relocated to the north wilds edge (walkable, reachable, enters), the 2 dev-warps REMOVED (gh-plaza-no-rocks.png). (3) APPROACH REGRESSION: 2 buildings had the tile in FRONT of the door blocked → _clearDoorwayThresholds now clears the approach tile too (0 blocked). (4) CHAPEL TRIGGER: sat on the window, not the right-side porch → per-sprite DOOR_OFFSET shifts the doorway/threshold/trigger onto the visible painted door (chapel-aligned-porch.png — enter through the porch). Eyes-on FRESH cleared save: all verified; 0 errors; 21 gates GREEN. | 👁️ **VAN (fresh load):** inside a house = only that room (black around); GH streets have no dungeon rocks (the cave is up at the north edge); every building reachable + you enter through the door you SEE. ⚑ (5) varied generic interiors DEFERRED (rotate a set); re-home the dev gen-warps in the wilds. |
| 2026-06-11 | **REBUILD: UNIFORM door system — EVERY building enterable, every region + visible door-open** ✅ | DIAGNOSED (fresh save): 16/22 buildings were NOT enterable — root cause = only GH was carved; Marsh/Mirefen (8) + Peaks (5) + GH manor/extra-cottages were solid props with no door. REBUILT as ONE system: _resolveBuildingDoor gives EVERY building a door (explicit, or a default OPEN door to a shared generic interior house_generic/forge_generic); _clearDoorwayThresholds disables any scattered rock/prop on a doorway tile (Peaks rock-in-front-of-door fixed); _solidAt now ignores disabled bodies; gate #21 extended to assert ALL building props in EVERY region are enterable. Visible OPEN: closed/locked door tried/broken → closed-sprite hides + dark threshold appears (door swings open) → THEN walk through (fixes 'break worked but no open door'). Break-strength scales the force morality; key-option wired. Eyes-on FRESH save: ALL 22 buildings (GH 9 + Marsh 8 + Peaks 5) enterable, 0 blocked, sample-entered each region by real movement; visible-open verified (closedSpr→false, opening→0.9, state→open); 165fps; 0 errors; 21 gates GREEN. | 👁️ **VAN (fresh load):** walk into ANY house in GH, Mirefen, or the Peaks — all enter; a shut door visibly OPENS when you try/break it before you step through. proof: uniform-doors-mirefen-enterable. ⚑ Coast/Emberwood have no inline buildings yet (marker-scenes → comes with inline-town conversion); shared generic interiors (repeat look); findable house-key deferred. |
| 2026-06-11 | **FIX+BUILD: exact tile-aligned doorways (consistent, no-stuck) + closed/locked door states** ✅ | Van's bugs: (a) inconsistent entry, (b) stuck-on-the-threshold-line. ROOT (a): the ~1.1-tile FLOAT gap left the walkable gap + the trigger tile mis-registered (trigger partly under a collider) → "works on some, not others" + a sub-tile sliver to snag on. FIX: the doorway is now EXACTLY ONE tile-aligned walkable tile (dCol=round((ccx-TILE/2)/TILE); solids end at dCol*TILE / (dCol+1)*TILE) — identical on every building; clean exit spawn in the YARD (no threshold line). PART C: built CLOSED (knock/try-handle → −3 morality + entered_uninvited) + LOCKED (try → break-down → −10 morality + forced_entry + alarm beat) on the Dialogue option system; gh_home1=closed, gh_home2=locked demo; states read visually (closed-door sprite + lock glyph). Eyes-on FRESH cleared save: all 4 open doors walkable+enter+exit-clean (no stuck); closed/locked choices correct; morality −3/−10 applied; deeds fire; 21 gates GREEN, 0 errors. | 👁️ **VAN (fresh load):** every building enters the SAME by walking into the doorway, never snags on the line; a shut cottage offers knock/try, a locked one offers break-down (with a morality cost). proof: door-states-closed-locked. ⚑ knock-answer + guard alarm are flavor beats (social pass); lock glyph small (polish). |
| 2026-06-11 | **SYSTEM: door/entrance — inset doorways you WALK INTO (GH) + gate #21 + visual check** ✅ | docs/DOOR-SYSTEM.md (SET standard: solid footprint + ONE carved inset doorway gap, trigger = the doorway tile; OPEN built, CLOSED/LOCKED knock/try/force morality states spec'd). BUILT: the 6 GH buildings (chapel/tavern/store/forge/2 cottages) each carve a doorway gap in the front wall + a dark inset threshold; the separate yard-doors REMOVED. Eyes-on FRESH cleared save: all 6 enter by walking UP INTO the doorway (not from the front); doorway tile walkable; no un-enterable holes; chapel reads as a recessed porch/threshold. NEW gate #21 doorway-geometry. 21 gates GREEN, 0 errors. | 👁️ **VAN (fresh load):** walk UP into a building's recessed doorway to enter — not from the path in front. proof: door-system-chapel-doorway · door-system-inset-doorway. ⚑ CLOSED/LOCKED states (knock/try/force + morality) spec'd, deferred. |
| 2026-06-11 | **FIX: FLOATING DOORS in Greenhollow (confirmed live, fresh-reset) — ZERO free-standing doors** ✅ | Reproduced Van's EXACT state (localStorage cleared, build 7df83902): 16 floating prop_door sprites in/around GH (gh-floating-doors-BEFORE). ROOT: every door rendered a prop_door SPRITE at its trigger tile — on the dirt in front of buildings / on open terrain (caves, the orphaned oasis fast-travel, the relocated entrances). I'd wrongly dismissed these as "legit" + blamed cache (broke meta-rule 2). FIX: doors no longer render a free-standing prop_door — a building entrance shows the building's OWN door (no sprite), a cave/dungeon shows a rock cave-mouth MARKER, a not-yet-inline village shows a waypost MARKER, interior EXITS keep the embedded wall-door; the orphaned Mirage-Oasis door REMOVED. NEW gate #20 no-floating-doors. Eyes-on FRESH SAVE: **0 floating prop_doors** in GH (gh-floating-doors-FIXED, gh-clean-town-center); building doors still walk-in (chapel entered); 0 errors, 20 gates GREEN. | 👁️ **VAN (after your reset):** GH should read as houses + paths with NO doors floating on grass; the cave is a rock mouth, the buildings' own doors are the entrances. proof: gh-floating-doors-BEFORE vs -FIXED · gh-clean-town-center |
| 2026-06-10 | **GH→Mirefen corridor — FEEL-LAYER to standard (partial: structure-geometry deferred)** ⏳ awaiting Van | Read the map route: width-3 walk road, 5 zig-zag bends (9.5,10.5→9,12→8,10.5→8,12.5→6.5,11) descending elev +1→−1; Mirefen 44×36 marsh-dread. BUILT the §E feel-pillars on the corridor: SEE-IT-BEFORE tease (a strongbox on a reed-islet across the black water + a legible "DASH could clear it" sign), SOFT GUIDANCE (waystones funnel to Mirefen), DENSITY (reeds/trees/pools/enemies/NPCs — no dead bog), ambient life + marsh-dread audio. Confirmed: corridor BOUNDED (276 channel colliders, can't stray), Mirefen inline/seamless, 57fps, 0 errors. | 👁️ **VAN:** walk GH→Mirefen — bounded bog, a town alive at the end, a cache teasing across the water. **HONEST DEFERRED (structure):** the exact zig-zag geometry, the elevation descent contours, the grass→bog terrain-transition bands, Mirefen resized to 44×36, and the dash-reach (engine). proof: corridor-see-it-before-tease |
| 2026-06-10 | **SEAMLESS WORLD (Van option B) — Mirefen converted to INLINE terrain (proof) + locked DoD/gate #19** ⏳ awaiting Van | LOCKED option B (PLAN-UNDERSTANDING + DONE-DEFINITION HARD rule): ONE seamless overworld, settlements are walk-through terrain, only interiors/dungeons/secrets are scenes. NEW gate #19 seamless-overworld (tracks town inline-vs-scene + regression-guard). BUILT: **Mirefen converted to INLINE terrain in the Marsh** — 8 bog-houses + 3 townsfolk + Yssa's-hut interior-door + a sign, placed AS overworld terrain at its map position; **the enter-door REMOVED**. Runtime-verified: you walk INTO/THROUGH Mirefen's streets with NO transition (region stays Marsh), it reads as a real bog-town embedded in the world, **60fps**, 0 errors (clean load); made it a no-aggro safe hub. Gate #19: 1/9 towns inline. | 👁️ **VAN:** walk west into the Marsh → you're IN Mirefen's streets, no door/transition (mirefen-inline-seamless.png). Is THIS the seamless world you mean? ⚑ HONEST: 1/9 towns done (the pattern); 8 deferred (gate #19 tracks). Streets a bit tight (polish). proof: mirefen-inline-seamless |
| 2026-06-10 | **FIX: what Van SEES now matches what's built (M-map + door-clutter)** ⏳ awaiting Van | Van saw the OLD layout despite gates saying 10/11 built. ROOT CAUSE 1 — the M-map rendered the 8-region skeleton + a corner TEXT LIST of settlements (not pins at their built positions). FIX: the M-map now PINS each settlement at its LIVE overworld-entrance position → it shows Marsh towns W, Peaks towns N, Coast E, Thornwell S (matches Van's map). ROOT CAUSE 2 — the old GH south notice-board (11 doors) was never removed when settlements were relocated → duplicate board+overworld entrances = the door-clutter. FIX: removed the 10 relocated board doors (kept only Mirage Oasis as a single fast-travel until it's relocated) → GH south reads as town+meadow, not a door-board. NEW GATE #18 rendered-vs-built / no-duplicate-entrances (board fast-travel:1, overworld:10, no dupes). 18 gates GREEN, 0 errors. | 👁️ **VAN:** press M → the map matches the world you walk; GH south is clean (one fast-travel, not a board). proof: mmap-built-layout · gh-south-declutter |
| 2026-06-10 | **CONNECTED OVERWORLD pass 2 — 6 settlements relocated to the walkable world** ⏳ awaiting Van | Relocated 6 of 7 board-only settlements to overworld entrances at Van's positions: Stonereach/Cragfoot/Cinder-Keep (Peaks N), Saltbreak/Cribbins (Coast E), Thornwell (Emberwood S) — walk-in doors on probed walkable tiles, reusing built content. RUNTIME-verified (DONE-DEFINITION): all 6 enter the CORRECT town by walking onto the door. **Gate #16 designed-vs-built: 10/11 on the walkable overworld** (was 4); only Mirage Oasis (desert) deferred to M3. **FOUND + FIXED a real latent bug:** the far-band settlement scenes OVERLAPPED (thornwell∩stonereach etc.) → an entrance landed you in the wrong area; moved the oty466 row to oty480 (no overlaps) + added **gate #17 no-overlapping-scenes** so it can't regress. 17 gates GREEN, 0 errors. | 👁️ **VAN (FEEL):** walk N into the Peaks → Stonereach/Cragfoot/Keep are there; E → Saltbreak/Cribbins; S → Thornwell. ⚑ DEFERRED: the landmark/dungeon nodes (High Pass/Lighthouse/Vault/Caught/Hollow), Spire (needs 24×24), Oasis (needs a desert region); town silhouettes; spell-route gates (abilities). proof: overworld-stonereach-entrance |
| 2026-06-10 | **CONNECTED OVERWORLD pass 1 — the WEST is ONE walkable world** ⏳ awaiting Van | Placed the marsh settlements at their map positions (Van's W→E): added door-entrances to the Ashen Marsh region — Mirefen (far-W), Sunken Shrine (W), Lost Cemetery (NW), Fenwick (E) — each a walkable bog tile + a naming signpost, reusing the built town content. WALK-ACROSS proven (real movement): GH → Belt → Marsh (region transitions) → walked onto the Mirefen entrance → entered Mirefen. Corridor 38-43/45 tiles walkable per row (eerie bog + marsh music). The GH board kept as optional fast-travel (world walkable without it). State tagged `pre-connected-overworld`. 14 gates GREEN, 0 errors. **HONEST DEFERRED:** the OTHER settlements (Stonereach/Saltbreak/etc.) not yet relocated to Peaks/Coast/Emberwood/Spire (still board); town silhouettes on the overworld (just signpost+door now); spell-route gates (need the abilities); 24×24 expansion; M-map settlement pins. | 👁️ **VAN (FEEL):** does walking WEST into the eerie Marsh and finding Mirefen/Shrine/Cemetery/Fenwick *there* (not via a board) read as ONE world? ⚑ NEXT: relocate the other regions' settlements (same pattern) + town silhouettes + the spell-route gates. proof: overworld-mirefen-entrance · world-map-M |
| 2026-06-10 | **BUGFIX 2 — door placement + dash-into-void + bush-farm (+ overworld diagnosis)** ✅ verified | 3 fixes, real-input verified: (1) interior door floated mid-floor (centered on the trigger tile) → `doorWallOffset` pushes the door SPRITE one tile into the nearest wall (trigger unchanged) — cemetery door now reads as a doorway in the wall (`door-fixed-in-wall`); (2) dash-into-void → a hard interior containment CLAMP in update() (clamp player to bounds, cancel dodge) + extended the containment navGate; real dash L/R/U all contained; (3) bush instant-respawn farm → cut bushes recorded + disabled with NO in-area respawn, regrow only on re-entry after 3-min cooldown (real swings: stays cut, no repeat loot; regrew=true after cooldown). DIAGNOSIS: the connected west-walking world from Van's map is NOT built — Mirefen(442)/Fenwick(480)/Cemetery(430) are far-band board-door scenes; walking west hits the streamed Marsh, not them; the overworld is the OLD 8-region skeleton, not Van's edited map. 14 gates + 23 suites GREEN, 0 errors. | 👁️ **VAN:** doors sit in the wall; you can't dash into the black; bushes can't be farmed (regrow only on return after a few min). ⚑ NEXT (big): build the CONNECTED overworld from Van's map (Mirefen/Fenwick/Cemetery as walk-to locations west of GH). proof: door-fixed-in-wall · cemetery-door-position |
| 2026-06-10 | **BUGFIX — doors (loose/sign) + interior void + Fenwick visibility** ✅ verified | 4 bugs, real-input verified: (1) door "too loose" = a VISUAL mismatch (the trigger is already exact-tile — beside-the-door does NOT enter, on-tile does; the tiny prop_sign in the front-yard read as "near"); (2) interior VOID = griddedSettlement floored only streets → black unfloored blocks; fixed by flooring the WHOLE settlement (continuous ground) + a new CONTAINMENT navGate (23 interiors sealed, no walkable perimeter); (3) exit-is-a-sign → drew a real **prop_door** doorway sprite (wooden door + stone frame, original CC0) + swapped all 22 door/exit markers prop_sign→prop_door (chapel exit now reads as a door); (4) Fenwick IS built + reachable — entered by walking the board door live; the "not visible" was a stale tab (hard-refresh :5273) + needing to navigate to the south board. 14 gates + 23 suites GREEN, 0 errors. | 👁️ **VAN:** doors now read as doors + you enter on the tile; interiors are continuous floor (no void); walk south to the board → into Fenwick/Mirefen/Cemetery (hard-refresh first). proof: mirefen-floored · door-closeup · fenwick-reached-walking |
| 2026-06-10 | **MARSH step 2c — Mirefen + Fenwick: greybox → real furnished marsh-towns** ⏳ awaiting Van | Extended `griddedSettlement` (write-once) to take buildings/npcs/dressing/groundTint/spawnTile → a greybox grid becomes a dressed populated town. **Mirefen**: 12 weathered bog-tinted houses + 4 forge NPCs (Bett/Coll/Mire/Tam) + dead-pine/barrel/reed dressing + eerie tint + Yssa's furnished hut — the core reads as a real marsh-town (`mirefen-closeup`). **Fenwick**: 4 houses + 2 NPCs + a furnished cottage (bed/hearth/table/dresser + a child) + eerie tint; its ⚡electric entry stays deferred (kept walk-reachable so not orphaned). 22 navGate suites + 14 gates GREEN, 0 errors, 154fps. **Honest:** real dressed cores, but coverage isn't uniform — bare greybox blocks remain at the grid edges (a density-polish pass). | 👁️ **VAN (FEEL):** do Mirefen + Fenwick now read as real, populated, eerie marsh-towns (vs the old greybox grid)? ⚑ NEXT: density pass (fill the bare edge-blocks) + the abilities session (dash-leap/electric → the M1 gates + Fenwick's electric entry) + deep M8–M10. proof: mirefen-before/after/closeup · fenwick-before/after · fenwick-cottage-interior |
| 2026-06-10 | **MARSH step 2b — the Lost Cemetery (FINISHED) + a furnished Mirefen interior** ⏳ awaiting Van | Built **the Lost Cemetery** to finished quality: vetted+extracted LPC Grave Markers Rework (CC-BY-SA 3.0/GPL 3.0), cropped 5 grave props, built `lost_cemetery` (22×16 walled graveyard, eerie dark floor, founder's stone + mourning angels, varied graves, a fresh **open grave** with **Mother Cray** the gravekeeper, dread music). Added **Yssa's Hut** — a furnished Mirefen interior (bed/hearth/table/dresser + Elder Yssa) walk-in from the Mirefen street. Engine: `interiorRegion` now supports NPCs + themed floor `groundTint`. 21 navGate suites + 14 gates GREEN, 0 errors, 156fps. **Honest:** Mirefen TOWN overworld is still greybox grid; Fenwick not built; deep M8–M10 + the dash/electric gates (engine-blocked) deferred. | 👁️ **VAN (FEEL):** does the **Lost Cemetery** read as a finished, mournful graveyard (graves + gravekeeper + dread music)? Is **Yssa's hut** a lived-in marsh home? ⚑ NEXT: finish the **Mirefen town** (real marsh-buildings, griddedSettlement→built) + **Fenwick** + deep M8–M10. proof: cemetery-built · cemetery-eerie · mirefen-town · mirefen-hut-furnished |
| 2026-06-10 | **ASHEN MARSH — atmosphere slice (M1 step 2; eerie bog read + audio identity)** ⏳ awaiting Van | Delivered the Marsh's eerie identity (NOT the whole region): (1) re-tinted the bog olive→**murky grey-teal** (`0x5a6357`, water near-black `0x2e433e`, grey dead trees) — ground mean RGB measured `(39,79,22)→(25,50,17)`, visibly darker/colder; (2) wired **`mus_marsh`** (the HydroGene dread bed) + fixed `_musicForRegion` so the Marsh plays it; (3) bog **ambient = desolate wind** (no birds); (4) **+8 grey dead pines + 4 reed clumps** → a denser, brooding dead mire. 0 errors, **148 fps**, 14 gates GREEN. **Honest:** still tinted-grass (bog-tile gap), and Mirefen/Cemetery/Fenwick are still greybox + the dash/electric ability-gates are **engine-blocked** (dash-leap-over-gap mechanic + the electric ability don't exist yet) → all deferred (MARSH-BUILD.md checklist). | 👁️ **VAN (FEEL):** does the Marsh now **read + sound eerier** — a darker brooding dead mire + dread music + dead wind (`marsh-before` ↔ `marsh-after-dressed`)? ⚑ NEXT: Mirefen furnish · the Lost Cemetery · Fenwick · build the **dash-leap** + **electric** abilities then their gates · deep M8–M10. proof: marsh-before · marsh-after-eerie · marsh-after-dressed |
| 2026-06-10 | **ENTRANCE fix + WALK-IN/WALK-OUT transitions (Van: can't enter the church; wants walk-through not press-E; stamp 7c920496)** ✅ | **BUG:** the chapel door tile (+ its press-E approach tile) was BURIED under the 5×5 `prop_house_paneled` collider → unreachable → couldn't enter. Diagnosed by walkability-probing every GH door tile: broken (non-walkable door tile) = **gh_chapel (worst — fully buried), gh_store, gh_home1, gh_home2** + 3 world-board doors; the chapel was the only one press-E couldn't reach (the others worked from an adjacent tile). **FIX:** moved each broken door to the walkable front step (chapel gy11→gy15, store→gy17, home1→gy28, home2→gy31, board row gy36→gy37). **FEEL:** switched ALL door/stairs transitions from press-E to **WALK-THROUGH** — doors are now edge-triggered walk zones (step onto the doorway tile → transition; `_checkDoorWalk` in update, rising-edge so spawning on a door doesn't re-fire + 180ms debounce). Applied uniformly to overworld↔interior↔floor; return-stack + state-persistence (Phase 0) intact. VERIFIED real-input: walk UP into the church → enters (bug fixed); all 6 GH building doors walk-in; walk onto the exit → back outside at the door; walk up/down the Tankard stairs; no instant-re-enter loop (stood on the spawn-door 400ms, stayed). 13 gates + 19 navGate suites GREEN, 0 errors. | ⏳ **VAN:** confirm walking into the church + the other buildings feels like walking through an opening (not a button-press); the door markers are still `prop_sign` placeholders (a doorway sprite is a small art polish). proof: walkin-chapel-reached · walkout-back-at-church |
| 2026-06-10 | **GREENHOLLOW first REAL build — FURNISHED interiors (rich library + forge; stamp 309c4d03)** ⏳ awaiting Van's eye | Stopped greyboxing: wired **6 real furniture props** (cropped from the ledgered library → public/art/furniture, registered in assets.js PROPS + interactionClasses) — **prop_altar** (LPC Statues), **anvil/bed/table/dresser/fireplace** (ElizaWy LPC Objects, OGA-BY/CC-BY/CC-BY-SA). Re-furnished the GH interiors: **chapel = a REAL stone altar** (the fountain-altar is FIXED) + pews; **forge = a real anvil + workbench**; **2 cottages = bed/hearth/table/dresser** (lived-in); **Tankard = hearth + tables, upstairs = beds**. Furniture placed solid → interiorRegion carves nav + collider → navGates still pass (all 19 interiors re-proven). Town stays populated (14 forge-composed LPC NPCs), warm pastoral, 60fps, 0 errors. 13 gates + 19 navGate suites GREEN. | 👁️ **VAN (FEEL):** does Greenhollow read + feel like a **real lived-in town you walk INTO** — beds by the hearth, an altar in the chapel, an anvil in the forge? ⚑ NOTES: the **altar + anvil crops** read right but are quick single-frame crops (precise frame-extraction polish = small follow-up); more furniture variety (chairs/shelves/rugs — all in-library) + wiring the new **Victorian/Colonial building sets** into the town are the next finish steps. proof: gh-before-town · gh-interior-chapel-altar · gh-interior-home-furnished · gh-after-town-populated |
| 2026-06-09 | **PHASE 2 — Greenhollow to finished quality + enterable interiors** (the showcase; stamp 6848b65f) ⏳ awaiting Van's eye | Built GH into a lived-in town with **7 enterable interiors** (Tankard +upstairs from Phase 0, + 5 new: **Hodge's forge**, **Pem's store**, **the chapel** — a real nave (altar-font + pews + aisle), **2 cottages**), each via a working building door (`via:'door'` → `interiorRegion()`), **furnished** (extended `interiorRegion` with furniture that carves the nav so navGates still validate), state-persistent (Phase-0 chest/save path). + a **non-solid flora enrichment** (14 varied-scale/tint undergrowth clumps framing the lanes — the variety rule, zero nav impact). LIVE-PROVEN (real input, 0 errors): all 5 building doors enter their interior; forge + chapel screenshotted (furnished, walkable, enclosed view). **9-factor DoD:** 8 ✅ (nav/readability/art-dir/density/collision-depth-interaction/audio[mus_green+amb_birds]/perf 57fps/no-soft-locks), composition 🟡 **good** (greybox furniture flagged). 13 gates + 15 suites GREEN (17 regions; all 9 interiors navGate-valid). | 👁️ **VAN (FEEL):** does Greenhollow now read + feel like a **real, lived-in town you can walk INTO**? ⚑ **ART GAPS (HARD RULE 9, flagged not faked):** (1) **interior furniture is greybox** — barrels/fountain-as-font/fences-as-pews/a potted bush; proper **beds/tables/shelves/an anvil/a hearth/an altar are an art need** (the chapel reads best; forge/homes want the furniture art); (2) the overworld is a **good small village**, not a dramatic sprawl (GH is small by design; the 3 huge cities are the full game). proof: p2-before/after-greenhollow · p2-interior-forge · p2-interior-chapel |
| 2026-06-09 | **WORLD-CORRECTNESS bugfix — 3 confirmed bugs** (dash-tunnel / Peaks path / white squares) ✅ awaiting Van real-speed confirm | **B1 DASH-TUNNEL (real, NOT a throttle artifact — I was wrong before):** swept collision (`_sweptDodgeVel` casts the feet-box along the dash path in 4px steps, clamps at the first solid, ends the dodge). Real-input dash UP/DOWN/LEFT/RIGHT into a boulder (54px), tree trunk (16px band), solid hedge (12px band), building (64×82) — **all BLOCKED**. **B2 PEAKS PATH (honest):** prior "walks" claims were INVALID — they teleported INTO the Peaks, never testing the path. A real cell-walk stuck at the fork: last session's class-solidity flipped the decorative scatter (placed `solid:false`) to solid, fragmenting the corridor (walk couldn't thread it, dash tunnelled — hence "only dashing gets up"). Fix: `isSolid()` lets an explicit `solid:false` WIN. Verified real-input WALK to BOTH forks' Peaks exits, no dashing. **B3 WHITE SQUARES = signs:** wired press-E READ + text on every sign. 13 gates + 11 suites GREEN, 0 errors. | 👁️ **VAN:** (1) confirm at real speed: dash blocked into a rock/stump from above + walk up both forks; (2) scatter rocks/bushes are non-solid by design (keeps the path clear) — say which standalone rocks should BLOCK; (3) ⚑ ART: the sign sprite is a blank-white placeholder (`sign.png` 235B) — proper signpost art is a GAP. proof: b1-swept-dash-blocked · b2-walk-up-foothill-midpath · b3-sign-read-text |
| 2026-06-09 | **INTERACTION-CLASS TABLE — consistent rules per asset type** (Van: bushes/cut/dash inconsistent per-region) ✅ (1 item flagged) | Replaced ad-hoc per-object behaviour with an SSOT class table (`src/data/interactionClasses.js`): every asset declares solid/cuttable/throwable/pushable/searchable, applied UNIFORMLY in every region. **CUT is now CLASS-driven via the SWORD SWING** — any bush is cuttable in EVERY region (the "cut works in Peaks not GH" fix): verified GH (471 cuttables) + Peaks (184) cut identically (hidden + loot). **SOLIDITY from the class** (`isSolid(key, placementSolid)`; bushes honour their small-vs-big placement flag, everything else = its class). **SAFE-ZONE RULE set + explicit:** Peaks town = a SAFE HUB (within `combat.safe` ring) — the sword **swings + cuts foliage** but deals **NO combat damage** there (matches GH); combat lives OUTSIDE the town ring (verified `_inSafeHub()` true at the plaza). **DASH-OVER:** added a dodge **blocked-check** (cancels the dash push into a solid it's touching, so the 611px/s burst can't creep through) — fixes it at real framerate; the **4fps Playwright throttle still shows creep (an artifact)** so the dodge-vs-solid wants Van's real-60fps confirm. **GATE:** verify now FAILS if any placed asset lacks a class (no undefined behaviour). 13 gates + 11 suites GREEN · 0 console errors. | ⏳ **VAN:** (1) confirm dashing down onto a rock/stump is blocked at real framerate; (2) OK that cuttable includes GH **hedge** bushes (they respawn ~12s)?; (3) **building base outline / porch** NOT done (flagged below). 👁️ **Ambiguous classes flagged:** `prop_bush` is dual-role (small walk-through vs solid hedge — handled by the placement flag; a dedicated big_bush/reed/stump asset is a GAP); `prop_sign` set NON-solid (readable waypost) — confirm. | proof: ix-cut-bush-gh |
| 2026-06-09 | **DEPTH-SORT fix — hero rendered UNDER buildings when standing in FRONT** (Van: collision correct, but y-sort wrong; stamp d3f9ed25) ✅ | **DIAGNOSED (depths logged):** the depth-sort tracked ACTORS (hero + NPCs) at offset **18**, but their feet (collision base = `body.offset.y + body.height`) are at **30** — so the sort key sat ~12px ABOVE the feet. Standing in front of the forge: hero feet **8120** (below the base 8112 = in front) but sortKey **8108 < building 8112** → drew **behind**. Props already sort by their opaque base (`b.offY+b.h/2`), so only the actors diverged. **FIX:** track hero + NPCs by `body.offset.y + body.height` = the **feet line** = the SAME ground anchor the pixel-truth collider + every prop use. **RE-TESTED (depths + eyes-on, stamp 88082d7e in-frame):** building FRONT heroDepth **8120 > 8112 → draws ON TOP** ✅; BACK **8064 < 8112 → occluded behind** (walk-behind preserved) ✅; tree FRONT 7212>7170 on top / BEHIND 7150<7170 occluded by canopy ✅; rock front on top ✅; NPCs now sort by feet (30) too. One rule, applied consistently to actors + props → collision and rendering agree. **13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN — hard-refresh (stamp = `88082d7e`), confirm the hero now draws ON TOP when standing in front of houses/trees, and behind when walking up behind them.** | proof: depth-house-front-ontop · depth-house-behind · depth-tree-front · depth-tree-behind |
| 2026-06-08 | **VERSION STAMP + collision re-verify on a confirmed-current build** (Van: still broken after hard-refresh — but couldn't tell if on stale code) ✅ | **PART 1:** added an always-visible on-screen **build stamp** (bottom-left, Vite `transformIndexHtml` plugin → re-runs `git rev-parse` on EVERY page load, so it always reflects the served code; defeats browser cache). Current stamp reads **`build e1a1b361 · 2026-06-08 23:00`** — Van's screen must match this hash to be on the latest. **PART 2 — re-verified collision WITH the stamp in-frame (build e1a1b361):** pixel-truth test → **GH 618 + Peaks 321 objects, CLIPPED-IN = 0** (no clip into any visible sprite). Real-input drives: **house** FRONT stops at base (0px), BACK walks 80px behind the roof (walk-behind), LEFT 0px, RIGHT clean; **rock** stops at the edge (0px); **mountain** keep-road mouth→Cinder Keep reached, W-flank dash **blocked** by the cross-wall. Honest note: **trees** stop ~10–11px *short* of the trunk (a small approach GAP, NOT a clip — cosmetic; you walk around trees). **VERDICT: collision is CORRECT on the current build** — front/back/sides clean, walk-behind works, mountain sealed + forks walkable. If Van still sees clipping, his browser is on a STALE bundle — hard-refresh (Ctrl+F5) and check the stamp = e1a1b361. **PART 3 (loading path):** dev server `npm run dev` on :5273 serves the live working-tree source (no preview/CDN cache); the ONLY staleness risk is the browser bundle cache → **hard-refresh + confirm the stamp hash** = guaranteed current. **13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN: hard-refresh, confirm the stamp says `e1a1b361`, then re-judge** — if it clips with that stamp showing, screenshot it (stamp visible) so we see the exact case. | proof: vstamp-boot · recheck-house-front/back/left · recheck-rock · recheck-tree · recheck-mountain-keep |
| 2026-06-08 | **PERSPECTIVE-AWARE VERTICAL collision** (Van: left/right correct, up/down clips/odd) ✅ | **DIAGNOSED** (overlay + drive): the full-opaque-silhouette collider was right horizontally but wrong vertically for tall buildings — FRONT stopped at the base correctly (+0–8px), but the **BACK blocked the hero at the ROOF TOP** (feet 128px above the base) so you couldn't walk behind the building (proof: diag2-back-clip-forge = hero perched on the roof). **FIX:** the ONE rule (`solidBox`) is now perspective-aware — **horizontal stays the opaque silhouette WIDTH** (preserves the left/right fix); **vertical is the GROUND FOOTPRINT anchored to the opaque base, by class:** trees → trunk band; **BUILDINGS → a BASE GROUND BAND** (depth ≈ opaque-width × 0.5, ≥24px), so you stop at the front base AND **walk BEHIND the upper roof** (y-sorted); **compact rocks → full silhouette** (unchanged — solid top-to-bottom). Re-drove the forge: FRONT stop at base (0px), **BACK now walks 80px behind the roof + stops at the band top** (vs perched on the roof), LEFT/RIGHT stop at the opaque sides (preserved). Boulder front/back unchanged (0px, full silhouette). **Pixel-truth test updated** to assert the GROUND-BASE truth (buildings: L/R at the band level, BACK at the band top — a full-height-front collider now FAILS) and re-run all 4 faces: **GH + Peaks → CLIPPED-IN = 0** (buildings + rocks + trees; residual = the known foreign-bush/cliff stop-shorts near trees). **GATE #10** extended: buildings must be a base band (full-height = FAIL), every solid's width = the opaque silhouette. Keep-road mouth→Cinder Keep + GH still walkable. **13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN — re-test:** walking into a house FRONT stops at the base (no clip), the BACK lets you walk behind the roof, sides still stop correctly; rocks stop at every edge. | proof: fix2-overlay-baseband · fix2-front-stop · fix2-walk-behind · (before: diag2-back-clip-forge) |
| 2026-06-08 | **PIXEL-TRUTH COLLIDERS + a NON-CIRCULAR test** (implement the diagnosed fix) ✅ | Replaced the frame-based `solidBox` with one that derives every collider from the sprite's **OPAQUE PIXELS** (`OPAQUE_BOUNDS`, precomputed by `scripts/build_opaque_bounds.mjs` — a minimal zlib PNG decoder → `src/data/opaqueBounds.js`, the SSOT both scene + gate read). Rule: **trees** → collider = the opaque trunk/ground band (walk behind canopy); **everything else** → the **FULL opaque silhouette** (rocks solid top-to-bottom; buildings solid to the visible walls). Collider + y-sort both anchor to the opaque bounds, not the frame → no padding overhang, no clip-in. Also fixed a real **flipX** bug (off-centre silhouettes — pine trunks — now mirror their collider with the sprite). **NON-CIRCULAR TEST** (`scripts/collision-pixeltruth-test.browser.js`): ground truth = the texture's opaque pixels (canvas, independent of the collider); drives the hero into every solid from 4 faces and asserts the leading body edge stops within ±8px of the OPAQUE edge (skips faces a neighbour/cliff blocks). **RESULT: Greenhollow + Sundered Peaks → 939 objects, 2,556 cleanly-driven face-tests, CLIPPED-IN = 0** (no clipping into any visible sprite; residual = a few dense-scatter "stop-short" cases the harness couldn't attribute, all GAP never CLIP — colliders verified exact by direct probe: oak + house_b collider bottom = visible base to the px). **Eyes-on:** overlay shows the collider now **fills the whole rock** + trunk-only on trees (vs lower-half in the diagnosis); the hero **stops exactly at a boulder's visible top edge** (0px, vs 24px buried before). Keep-road mouth→Cinder Keep + GH still walkable (bigger colliders didn't block paths). **GATE #10** now asserts pixel-truth: every collider must sit inside its opaque silhouette (no padding overhang) with its bottom at the visible base — a frame-padding collider FAILS. **13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN — re-test:** you should stop cleanly at the visible edge of every rock/house/tree (no sinking in, no invisible walls). | proof: fix-overlay-scree · fix-overlay-forge · fix-hero-stops-rock |
| 2026-06-08 | **COLLISION DIAGNOSIS — the test passes but the game clips (the test is circular)** (Van: visibly broken everywhere) ⚠ DIAGNOSIS-ONLY, no fix | Proved the auto-test is **self-referential**: it asserts "hero doesn't cross the COLLIDER centre" using the collider as ground-truth — but the collider IS where the hero stops, so it always passes even when the collider doesn't match the visible sprite. **Visual proof** (overlaid the collider RED vs the sprite's visible bounds CYAN, + drove the hero in): `diag-clip-rock` = hero **buried to the waist inside a boulder**; `diag-clip-house` = hero **walked 58px into the forge and vanished behind it**; `diag-overlay-forge`/`-town` = the building/rock upper mass sits ABOVE the collider. **Quantified:** boulder collider top **24px below** the visible top (top ~45% walkable-into); forge **58px**; eliza houses (house_a/b) + fountain carry **transparent frame padding** (24px sides, 23px bottom) so the frame-based collider floats in empty space. **ROOT CAUSE:** `solidBox` derives the collider from the PNG **frame × a fixed fraction (lower-55%)**, NOT the **opaque pixels** nor the object's true solid extent — so (1) padding ≠ wall, (2) "55%" is wrong for compact rocks (should be ~100% solid), (3) it "varies per house" because each PNG's padding differs. Player body is fine (correct feet-box). **PROPOSED FIX (next session):** a PIXEL-TRUTH test+collider — derive ground truth from each texture's opaque-alpha bounds (probe already built), anchor the collider to the silhouette (compact=full opaque box; walk-behind=opaque ground band), and assert the hero stops within ±~8px of the OPAQUE edge (not the collider) so it can't pass while clipping. Full writeup: **docs/COLLISION-DIAGNOSIS.md**. **13 gates GREEN · 0 console errors · no collision code changed.** | ⏳ **VAN:** confirm the diagnosis matches what you see (hero sinks into rocks/houses); next session implements the pixel-truth collider+test. | proof: diag-clip-rock · diag-clip-house · diag-overlay-forge · diag-overlay-town |
| 2026-06-08 | **COLLISION OVERHAUL — ONE rule + auto-test EVERY object from 4 directions** (Van: "houses off to different degrees" = per-object guessing; still clips/can't climb path) ✅ | **Stopped hand-tuning colliders per object.** Established ONE rule (`solidBox` in assets.js): a solid's collider is DERIVED from its sprite by CLASS — **CANOPY** (trees → solid trunk, walk behind the canopy), **MASS** (rocks+buildings → ~full width × the lower ~55% of the sprite, base-anchored so you stop AT the front and walk behind the top), **SMALL** (barrel/sign/fence → the base footprint). Applied UNIFORMLY in `_buildRegion` (collider + y-sort both from the one rule); deleted the per-building hand-tuned footprints. **AUTO-TESTED every solid from all 4 faces** (`scripts/collision-autotest.browser.js`, manually-stepped real physics): **Greenhollow 2027 objects / 6628 face-tests / 0 fails · Sundered Peaks 1454 objects / 5258 face-tests / 0 fails** = **3,481 objects, 11,886 directional tests, ZERO pass-throughs.** **MOUNTAIN ASCENT:** real DASH-test at W/E perimeter + the cleft cross-wall flank = all **BLOCKED, no tunnel**; real-input walked **BOTH foothill forks** GH→Peaks — QUARRY (NW) and KEEP (NE) both reach the top (the Shard-I gate opens), and the Peaks keep-road mouth→Cinder Keep walks clean. Eyes-on (real keys): hero stops **clean (no clip)** at a house front, at a boulder, on the impassable flank, and **reaches Cinder Keep up the dirt road** (occluded behind it = walk-behind y-sort working). **GATE:** #10 now asserts THE RULE — every MASS object's collider must cover ≥60%×≥40% of its frame (can't drift "off to different degrees" again). **13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN — re-test:** stop cleanly at every house/rock/tree from all sides (no clip-through, no walk-into-front); both forks up are walkable. **WORKING ROUTE UP:** from Greenhollow's north trailhead hold **UP** to climb the foothill trunk to the waystone fork; then **UP+LEFT** = the QUARRY fork (exits NW) **or UP+RIGHT** = the KEEP fork (exits NE) — *both* drop you in the Peaks town (the rockfall gate opens once you carry **Shard I**); then hold **UP** straight up the central dirt **keep-road** through the cleft pass to **Cinder Keep**. | proof: collision3-house-blocked · collision3-cliff-flank-blocked · collision3-path-up-keep |
| 2026-06-08 | **PEAKS COLLISION — PROPERLY fixed with a REAL-INPUT test** (Van: STILL walk into house fronts + dash up the cliff sides + can't find the way up) ✅ | Built a **real-input test helper** (`window.__drive`: holds the ACTUAL movement keys + chains real DASHES, awaits real frames) and used it for EVERY assertion (prior "fixes" used `body.setVelocity`, which the input loop zeroes each frame = fake passes). **THREE fixes, each real-input-proven:** (1) **HOUSE FRONTS** — the building colliders were thin BASE strips, so the front/door/walls *above* the base were walkable-into. FIX: enlarged the building footprints (forge/houses) to cover the **full mass** (bottom-aligned so y-sort is unchanged) → drove the hero into all **N/S/E/W faces** of the town hall = **blocked on every face** (can't pass through; no interiors yet). (2) **CLIFF FLANKS** — real **DASH** test (2s of chained dodges) into the W/N perimeter + up the W flank toward the keep = **all BLOCKED, zero tunnel** (the cleft cross-wall + 2-tile perimeter hold even against the dash). (3) **NO PATH UP** — the town hall + fountain sat **on the keep-road corridor (x29–31)**, blocking it. FIX: moved them to the flanks → the central dirt KEEP-ROAD is clear; drove the hero from the Peaks S mouth **straight N up the road, through the cleft pass, to Cinder Keep** = reached it ✅. **GATE:** extended collision-matches-visual-mass (#10) — a BUILDING whose footprint covers <70%×<50% of its frame now FAILS (a thin base strip = walk-into-front). **All 13 gates + 9 suites GREEN · 0 console errors · 61fps.** | ⏳ **VAN — re-test Peaks:** you should be unable to walk into any house face or dash up the cliff sides; the **keep-road up the middle of the town (through the cleft gap) is the only way up.** **THE ROUTE UP (plain words):** from Greenhollow walk **NORTH** off the village's top edge onto the **foothill trail** → up the lane (it forks; either reaches the top) → the **ROCKFALL** at the top opens once you carry **Shard I** (from the Marsh) → into the Sundered Peaks stone town → walk **straight NORTH up the central dirt keep-road**, through the **gap in the cliff** (the riven cleft pass), to **Cinder Keep** at the top. | proof: collision2-house-front-blocked · collision2-cliff-flank-blocked · collision2-path-up-keep |
| 2026-06-08 | **PEAKS COLLISION BUGFIX** (Van: walk through houses + dash up the mountain sides) ✅ **live-diagnosed + fixed + re-tested** | Honest live diagnosis found THREE real holes (my earlier "blocked" tests were INVALID — I drove via `body.setVelocity`, which the input loop zeroes each frame; re-tested by holding the actual movement keys). **ROOT CAUSES + fixes:** (1) **solid props had DYNAMIC bodies in a STATIC `solids` group** → they never entered the static collision tree, so the player walked straight through **every house/boulder/fountain** (only the static cliff RECTS blocked). FIX: each solid prop now gets a **static collider RECT at its footprint** (the proven cliff-wall pattern). This fixed GH/Marsh too (their buildings silently didn't block either). (2) the **riven-cleft shoulders only spanned x18–27 / x33–42**, leaving the **W/E flanks of the upper bowl OPEN** → walk up the sides to the keep, bypassing the path. FIX: the cleft is now a **full cross-wall with a pass-only gap (x27–33)** — the keep-road through the pass is the ONLY way up. (3) Peaks scatter **pines were `solid:false`** (walk-through). FIX: pines→solid; widened the keep-road/plaza skips so solid obstacles never block the path. **GATE:** extended collision-matches-visual-mass (#10) — a SETTLEMENT (non-route) region must have its tree/house/etc. props `solid:true` (route regions exempt); this would have caught the non-solid pines. **RE-TESTED LIVE (key-driven, valid):** hold-W up the keep-road = walked 31 tiles through the pass to the keep ✅ (path works); hold-W on the W flank = **BLOCKED at the cross-wall** ✅ (can't bypass); hold-W into the town hall = **STOPPED at the base** ✅ (can't walk through); GH plaza still freely walkable (not stuck) ✅. **13 gates + 9 suites GREEN · 0 console errors · 61fps (rebuild spike 40ms — the extra static rects).** | ⏳ **VAN — re-test Peaks: you should NOT be able to walk through houses/boulders/pines, and the cleft pass should be the ONLY way up to the keep (no dashing up the sides).** Any spot still passable? | proof: peaks-collision-cleft-wall (cross-wall+pass) · peaks-collision-blocked-building (hero stopped at the wall) |
| 2026-06-08 | **GH→PEAKS VERTICAL SLICE** — composition + interaction + audio (the region-completion DoD pass) ✅ **live BEFORE/AFTER** | Van's #1 note: the slice read as a **monotonous tree-corridor** (rows of identical big conifers walling a path). **HEADLINE FIX — placement variety + composition:** reworked the foothill treeline + the streamed green belt — vary tree SIZE (saplings→big canopy via per-prop `scale`), SPECIES (oak/pine mix), SPACING (off-grid jitter + random H-flip, no rows), layered UNDERGROWTH (bushes/ferns at varied scale), low-silhouette GAPS (breathing room), and CLUSTERED woodland with CLEARINGS (a few clump-centres per chunk, gaps between) — added `scale`/`flip` support to both the streamed-chunk + region prop renderers. BEFORE=identical-pine hedge → AFTER=a varied, layered, composed forest path (the dirt lane still parses — R-VIS held). **WORLD-INTERACTION INTEGRITY (tested live, not assumed):** ✅ OBJECT PERMANENCE — opened the Peaks quarry cache (+45g), then re-pressed E = **no re-grant** (added a `looted` guard + `isOpened` check — closed a same-session re-loot hole), then cycled the region away+back = **chest did NOT respawn**, gold held; ✅ COLLISION — drove the hero N into a Peaks cliff at 160px/s for 1s → **blocked at 2px** (881 solid bodies); chest pickup is the wired world-interaction (cut/push/search remain deferred engine features per INTERACTABLES-DESIGN). **AUDIO (wired a minimal layer):** region AMBIENT beds swap on region change (✅ confirmed **amb_wind in Peaks / amb_birds in the green**, OGA CC0) + UI/feedback SFX (Kenney CC0: pickup/confirm) on chest-open + the grant; music beds left as a hook (pending Van's download). **GAME-FEEL:** the 2D item-get flourish (glow rises + scales + fades + a confirm sting) on chest-open + the Cinder Keep grapple grant (the tool 2D-fallback). **STONE-TOWN C4 TRIAL:** trialled the staged Allacrost stone set side-by-side vs the ElizaWy hero/houses → **REJECTED (C4 fail — a higher-detail painterly hand clashes with the flat ElizaWy LPC)**; the slice keeps the cohesive cool-tinted ElizaWy houses (+ the eliza-structure stone kit for future cold-stone assembly). **All 13 verify gates + 9 suites GREEN · 0 console errors · perf 61fps/avg16.7/max21.7ms.** | ⏳ **VAN — walk GH→foothill→Peaks: is it now a BEAUTIFUL, varied, composed slice** (forest framed by varied natural growth, not a corridor)? Do objects have permanence (looted chests stay looted), can you not walk through cliffs/trees, do the pickup/ambient sounds land? **Is it finally "amazing, not just correct"?** Remaining for full region-DoD: the 2 music beds (your itch.io download), a true stone-town set (or assemble from the eliza kit), and the advanced interactables (cut/push/search). | proof: slice-BEFORE-foothill vs slice-AFTER-foothill (varied forest) · slice-AFTER-peaks · stone_c4_trial (reject) |
| 2026-06-08 | WORLD-BUILD — **PEAKS RE-FIX** (C1+C2 cohesion) + **ENTRANCE-CONTRACT** model ✅ **live-verified, BEFORE/AFTER** | The Peaks read WRONG (improvised): ground was stone-TINTED green grass (C2 fail) + cliffs were scattered single props with green showing through (C1 fail). FIX by WIRING in-repo assets (no sourcing): **(C2)** added `rock`(Rock_Gray)/`snow`(Snow_1)/`stone`(Stone_Tan) to the terrain autotiler (`build_terrain_autotile.mjs`→`terrainTiles.js`, all already in the shipped terrain-v7 atlas) + laid a **rock** patch under the whole Peaks bowl, **snow** cap on the high N slope, **stone** town floor → the ground is now real GREY ROCK, zero tinted grass. **(C1)** extracted a seamless cliff FACE + rock-LIP TOP tile from eliza `cliff_summer.png` (`public/art/terrain/cliff_wall.png`+`cliff_top.png`, OGA-BY, ledgered) + a new `_buildRegionCliffs` RenderTexture that renders cliff MASSES (`R.cliffWalls`) as ONE continuous face (face body + lip top) → cliffs are now continuous (cleft + perimeter), 0 orphan tiles, 0 gaps. + Ice tarn (pale tint), denser scree/pine scatter to the wild band. **LIVE-VERIFIED (1280×720):** BEFORE vs AFTER — ground green→grey-rock ✓, cliffs scattered→continuous faces (the riven cleft + W/E/N perimeter) ✓, snow cap ✓, seam green-foothill→stone-Peaks bleeds ✓, town reads as a stone settlement ✓. **PERF IMPROVED: max-frame 36.7ms→20.0ms** (one cliff RenderTexture ≪ ~640 scattered cliff props), 60–61fps, 0 console errors. **PART B — ENTRANCE CONTRACTS:** formalized the modular connection model as DATA (`src/data/entrances.js`: every region's N entrances = {pos, connects-to, gate}) for GH/Belt/Marsh/Foothill/Peaks + reserved stubs for Coast/Emberwood/Spire; new **verify gate #13 ENTRANCE-COHERENCE** (two-sided handshake + gate matches gating.js + reachability from Greenhollow) — demoed it catches an injected coord mismatch, reverted. **All 13 verify gates + 9 suites GREEN.** GH/Belt/Marsh + the Peaks ENTRANCE (foothill→rockfall→shard_1) untouched/coherent. ⚑ STILL FLAGGED (not faked): the terraced-stone-town EXTERIOR building set (brick-house stand-ins remain) wants a real LPC stone-town hunt. | ⏳ **VAN — walk GH→foothill→Peaks: does it now read as a real MOUNTAIN** (continuous cliffs not scattered tiles, grey stone ground not green, snow cap, full-not-sparse)? Is the **green→stone seam** seamless? And is the **entrance-contract model** sound (regions connect via a few verified entrances; interiors stay free to rebuild)? Last thing to source = the stone-town building set. | proof: peaks-refix-BEFORE-town vs peaks-refix-AFTER-townwide/-cleft (continuous cliffs)/-seam/-perimeter-tarn |
| 2026-06-08 | WORLD-BUILD — **SUNDERED PEAKS** (the N region, painted to WORLD-BLUEPRINT) ✅ **Playwright BACK — live-verified** | Built the **first region after the re-centre**, into its reserved N block: `SUNDERED_PEAKS` (x288–348 / y218–278, 60×60) + the `FOOTHILL_ROUTE` (x303–333 / y278–288) bridging it to Greenhollow's N edge — both as worldmap.js region adapters (the Marsh/Belt pattern). **Peaks** = a mountain BOWL framed by real **cliff/rock art** (extracted `cliff_face` + `rock_crag/boulder/small` from the licence-safe ElizaWy eliza-terrain sheets, OGA-BY → `public/art/terrain/`, PROPS + ledger): perimeter + internal CLIFF WALLS (invisible tile-colliders + cliff visuals) frame a terraced stone **TOWN** (no-aggro hub: town hall/houses/well/cast — the canonical Miner/Hunt/Stranger/Bounty/Stonewright from peaks.js, M11/SP2/SP3/SP5 wired into the Overworld QuestEngine), the looming **CINDER KEEP** (the grapple+shard_2 GRANT point), the riven **CLEFT** with a keep-road pass, a high-pass tarn, scree slopes with the **6 mountain combat archetypes** (live outside the town ring), and a quarry cache. **FOOTHILL** = a channelled, LEGIBLE, FORKING lane (trunk → quarry-road / keep-road, + a spur cache) walled by cliff/scrub colliders. **GATE (gating.js exact):** a **rockfall** seals the Peaks mouth until `shard_1` (the Marsh truth) is borne; **GRANT:** entering Cinder Keep records `tool_grapple` + `shard_2`. **SEAM:** Peaks.S=278 ↔ Foothill.N=278, Foothill.S=288 ↔ GH.N=288 (exact, seam-coherence GREEN); green→stone ground-tint bleed across the foothill band (mirror of the bog seam). **LIVE-VERIFIED (Playwright restored):** walked GH→foothill→(gate blocks w/o shard_1, collider confirmed)→granted shard_1→Peaks→town→cleft+keep→grapple grant FIRED (`tool_grapple`+`shard_2` recorded, banner shown). 0 console errors; **perf 61fps, avg 16.7 / max 31.7ms** with 25 chunks + Peaks streaming. **All 12 verify gates + 9 test suites GREEN** (no-soft-lock, channelled-not-open, density-floor, seam-coherence, prop-key, collision-mass…). Greenhollow/Belt/Marsh untouched. **ART OMITTED+FLAGGED (HARD RULE 9, not faked):** real SNOW-cap tiles, a true terraced-stone-town building set, and a stone GROUND tile (the bowl grass is stone-TINTED, reads muted-green not grey) — each wants a targeted OGA/LPC pass. **DEFERRED:** the full M12 Keep-Sentinel boss fight (as Marsh's Drowned-Guardian) isn't wired into the Overworld boss flow yet — grant taken at the Keep entrance for now. | ⏳ **VAN — please WALK it (Overworld, grant yourself shard_1 or finish the Marsh): GH north → the foothill → the Peaks.** ① Does the **foothill route** read as a clear, channelled, forking PATH (framed by terrain, not an open field, not a blind thicket)? ② Does the **Peaks feel like a distinct MOUNTAIN region** (the cliffs/cleft/keep carry it — but the ground stays greenish; is the stone-tint enough, or is sourcing real snow/stone-ground tiles the priority)? ③ Is the **GH→Peaks seam** seamless (green bleeding to stone)? ④ Does the **rockfall gate** read (blocked until the Marsh) and the **grapple grant** land at the Keep? ⑤ Which art to source FIRST — snow, stone-town buildings, or stone ground? | proof: peaks-ow-greenhollow · peaks-foothill-route · peaks-foothill-gate · peaks-town-entrance · peaks-keep-cleft-combat · peaks-seam · peaks-grapple-grant-banner |
| 2026-06-08 | WORLD-BUILD **decision B** — re-centre + enlarge the world (coordinate relocation) ⚠ **Playwright DOWN** | Applied WORLD-BLUEPRINT decision B while only GH/Belt/Marsh exist (coords cheap to shift): `WORLD_CHUNKS 16→20` (world **512→640 tiles**) + `GH_ORIGIN` chunk(5,5)→(9,9), so Greenhollow centres at **(314,308) ≈ world centre (320,320)** and regions radiate symmetrically (was NW-biased). Because Marsh + West Belt are defined RELATIVE to `GH_ORIGIN`, the whole built cluster relocated as ONE coherent **+4-chunk (+128-tile)** move — internal layouts/quests/cast/combat/safe-zone untouched. **SAVES:** SaveManager **v2→v3** migration shifts a pre-recentre save's position (+4096px) + remaps every chunk-delta key (+4 chunks) → a playthrough is PRESERVED (relocated, not reset); coord-independent systems (karma/quests/inv) untouched. **HEADLESS-VERIFIED:** new coords correct (GH x288–340 y288–328; Belt x278–288; Marsh x238–278); the **GH↔Belt↔Marsh seam math is EXACT** (Marsh.E=Belt.W=278, Belt.E=GH.W=288); spawn (310,307) inside GH; all-3 within the 640 world; regionAt resolves GH+Marsh; SaveManager v2→v3 test passes (pos+delta shifted). The 4 unbuilt regions are **coordinate-RESERVED** per the blueprint (Peaks x278–353 y206–278; Spire x303–338 y152–202; Coast x388–473 y278–343; Emberwood x288–363 y363–433). 9 suites + verify GREEN; discrete fallback untouched. **⚠ Could NOT verify LIVE — Playwright MCP is down this session;** no render/walk/screenshot. | ⏳ **VAN — please confirm on your load (I could not):** ① Greenhollow renders centred (not jammed NW); ② walk GH→west trailhead→Belt→Marsh is still SEAMLESS (the seam moved with the cluster); ③ an EXISTING save (if any) loads you into the relocated world at the right spot (v2→v3 migration) — or starts fresh cleanly; ④ minimap/full-map show the player + regions correctly at the new world scale. | headless-only (Playwright down) |
| 2026-06-08 | WORLD-MIGRATION **PHASE 5** — west belt rebuilt as a terrain-CHANNELLED route (Greenhollow→Marsh) ⚠ **Playwright dropped MID-build** | Replaced the open-field scatter belt (the §2.1b VIOLATION) with an authored **WEST_BELT route region** (10×34-tile corridor between Marsh's E edge and Greenhollow's W edge): a winding dirt LANE walled by treelines/bush/water, FORKING at a junction into a NORTH scenic branch (skirts a pond) and a SOUTH direct branch (to the Mirefen hub) — **two viable routes** (WEB-NOT-LINE); a short SPUR hides a **reachable cache** (belt_cache, +30g, reward-for-looking); a chest sits on a pond **island you can SEE but can't reach** (§2.5 tease→payoff, gated on a future water-crossing tool [ENGINE]); a trail WAYSTONE landmark + a path-side TRAVELLER (Joss, safe spot near the GH end). Green→bog bleed preserved (per-chunk seam tint + oak→pine/reed gradient). **LIVE (before the drop):** captured the BEFORE (open field) + the AFTER junction (channelled dirt trail through dense wood) + the pond sealed off the lane; confirmed the route renders + reads as a route, and that the player is BLOCKED walking off the lane into the trees. **DIAGNOSED + FIXED a real collision bug on screen:** the player strolled straight THROUGH the bush walls when walking horizontally — a prop's solid footprint is only ~12px tall in a 32px tile, leaving ~20px vertical SEAMS the hero slips through. FIX = decouple collision from prop footprints: the walls are now **contiguous invisible 32×32 tile-COLLIDERS** (new `R.colliders` in _buildRegion) over every non-lane tile, bushes/trees became pure visuals. Also fixed a crash: NpcLife.setPhase choked on the scheduleless traveller (guard added — scheduleless NPC = static, the documented contract). **⚠ HONEST — could NOT re-verify LIVE after the collider fix: the Playwright MCP disconnected mid-session.** Instead PROVED it **headlessly** with a foot-box flood-fill over the ACTUAL collider rects: 893 reachable lane-cells; the GH-entry connects to the Marsh edge at BOTH branch exits (north rows 11-13 + south rows 18-20 = 2 routes); the **tease pond is sealed (unreachable)**; the **cache is reachable**. 9 suites + verify GREEN; discrete fallback untouched. NOT re-confirmed on screen (browser down): the full GH→Marsh walk AFTER the collider fix, the GH-trailhead↔belt-entry seam handoff, perf with the 276 colliders, and the full visual tour (fork/west-bog-end/density). | ⏳ **VAN — please WALK it (I couldn't, after the fix): boot Overworld, leave Greenhollow's WEST trailhead.** Does the belt now read as a CHANNELLED, forking, populated ROUTE (paths that fork, every screen has something, the pond-island tease, the hidden cache) — NOT an open empty field? Can you walk the lane GH→Marsh (both branches), and are you BLOCKED from cutting through the trees? Anything still open/empty, or any spot you get stuck? | proof/p5-00-belt-BEFORE (open field) · p5-01-junction (+crop, channelled trail) · p5-03-pond-sealed (+crop, tease) |
| 2026-06-07 | WORLD-MIGRATION **PHASE 4b** — LIVE-VERIFY the Phase-4 UI + fix what's broken (Playwright BACK) | **Tooling restored — I ran it live, screenshotted, and self-checked all 9 items (HARD RULE 9/10).** ① **Esc MENU — was BROKEN (Van right), now FIXED:** root cause = scene render order. `Options` is registered BEFORE `Overworld` in main.js, so the paused-but-still-RENDERING Overworld drew ON TOP of the active Options scene → menu invisible (pause halts update, not render). FIX = `OptionsScene.create()` calls `this.scene.bringToTop()` (no-op for the discrete callers below it). Verified: Esc now shows the full SETTINGS panel (controls/audio/mods); Esc again closes + resumes. ② **SWORD — WORKING-AS-DESIGNED (confirmed, no fix):** Greenhollow is `safeZone:true` (no combat by design) so J/sword are intentionally inert there; in the Marsh (`safeZone:false`) the sword equips + SWINGS + damages enemies — screenshotted the slash arc landing on a worm. ③ **HUD stable** — stats top-left, minimap+tracker top-right, pinned (rides the new uiCamera at zoom 1, no drift) across camera moves + combat. ④ **TOGGLES all work on screen:** O opens the world map (both region footprints + player dot); **T NOW CYCLES** the tracker full→title→off (was a MISSING handler — added `keydown-T` mirroring RegionScene's `(n+2)%3`); H hides/shows the whole HUD. ⑤ **BORDER POP — fixed/confirmed:** at the belt BOTH regions render (Marsh bog+pools+ghost LEFT, Greenhollow grass+treeline+buildings+NPC RIGHT), no empty footprint. ⑥ **PERF: 166 FPS, avg 6.06 / max 6.1ms** with both regions + 25 chunks + 9 NPCs loaded. THEN the 3 deferred visual pieces, now watchable: ⑦ **DIALOGUE PORTRAIT** — ported RegionScene's face-RT (idle/down/expression from `this._faces`); Mara's portrait renders in the box. ⑧ **OBJECTIVE ARROW** — ported the gold chevron near the player toward the active quest's NPC (`_npcByQuest`); screenshot-confirmed pointing at Hodge for SG3. ⑨ **uiCAMERA + 1.25 ZOOM** — added a zoom-1 `uiCamera` rendering only the HUD; main camera at **1.25** ignores it; `_reconcileCameras()` ignores every STREAMED/combat object on the uiCamera each frame so nothing leaks. Verified live: HUD clean during Marsh combat (both regions + VFX), Esc menu still composes on top, 0 leaks. All 9 suites + verify GREEN; 0 console errors; discrete fallback untouched (OptionsScene change is additive/safe). FLAG: the Overworld dialogue box + bottom help-bar still use fixed 432-era Y coords so on a 720-tall canvas they sit mid-screen (functional, but should be bottom-anchored to `scale.height` — small follow-up, left to avoid regressing working code this session). | ⏳ **VAN — all 9 now confirmed ON SCREEN (proof per item below):** Esc menu shows; sword swings outside the village (none inside = safe-zone by design); HUD stable; O/T/H work; border no pop; perf 166fps; portrait + arrow + 1.25 zoom added with no HUD leak. **Play-judge the FEEL:** is 1.25 the right zoom (vs 1.0)? does the objective arrow help or clutter? portrait reads right? And the dialogue box sitting mid-screen — want it bottom-anchored next? | proof p4b-02 (esc) · 05 (sword) · 06 (hud) · 07 (map) · 08 (hide) · 09 (border) · 11 (portrait) · 14 (arrow) · 15 (combat-hud-clean) · 16 (esc+uicam) |
| 2026-06-07 | WORLD-MIGRATION **PHASE 4** — Overworld UI port + both-regions border loader ⚠ **TOOLING DOWN — VISUALS UNVERIFIED** | **⚠ The Playwright/browser MCP has been down ~4 sessions and is STILL down — I could NOT run live, screenshot, or watch ANY of this. Verified by headless logic + code reasoning only.** DIAGNOSED + FIXED: (a) **"no sword"** — OverworldScene never called `player.equip('sword')` (discrete RegionScene does); added it. (b) **HUD "moving funny"** — no uiCamera + main camera at zoom 1.25 zoom-scales scrollFactor-0 HUD about the midpoint; set Overworld zoom to **1.0** so the HUD pins (the closer 1.25 zoom needs a uiCamera that ignores every streamed object — DEFERRED until verifiable). (c) PORTED the **stats HUD** (HP/MP-reserved/gold/morality/purity/time) + a **WORLD-COORD minimap** (regions + player world-pos) + **full-map (O)** + **quest-tracker (T)** + **hide-HUD (H)** from RegionScene; (d) **Esc→Options** menu (audio + modifier toggles; rebind/big-head are no-ops without the InputManager — flagged). (e) **BOTH-REGIONS-NEAR-BORDER LOADER** (the border pop): loads every region within the load margin (not one-at-a-time) so both interiors render across a border; primary region (regionAt) drives safe/combat, `_activeRegions` = the full set. **HEADLESS-VERIFIED:** the loader selects BOTH regions at the belt (none far out) + the minimap maps both region footprints in-box. Build + 9 suites + verify GREEN; discrete untouched. **DEFERRED (visual, can't do blind):** the dialogue PORTRAIT (Overworld keeps the minimal box), the OBJECTIVE-ARROW, and the 1.25-zoom-via-uiCamera. | ⏳ **VAN — please CONFIRM on your load (I could NOT watch any of these):** ① the **sword** shows when you attack (J); ② the **HUD is stable** (not drifting) — stats panel top-left, minimap top-right; ③ **Esc** opens the settings menu (audio sliders work); ④ **O** opens the world map, **T** toggles the quest tracker, **H** hides the HUD; ⑤ the **region border no longer pops** (walk Greenhollow↔Marsh — both render, no empty footprint); ⑥ **perf** holds with both regions loaded (~2× objects). Report anything broken/ugly — the visual layout is unverified. | — |
| 2026-06-07 | FIX (regression) — characters stuck facing UP in the Overworld | After booting into Overworld, the player + all NPCs could move but never turned (stuck facing up, no directional walk/idle). ROOT CAUSE: AssetLoader has two phases — `queue()` (preload: load files) + **`build()` (create: register the per-facing `tex-state-facing` anims)**. `RegionScene.create()` calls `AssetLoader.build(this)`; **OverworldScene never did**. Reaching Overworld via console BEFORE worked only because discrete Greenhollow had already run build() (anims are global). Booting straight into Overworld → directional anims never created → `spr.play('…-walk-left')` silently no-ops → every Character layer stuck on spritesheet **frame 0 = the up pose** (hits player AND NpcLife movers — both Character rigs). FIX: OverworldScene.create() now calls `AssetLoader.build(this)` (same path RegionScene uses, before any Character is built). Headless check: build() registers a WALK anim for all 4 facings (up/down/left/right). npm test + verify GREEN; discrete scenes untouched. ⚠ **Live 4-direction walk + screenshots NOT watched — the Playwright MCP server is still disconnected this session;** verified headlessly (anim registration) + by root-cause certainty (identical to the working RegionScene path) | ⏳ **VAN:** on next load, do the player + NPCs now face/animate the way they move (all 4 dirs + correct idle facing)? | — |
| 2026-06-07 | FIX — "can't walk out of Greenhollow": BOOT into the seamless Overworld | Van couldn't leave Greenhollow into wilderness. DIAGNOSED all 3: (1) SCENE-ON-BOOT = the BUG — a fresh load booted the DISCRETE GreenhollowScene (self-contained, hard-edged, can't walk out); the seamless Overworld was reachable ONLY via the console `__EMBER.scene.start('Overworld')` (no in-game route) — so Van was always playing the old walled scene. (2) COLLISION = CLEAR — no leftover edge-wall in the Overworld (depth-band has a trailhead gap). (3) CONTINUITY = CLEAR — holding west from the W trailhead, the player walked 5440→4730 continuously Greenhollow→belt→Marsh, **blockedAt null, reachedMarsh true** (no wall/invisible stop). FIX = `BootScene.create()` now `scene.start('Overworld')` (was 'Greenhollow') → a fresh, normal session starts IN the seamless world; walk west out the trailhead through the green belt into Marsh, no wall, no console. Discrete scenes stay registered (fallback). On-screen hint updated with J/Space/C combat keys. npm test + verify GREEN. ⚠ live re-load of the boot couldn't be re-verified (Playwright MCP dropped mid-session) but it's a deterministic one-liner + the Overworld walk-out was proven pre-drop; flag for Van to confirm on next load | ⏳ **VAN:** fresh load now drops you in the seamless world — walk WEST out of the village (the W trailhead lane) through the belt into the bog. No wall? (UI is the rough overworld HUD — minimap/options are Phase 4.) | — |
| 2026-06-07 | WORLD-MIGRATION **PHASE 2b** — Ashen Marsh migrated onto the overworld + the green→bog SEAM + LIVE combat (additive, discrete untouched) | Migrated Marsh as a world-coord region placed WEST of Greenhollow (east edge 10 tiles from GH's west edge = the green→bog belt). Generalised region-load to handle combat regions; ported the FULL combat stack to OverworldScene (EnemyController + PlayerCombat + J attack/Space dodge/C block + hit-feedback/HP/player-down + the boss). **LEVEL A (verified live):** all **7 archetypes** spawn + behave (counters intact — kill verified); **safe-hub holds (0 aggro in Mirefen)**; the **Drowned Guardian boss** spawns via the M9 'guardian' beat + the **lantern tool strips its shroud (hits land)**; Hagga social + Yssa M8 dialogues open; HP bar shows only in Marsh. **LEVEL B — THE SEAM (verified live):** walking Greenhollow→belt→Marsh switches regions cleanly; the ground tint is a **CONTINUOUS green→bog gradient** (sampled 0xffffff → 0xdbdfd2 → 0xb1ba9b → 0x8d9a6e — no per-chunk step), trees thin to pines + flowers→reeds/mud toward the bog; **combat does NOT bleed into Greenhollow** (0 enemies there, safe). **SAVE:** enemy-killed → SaveManager chunk-delta; kill **survives save/reload** (Marsh reloads 7→6, killed stays dead; save 0.3ms/load 2.3ms). **PERF: 166 FPS, avg 6.06 / max 6.08ms** with both regions + the belt + combat streaming. 0 console errors; all 9 suites + verify GREEN; Greenhollow (Phase 3) still gold-standard (terrain RT + safe + 7 NPCs); discrete Marsh/Greenhollow/Peaks untouched (fallback). FLAG (for the polish pass): (a) **one region loads at a time** — at the very border you can see into the adjacent region's empty footprint (belt scatter fills it, but the village/bog interior doesn't render until you enter); a both-regions-near-border loader would perfect it; (b) **deep Marsh art** (it was only tint-mood in discrete) wants its own gold-standard pass | ⏳ **VAN — play `__EMBER.scene.start('Overworld')`**: can you WALK Greenhollow plaza → west belt → bog as ONE world (green→bog bleed convincing)? Does Marsh combat + the boss work streamed? Safe zone hold at the border? | proof/ow-p2b-marsh-play + ow-p2b-seam |
| 2026-06-07 | WORLD-MIGRATION **PHASE 3** — Greenhollow gold-standard ART restored on the overworld (terrain/depth-band/decals; additive, discrete untouched) | Closed the function-first art gap to discrete-v3: (1) **AUTOTILED FEATHERED TERRAIN** — ported the v3 RenderTexture corner-autotiler onto the region (gravel plaza / packed-dirt road / tan dirt lanes / soil fields interpenetrate the grass via the lpc_terrain atlas), **killing the flat per-chunk tint-step** (the per-chunk grass is now UNIFORM, no tint). Gate D met. (2) **DEPTH-BAND → SEAMLESS BLEED** — reconciled the v3 edge-treeline (no longer a map-edge wall): the green-belt tree density now RAMPS from the village outward (dense at the edge → thinning into open belt), so the treeline reads as a continuous forest fringe bleeding into pasture, with the trailheads opening as road gaps. (3) **LIVED-IN DECALS** — 210 village decals (tufts/grass/clover/flowers/ferns/dirt, seeded, edge+pond-avoided, FLOOR-pinned) + the SW **brook** (pond tiles) + light belt flower/tuft scatter. All RESIDENT art (no texture-streaming). **VERIFIED LIVE:** terrain RT + 210 decals + 12 pond tiles present; function INTACT (cast 7, M1 completes); **seam still seamless** (walk out → region+art unload to 0 objs → back → rebuilds 534 objs incl. RT, 7 NPCs, no leak); **PERF 163 FPS, avg 6.06 / max 6.67ms** (the RT + decals cost ~nothing). 0 console errors; all 9 suites + verify GREEN; discrete Greenhollow/world.js untouched (fallback intact). DEFERRED (next session): HUD/dialogue UI + minimap (explicitly out of scope); the depth-band tuning was authored for a map-EDGE so its inner receding-dark rows still read slightly wall-like at non-trailhead edges; belt is procedural placeholder | ⏳ **VAN — play `__EMBER.scene.start('Overworld')`**: does migrated Greenhollow now MATCH discrete-v3's gold standard — feathered terrain (tint-step gone), depth-band bleeding seamlessly into the green belt, lived-in — at framerate? What still reads below the bar? | proof/ow-p3-terrain + ow-p3-belt-bleed + ow-p3-village-play |
| 2026-06-07 | WORLD-MIGRATION **PHASE 2** — Greenhollow migrated onto the seamless world (FUNCTION first; additive, discrete regions untouched) | Greenhollow now PLAYS on the OverworldScene as a world-coord REGION unit (loaded as a cohesive settlement when the player enters its central bounds; terrain/green-belt streams per-chunk). MIGRATED + re-wired to world-coords: buildings/props/plaza/depth-band (RESIDENT real art, world-coords), the full CAST (7 NPCs) on NpcLife schedules (schedule tiles → absolute world tiles so NpcLife runs UNMODIFIED), Interaction→dialogue (real Dialogue+Social engines + a minimal box), 3 secret CHESTS (loot→SaveManager chunk-delta), safe-zone (world-rect; no overworld combat yet = safe). Composed Karma/Quest/Inventory/Time into SaveManager via **link()**. **LEVEL A (verified live):** cast present (7); M1→M2→M3 + SG1/SG3 quests open dialogue + complete + chain-unlock (M1 done → M2 available); Bram's shop (social) opens; chest_wood → +40 gold + delta; safe-zone holds; NpcLife active (7 movers, schedules). **LEVEL B SEAM (verified live):** walk OUT into the green belt → region unloads cleanly (npcs 0, no error) → walk BACK → reloads (7 NPCs, chest_wood STAYS looted, M1/M2 still complete, gold intact). **SAVE/RELOAD:** F9 from storage restores position + quests + gold + chest deltas (save 0.4ms, load 9ms). **PERF: 165 FPS, avg 6.06 / max 6.15ms** with the full region streaming. 0 console errors. All 9 suites + verify GREEN; discrete Greenhollow/Marsh/Peaks still run via boot/M/N/G (fallback intact). **DEFERRED to the POLISH pass (function-first, expected rougher):** the Part 2.6 v3 terrain autotiler + boundary depth-band treeline + decals/flowers (overworld = flat per-chunk grass + region props); the polished dialogue box/portrait + HUD (minimap/full-map/quest-tracker/objective-arrow); ambient smoke/birds. FLAG (pre-existing, NOT a regression): M4 boarded-cave trigger is unwired in BOTH discrete + overworld (no cave interactable exists anywhere) | ⏳ **VAN — play `__EMBER.scene.start('Overworld')`**: does Greenhollow PLAY right (quests/social/chests/safe-zone, NPCs scheduled, save survives chunk+region cycling)? Does walking out into the green belt feel seamless? (It WILL look rougher than discrete-v3 — the art re-polish is next.) | proof/overworld-greenhollow |
| 2026-06-07 | WORLD-MIGRATION **PHASE 1** — OverworldScene shell + SaveManager (the real seamless foundation; additive, real regions untouched) | Stood up the PRODUCTION substrate every region migrates onto: ONE world-coord space (16384px), OBJECT streaming (props/orbs) around the real **Character hero** with RESIDENT real art (grass tileSprite + real tree/bush props — per the asset spike, NO texture-streaming), continuous fixed-zoom follow camera, depth-sort over the streamed set (added `DepthSort.untrack` — a flagged minimal engine gap for stream-unload). **PERF: 164 FPS, frame avg 6.06ms / max 6.7ms** traversing, 30/256 chunks loaded, 0 console errors. **SaveManager v2** (versioned, storage-adapter): persists world-position + per-chunk DELTAS (opened/killed/picked/flags). **SAVE/RELOAD ROUND-TRIP PROVEN**: picked 3 orbs → save (0.2ms) → moved the player far + re-streamed → load (0.6ms) → **position restored EXACTLY (5000,11000) + the 3 picked-orb deltas did NOT respawn**; a FRESH SaveManager reading storage (a real reopen) reads back pos + 3 deltas + schema v2 = durable persistence, not in-memory. 5 SaveManager unit tests green (round-trip, deltas, **forward-migration of a legacy save**, composition via link(), corrupt-save survival). All 9 suites + verify GREEN; real regions still run via M/N/G. EMPTY-but-WORKING (no region content yet — Phase 2). ⚑ flagged: ground per-chunk flat-tint shows a faint seam (placeholder; real per-tile terrain blends it); orb = a flagged test marker | ⏳ **VAN — play `__EMBER.scene.start('Overworld')`**: does the seamless shell WALK clean (smooth camera, holds fps), and F5 save / F9 load restore position + picked orbs? GO for Phase 2 (migrate the first real region onto it)? | proof/overworld-shell |
| 2026-06-07 | WORLD-MIGRATION **ASSET-LOAD SPIKE** — streaming REAL art mid-game (throwaway AssetSpikeScene; closes the one Phase-0 unknown) | Tested the cost Phase 0 skipped: real PNG/atlas **decode + GPU upload** mid-traversal. Streamed the **real lpc_terrain atlas (1024×2048 = 8.0MB decoded RGBA)** + a structure PNG per chunk under **cache-busted keys** (forces genuine re-decode + re-upload each load), auto-sprinting a 12×12-chunk world. RESULTS (uncapped, 60fps budget = 16.7ms, drop threshold 33ms): **STREAM budget-1 (spread uploads): 162 FPS, avg 6.5ms, max 11.6ms, 0 drops, 0 hitches** (84 real decodes). **STREAM budget-99 (WORST CASE — all decoded textures applied in one frame): 162 FPS, avg 6.1ms, max 10.9ms, 0 drops, 0 hitches** (108 decodes). **RESIDENT baseline (load once, reuse): 165 FPS, flat avg 6.06 / max 6.09ms, 0 per-chunk decode.** Read: a single 8MB atlas upload costs **~5ms** on its frame (6→11ms) — absorbable, never a dropped frame even all-at-once (the async loader naturally trickles completions). Worst transient observed under mode-thrash = one 20.6ms frame (sub-perceptible, still 0 drops). VRAM: whole repo art = **2.8MB on disk** → unique art resident decodes to **≪100MB vs the 8GB budget (<2%)**. 0 console errors (after a recycled-frame fix). **STRATEGY for Phase 1: RESIDENT — load all/per-biome art once + keep it (flat 6ms, no decode cost); the 2D art fits VRAM trivially. IF a biome ever needs streaming, it's proven safe: budget texture uploads to ~1/frame.** **GO/NO-GO: GO** — streaming real art holds framerate with zero hitches; the last Phase-0 unknown is CLOSED; proceed to Phase 1 (OverworldScene + SaveManager) with RESIDENT art | ⏳ Van: accept GO + RESIDENT strategy for Phase 1? (`__EMBER.scene.start('AssetSpike')` — M toggles STREAM/RESIDENT, B upload-budget) | proof/asset-spike-hud |
| 2026-06-07 | WORLD-MIGRATION **PHASE 0** — seamless-streaming PROTOTYPE (throwaway PrototypeScene; real regions UNTOUCHED) | Proved on a 16384px (256×256-tile, 256-chunk) sacrificial world: (1) ONE world-coord space; (2) chunk STREAMING — only **20–36 of 256 chunks** ever loaded (whole world never resident); (3) seamless HANDOFF (5×5=160-tile load ring ≫ 24-tile viewport → chunks load off-screen, no pop); (4) continuous follow CAMERA, no jump at seams; (5) POOLING — drove the **entire world W→E** and `ever-created` PLATEAUED at **36 objects** (every later load recycled, zero churn). **PERF (uncapped): 165 FPS · frame avg 6.06ms · max 6.1ms during full-speed traversal · 0 dropped frames · 0 load-hitches.** WORST CASE (all-25-chunks-in-one-frame + a fresh teleport): STILL flat ~6ms, 0 spikes — pooling makes loads effectively free; budgeting is just margin. Only spike = cold boot (one 21.8ms frame creating 725 fresh objects — one-time, sub-perceptible). 0 console errors. HONEST GAPS: did NOT test mid-game **asset-bundle loading from disk** (used generated textures) — the one remaining hitch risk (texture decode/upload); and real chunks are heavier (autotile RT bake + NpcLife + enemies + interactables). Flat-tint-per-chunk shows a faint seam = placeholder art, NOT a streaming defect (real per-tile terrain blends it). **GO/NO-GO: GO** — full seamless is viable with huge headroom (6 of 16.7ms; 165 vs 60fps); connected-scenes middle path NOT needed on perf grounds; remaining work is ENGINEERING (impact-matrix systems + an asset-bundle-load spike), not a feasibility wall | ⏳ **VAN — play `__EMBER.scene.start('Prototype')`**: does walking the seamless world FEEL right — no seam, smooth camera, holds framerate? And do you accept the GO verdict (proceed to Phase 1 OverworldScene+SaveManager) vs reconsidering connected-scenes? | proof/proto-seam-grid + proto-seamless-region |
| 2026-06-06 | Slice S1 — Greenhollow walkable + M1 dialogue | movement (WASD/arrows), camera-follow, dialogue advance/choice responsiveness | ⏳ awaiting Van | — |
| 2026-06-06 | Slice S1.5 — presentation pass | same, plus readability at normal zoom | ⏳ awaiting Van | screenshots passed self-check; feel not yet played |
| 2026-06-06 | Slice S1.6 — art foundation | same, plus walking BEHIND the forge + trees (depth-sort occlusion) | ⏳ awaiting Van | confirm occlusion reads right |
| 2026-06-06 | Slice S1.7 — 4 bug fixes (Van-found) | portrait pants (FIXED, screenshot), whole trees at edges (FIXED — RESIZE camera, no crop, no bars, proven on a 21:9 window), camera jolt (roundPixels off + lerp), E-to-talk reliability (radius 50→72 + lenient facing, tested) | ⏳ awaiting Van on CAMERA smoothness + INTERACTION feel | self-checks pass; the two FEEL items need Van's hands |
| 2026-06-06 | Slice S1.8 — 4 polish items (Van-found) | TALK DISTANCE → canonical INTERACTION_RADIUS 72→96 (deliberate ~1.5-tile gap, no clipping; probed); BRAM now stands AT the forge + greeting (pays off "he's at the forge"); TREES whole — taller world (34) + grass margin + repositioned, verified on 16:9 AND 16:10; GROUND VARIETY — grass/clover/fern/flower decals + soft worn-dirt patches (lived-in, ferns thinned to sparse). Rock decal REJECTED (looked like a box) — flagged | ⏳ Van to re-judge talk-distance + camera feel by playing | screenshots pass self-check; rock needs a real OGA-BY rock prop if wanted |
| 2026-06-06 | Slice S1.9 — 4 fixes (Van-found) | TALK DISTANCE 96→**40** (stand beside the NPC; probed: prompt ≤~40px); KARMA RE-FARM (real bug) — QuestEngine.choose() now applies each quest choice EXACTLY ONCE (persisted guard + test); re-talking a finished Mara keeps Morality +10 (verified live) + she gives a post-quest line; CHOICES now unmistakable (─ CHOOSE ─ label + amber highlight bar + numbers + ▶ cursor on the active option); TREES diagnosed — art is FINE (no padding); the clip was the PLAYER + foliage reaching the world/camera edge → fixed by insetting the play bounds (3-tile) + decal margin (player now whole at the top, BEFORE/AFTER proofs) | ⏳ Van to re-judge talk-distance + camera feel | engine re-farm bug fixed + tested; verify green |

## Open feel questions for Van (carry forward until answered)
- Walk cadence / stride — does the character move feel responsive, right speed?
- Camera follow — smooth, or choppy? (PARKED: camera lerp + pixel rounding.)
- Does the player pass cleanly behind the forge + tall trees, and in front of low props?
- Dialogue — does E-to-advance + ↑↓-to-choose feel snappy, not sticky?

| 2026-06-06 | Slice S2a — the FIRST fight (charger) | sword attack (Space; hitbox + cooldown + SSOT tuning); ONE CHARGER driven by the Monsters FSM (idle→telegraph→charge→recover); player HP from Inventory + enemy HP, death/respawn, charge damages player; FEEL: hit-pause + screen-shake + Kenney swing/hit/charge SFX + enemy white-flash + knockback; readable TELEGRAPH (charge-line + "!" + wind-up pulse) + cyan PUNISH-WINDOW. Verified live (FSM cycles, hit lands 12, charge hits player, shake/knockback/flash fire); 0 console errors | ⏳⏳ **VAN — this is the whole point**: does HITTING feel satisfying? is the DODGE readable (charge-line)? does the CHARGE feel dangerous? | proof/slice2-combat-telegraph + punish-hit |

| 2026-06-06 | Slice S2b — combat MECHANICS + Marsh (overnight) | DODGE (i-frames, beats charger) + BLOCK + PARRY built as a PURE, unit-tested system (PlayerCombat, 5 checks) + wired into the slice (Space attack / Shift dodge / K block); verified live (dodge negates a charge, block −75%, parry negates + staggers). Design rule recorded (no childhood combat). ASHEN MARSH rendered (bog mood via tints, black-water pools, dead trees, Yssa/Hagga) + M8 plays on-screen with choices + karma (verified). 0 console errors; verify green | ⏳⏳ **VAN, MORNING — all combat FEEL** (see MORNING TODO below) | proof/slice2b-* + slice3-marsh-* |

| 2026-06-07 | Combat/life tuning — threat-arrows OFF, boss threatens, monsters roam | THREE play-judged fixes (tuning + small additive, logic/counters UNCHANGED, all combat tests green). **(1) THREAT ARROWS off+gated:** the enemy DIRECTIONAL aim-line (charger/brute/ranged toward you) was always-on; now gated on `this.indicators` (ENEMY-INTUITION perk / threat-indicators toggle). Verified: fresh play indicators=false → no arrow (the readable wind-up tint/pulse/rings/guard-arc STAY = Gate E); perk on → the red aim-line + chevrons appear. **(2) BOSS (Drowned Guardian) too-easy:** Van's read = tracks perfectly but can't threaten. Slowed its TURN (guard re-faces only every GUARD_TURN_FRAMES=22 → verified ~4 face-changes per 2 circles = FLANKABLE) and raised THREAT (speed 2→3, guard advance 8×→13× → closes at **39px/s** vs old 16; dmg 14→16). Boss data + the duplicate inline stat table both updated. Phase/trick/twist/counter intact. **(3) ROAMING:** pre-aggro idle was static; now a gentle patrol near home (reuses NpcLife-style seeded per-agent jitter — staggered, varied speed/path, sometimes pauses, stays within ROAM_RADIUS_PX=80, bosses excluded); de-aggro at 2× range returns to roam. Verified idle enemies moved 12-19px while still asleep, then aggro/chase unchanged. standards.js NPC... COMBAT deltas: +GUARD_ADVANCE_MULT 13, +GUARD_TURN_FRAMES 22, +ROAM_RADIUS_PX 80, +ROAM_SPEED_MULT 6. No Greenhollow regression (safe zone); 0 errors | ⏳ **VAN:** arrows gone unless perked? does the boss now THREATEN without being unfair (closes in, but flankable)? do roaming enemies make the area feel alive? | proof/arrows-off-default + arrows-on-perk |
| 2026-06-07 | COMBAT JUICE PASS — Marsh first fight (feel, not logic) | AUDIT (combat feel never play-judged): BEFORE = hit-pause 4f, shake 0.006, enemy white-flash ~6f, player red-flash, knockback 240/8f, swing/hit/charge SFX, telegraphs (charge-line/rings/guard-arc/"!"), parry→stagger. WEAK/MISSING: no INPUT BUFFER (a press during recovery was DROPPED), no enemy hit-POP (flash+slide only), DEATH = a bare 320ms alpha-fade (no pop/ring/sfx), no kill emphasis. AFTER (feel/feedback only — logic/counters/balance UNCHANGED, all combat tests green): (1) INPUT BUFFER 140ms — a press during recovery flushes the instant the swing is ready (verified buffers + no double-fire); (2) enemy HIT-POP — a 1.16× squash-punch (6f) so the strike reads as LANDING; (3) KILL emphasis — killing blow freezes 9f + shakes 0.013 (vs 5f/0.008); (4) DEATH POP — white kill-flash + expanding shock ring + 6 debris specks + a death cue (verified 7 FX objects on kill); (5) DODGE reads as a real ESCAPE — 110px + 265ms i-frames (verified negates during, takes after). standards.js DELTAS (re-tunable): HIT_FREEZE 4→5; +KILL_FREEZE 9; SHAKE_HIT .006→.008; +SHAKE_KILL .013; FLASH 90→100; KNOCKBACK 240→285, 8→9f; +HIT_POP_SCALE 1.16; +INPUT_BUFFER_MS 140; DODGE_DIST 96→110, DUR 170→180, IFRAME 230→265, CD 620→600. Benefits Marsh AND Peaks (inherited); Greenhollow safe (unaffected). npm test + verify GREEN; 0 errors. FLAG: dedicated sfx_death + sfx_hurt CC0 cues wanted (death reuses sfx_hit) | ⏳⏳ **VAN — PLAY IT**: does the hit land with WEIGHT? is the dodge a real escape? are telegraphs readable + the punish window fair? does killing feel satisfying? what still feels floaty/mushy/off — NAME it for the next pass | proof/juice-death-pop |
| 2026-06-07 | NpcLife DE-SYNC — kill the "clockwork" lockstep | Van flagged NPCs pivoting + walking in unison on phase-change. AUDIT confirmed the cause: setPhase retargeted ALL movers in one onPhaseChange frame, identical moveSpeed, dead-straight seek to the SAME tile centre, snapped facing, identical loop periods. FIX (all [FREE NOW] jitter, seeded per-NPC = deterministic): (1) STAGGER — per-NPC departure delay 0.3-2.5s (verified delays fanned 0.43-3.37s); (2) SPEED — persistent ±18% multiplier (verified speeds 50-93, all differ); (3) CHORE — per-NPC loop periods + start offsets (out of phase); (4) PATH — ±12px target jitter so paths differ; (5) EASE TURN — facing changes gated by a per-NPC cooldown (no synced pivot); (6) TEMPO trait (brisk/ambler/dawdler) scaling speed+pause+delay as PERSONALITY (Tam/Pem brisk fast, Fatley dawdler slowest). Mid-transition states verified MIXED (wait/walk/arrived), never lockstep; all still reach home (reachability intact); quests M1-3/SG1-3 + social + chests work; no Marsh/Peaks regression. Tuning in standards.js NPC_LIFE; new Part 2.6 Pillar 2 rule "crowds are noisy, not uniform". npm test + verify GREEN; 0 errors | ⏳ **VAN:** do NPCs move like individual people now (staggered, varied pace, no creepy unison)? any lockstep left? | proof/npclife-desync-transition |
| 2026-06-07 | NPC-LIFE system — schedules + chores + ambient (REUSABLE, all regions) | Built `src/systems/NpcLife.js` + wired into RegionScene so EVERY region inherits it as DATA. SCHEDULES: a world NPC gets `schedule:[[phase,tx,ty,activity],…]` (dawn/day/dusk/night) → NpcLife retargets on TimeOfDay phase-change; NPCs without one stay static (back-compat — Marsh/Peaks unchanged, verified). MOVEMENT: simple seek (Movement.drive) + collision (scheduled NPCs are movable `actors` that separate from buildings/dividers) + a light unstick nudge; FLAG: cross-divider routes need real navmesh (one Bram/Tam home had to be moved off a building/behind a hedge to stay reachable). CHORES: looped activities (hammer/tend/chat/sleep) so an arrived NPC reads as doing something (FLAG: dedicated chore anims; uses idle/attack frames). AMBIENT hook: `cfg.world.ambient` — a birds silhouette FX + a `critters` DATA slot that's inert+FLAGGED (no animal sprites licence-loaded → never faked). INTEGRATION: Interaction now FOLLOWS a moving NPC (`target` ref) so you talk mid-routine (verified: prompt tracks walking Mara, M1 opens, she freezes); moving NPCs depth-sort by footprint base; minimap/objective read live positions; dialogue freezes the village. GREENHOLLOW: the 7 cast given light routines — verified ALL reach day-work spots + night-home spots across the cycle; quests (M1-M3/SG1-3) + social + chests still work. npm test + verify GREEN; 0 errors. FLAG: day/night contrast is subtle pending the smooth dusk/dawn world-tint lerp (no night-darkening yet — ENGINE) | ⏳ **VAN:** does Greenhollow feel ALIVE now (people with somewhere to be, busy by day) without breaking quests? what still reads static? | proof/npclife-day + npclife-day-plaza + npclife-night + npclife-chore-forge |
| 2026-06-07 | GREENHOLLOW nudge — 2 bugs + district dividers + a taste of life | BUG1 (big-head defaults ON): root cause = GreenhollowScene set `devModifiers:true` → registry used each modifier's `devDefault:true` → big_head ON (scale 1.8) on fresh load. Fix: `devModifiers:false` → big-head OFF by default; verified toggle on→1.8 / off→1 (earned-modifiers design intact; Marsh/Peaks were already off). BUG2 (person clips BEHIND a building): root cause = DepthSort's header says "sort by FOOTPRINT BASE" but the track() calls passed `footprint.offY` (the footprint CENTRE, ~10-16px above where a building meets the ground) → a character at a building's base could sort behind it. Fix: sort by the footprint BASE (`offY + h/2`) for props AND characters; manor now sorts at depth 476 ≈ visual base 480 (was 464). Verified the player occludes correctly BEHIND (occluded) and IN FRONT (renders over) a cottage. DIVIDERS: short hedges (solid bushes) in open grass between plaza/homes/smithy/farm to break sightlines — all lanes/NPC/quest routes re-verified passable. LIFE (taste only, not the system): chimney SMOKE drifting from 3 homes (an FX, not a fake sprite); FLAG: animal sprites not licence-loaded → omitted for the real NPC-life pass. Cast/M1-M3/SG1-3 re-verified; no Marsh/Peaks regression; npm test + verify GREEN; 0 errors | ⏳ **VAN:** do the dividers make the districts feel like separate places, and does the taste of life start to feel inhabited? | proof/greenhollow-v3b-bighead-off + bighead-on + occlusion-behind + occlusion-front + dividers + smoke |
| 2026-06-07 | GREENHOLLOW v3 — SPACE-FIRST + real autotile transition tiles (Van flagged v2 not-quite-right) | Refined QUALITY-BIBLE Part 2.6 with town-craft (space-first not path-first / enclosure / off-centre node / districts / path hierarchy / boundary DEPTH BAND / diegetic trailheads / real transition tiles). Applied to Greenhollow: (1) loaded the **[LPC] Terrains autotile atlas** (bluecarrot16 et al., CC-BY/CC-BY-SA/GPL, ledgered) + a corner-autotiler (parsed the .tsx → src/data/terrainTiles.js; RegionScene renders a grass base + a RenderTexture of feathered transition tiles) — gravel plaza / dirt road / light-dirt lanes / soil fields now INTERPENETRATE grass (no hard squares). (2) re-laid SPACE-FIRST: an off-centre gravel PLAZA (fountain + notice board) ENCLOSED by buildings fronting it, smithy set apart W, homes district SE (cottages + back-yard fences), farm margin S (fenced soil field); path HIERARCHY gravel→road→lanes→a desire-path. (3) BOUNDARY DEPTH BAND — 3 rows deep, scale+tint gradient (front big/bright → back small/dark) + undergrowth, marsh-gloom W / foothill-grey N, narrowing trailheads at the exits (spillCount 0). (4) bushes/undergrowth now SOLID (fixed prop_bush footprint; 33 solid bushes; player verified BLOCKED) to channel movement. (5) camera deadzone (220×150) so you can approach edges. Cast/M1-M3/SG1-3 + Bram shop re-verified live; 3 secrets kept; no Marsh/Peaks regression. npm test + verify GREEN; 0 errors. FLAGS: bench + real crop-growth-stage sprites wanted | ⏳ **VAN:** does Greenhollow read as a PLANNED, enclosed, lived-in village now, and can you feel the world continuing past the treeline? | proof/greenhollow-v3-overview + v3-plaza + v3-transition + v3-depth-band + v3-farm |
| 2026-06-07 | HUD — stats + map + quest tracker + objective marker (shared, all regions) | Built into RegionScene so every region gets it. STATS panel (top-left): HP (Inventory/combat), MP **reserved+greyed** (no mana system yet — never a fake value), Gold (Inventory), Morality+Purity with tiers (Karma both axes), Time phase + day (TimeOfDay, now instantiated + ticked). MINIMAP (top-right) + FULL-MAP overlay — rendered from the real ground rects + ponds + props + NPC POIs + player. QUEST TRACKER (QuestEngine active quests → title + current objective; T cycles full→off→title). OBJECTIVE arrow toward the active quest-giver (basic directional; the Fable glowing-path FLAGGED as follow-up). Toggles via the canonical control map (O map / T tracker / Y marker / H hide-HUD; remappable). Verified LIVE: HP shrinks on damage (45→68px), gold 20→160, time D1→D4, karma choice moves tiers, accepting a quest fills the tracker, toggles work (incl. real keypress). npm test + verify GREEN; 0 errors. Doc: docs/HUD-DESIGN.md. FLAGS: mana/MP, glowing-path objective, toggle-state persistence (needs save plumbing) | ⏳ **VAN:** is the HUD clean/readable/unobtrusive, and do the map/tracker/objective toggles feel right? | proof/hud-full + hud-fullmap + hud-tracker-title + hud-off |
| 2026-06-07 | GREENHOLLOW — natural/immersive pass + TREE ROOT-CAUSE | **AUDIT (candid):** the "gold-standard" village was real-art but all biome borders were HARD square tile edges (no transition tiles loaded — the LPC terrain-v7 atlas exists in asset-library but isn't sliced), every edge stopped at flat grass, paths were dead-straight rects. **TREE CLIPPING — ROOT-CAUSED with numbers (defect a, not the sprite):** no tree pixel-spilled (spillCount 0), but camera bounds (right=1664=full world) extended 96px / 3 tiles PAST the physics bound (1568=tx49) and past the last props — so the camera revealed a 3-tile EMPTY-GRASS VOID at every edge, and the rightmost tree stood against it = reads as "cut off." Same defect as the missing boundary. **FIX:** a diegetic BOUNDARY treeline now walls all 4 edges (W marsh-gloom tint, N foothill-grey ridge, road GAPS + gate posts at the exits), every boundary sprite kept strictly inside the world bounds (re-verified spillCount 0, rightmost tree edge 1646 ≤ 1664 = whole). Also: EDGE-SCATTER decal layer (tufts/ferns straddling every grass↔path seam — no licence-loaded transition tiles) softens the borders; village re-CLUSTERED tight around the fountain (forge set off, farm/homestead at the S edge); WINDING stepped tracks; the 3 secrets kept; cast/M1-M4/hub quests/Bram social all re-verified live after relayout. npm test + verify GREEN; 0 console errors. FLAGS: gloom could be mistier; N wants a real peak/cliff silhouette sprite; some straight seams remain where scatter is sparse (no transition tiles) | ⏳ **VAN:** does Greenhollow read as ONE continuous lived-in place now, and are the right-side trees finally WHOLE? does the walk/camera feel right along the bounded edges? | proof/greenhollow-v2-overview + v2-east-trees-whole + v2-west-gloom + v2-north-ridge + v2-square-clustered + v2-transition |
| 2026-06-07 | GREENHOLLOW VALE — full GOLD-STANDARD region (the quality bar) | Rebuilt Greenhollow from a 40×34 test field into a designed 52×40 village with REAL LPC buildings (no brick-red placeholder): a fountain SQUARE ringed by 8 VARIED buildings (manor/chapel/tavern/forge/Pem's store/3 tinted cottages/homestead) on radiating paved STREETS, opening N to the crossroads (waystone), S to the OUTSKIRTS (brook + orchard + farmland + the boarded-cave sign), W into the WOOD FRINGE. Lived-in scenery (fences, gardens, barrels, signs, trees, flowers). 3 SECRET chests (wood hollow / behind the manor / behind the tavern → gold). The full CAST placed + working (Mara/Bram/Hodge/Tam/Phil/Fatley/Pem) with the social system live (Bram's CHA shop). Childhood M1-M4 + hub side quests (Fatley's mug) playable. SAFE ZONE. Loaded real ElizaWy LPC structure art (houses/fountain/chest/barrel/fence). verify green; 0 errors; no regression (Marsh/Peaks). FLAG: path/grass hard edges (no transition tiles) + a real cave-mouth sprite wanted | ⏳ **VAN — THE QUALITY BAR**: does Greenhollow now feel like a real, full, alive village (vs a staging map)? what to add/move/change? | proof/village-overview + village-square + village-outskirts + village-secret-chest |
| 2026-06-07 | SOCIAL SYSTEM (CHARISMA / TRUST / DECEPTION) | Built per SOCIAL-SYSTEM-DESIGN.md, layered on the existing dialogue/karma/economy (no rewrites). Added: CHA attribute (Inventory); a Social module (CHA price mults, trust derived over deeds, check resolver, option gating/tags); Dialogue.select handles option `check` (onPass/onFail); Economy prices CHA-adjusted (cha5=neutral); RegionScene renders Fallout-style option tags ([Charisma 7], [Insight]) with hidden/locked gating + a {price} template + a CHA-priced buy. REAL examples wired into existing NPCs: Bram (CHA-priced steel sword + [Persuade 6] haggle), Hagga ([Insight] reveals she's lying — hidden without the skill), the Miner ([Trust 2] secret pass — only with earned deeds). Tests: Social 4 checks; no regression (M1/M8 still play). 0 errors; verify green | ⏳ Van: try a charisma/insight chat — does it feel Fallout/D&D? | proof/social-charisma-shop + social-insight-option + social-insight-reveal |
| 2026-06-07 | Safe zones + threat-indicator toggle/skill | FIX1 SAFE ZONES: no monsters in settlements — `combat.safeZone` (Greenhollow village = peaceful; dev charger behind key 0) + `combat.safe:{tx,ty,r}` hubs (Mirefen, the peaks town: no aggro/wander-in). Verified live: Greenhollow 0 enemies; enemy de-aggros in the hub, aggros in the wilds. FIX2 THREAT INDICATORS off by default (clean screen; telegraphs still show for Gate E), a toggle in options, and always-on+enhanced via the ENEMY INTUITION skill. Verified: off=no chevrons, toggle=amber chevrons on aggroed, skill=on+red even with toggle off. Rule recorded in QUALITY-BIBLE. 0 errors; verify green | ⏳ Van: confirm towns are safe + the clean-screen default | proof/indicators-off + indicators-on |
| 2026-06-07 | Monster FACING fix (all scenes) | BUG (Van): monster sprites always faced UP — the EnemyController played only `mon_<sheet>_idle` (row 0); it updated `spr.facing` but never switched the animation row. FIX: BootScene now builds 4 directional anims per sheet (up/left/down/right = LPC rows 0-3); the controller plays the row matching facing (toward the player; toward the charge dir while rushing). Verified live: a 4-way snake chart + 6 marsh creatures all turn to face the player (the eyeball's pupil tracks). All 8 sheets × 4 dirs exist; 0 missing. Behaviour/telegraphs unchanged | ✅ verified (snake + all marsh creatures turn) | proof/facing-snake-chart + facing-marsh-creatures |
| 2026-06-07 | SUNDERED PEAKS region built (on the RegionScene template) | New PeaksScene extends RegionScene — thin DATA/config (mountain theme via gray-stone tints; FLAG: real cliff/rock tiles want an atlas pass; town buildings still brick-red). Wired the EXISTING quests on-screen: M11 (arrival + miners-vs-owner lean) + M12 (Cinder Keep → grapple tool + Keep Sentinel boss [NEW: phased shielded→brute, grapple-trick] + Shard #2 + the order-chose-lie truth, M10-reactive) + side quests SP1/SP3/SP5. Verified live: M11→M12 play, grapple + shard_2 (Shards 2/5), boss fight (grapple trick + enrage), SP5 trick + SP3 fork + combat counters work; M1/M8/M9 in other regions UNREGRESSED. Reach via N from Greenhollow. 0 errors; verify green | ⏳ Van: play Sundered Peaks — does the mountain read distinct, do the fights + boss land? | proof/peaks-region + peaks-combat + peaks-boss |
| 2026-06-07 | RegionScene refactor + Greenhollow charger fixed | Extracted a reusable RegionScene base (rendering/dialogue/interaction/camera/combat/quest-orchestration); GreenhollowScene + MarshScene are now thin DATA/config subclasses — the template for all remaining regions. DIAGNOSIS confirmed Van's report: the Greenhollow [DEV TEST] charger was still a HUMANOID (its own legacy _spawnCharger path); only the Marsh used EnemyController/monsters. The refactor unifies Greenhollow onto EnemyController -> the charger is now a real `mon_snake`. Verified BOTH scenes live + identical: Greenhollow (walk/M1/Mara+Bram/combat/big-head/options) + Marsh (walk/M8/shrine-M9/lantern/boss/7 creatures/combat). verify green; 0 console errors | ✅ both scenes verified identical; ⏳ Van: confirm both still play right | proof/before-greenhollow-charger-humanoid + after-greenhollow-charger-monster + after-marsh-on-base |
| 2026-06-07 | REAL monster sprites wired | The Ashen Marsh archetypes now render as REAL LPC creatures (snake/bat/segmented-worm/eyeball/ghost/slime/pumpkin-king; boss = a tentacled man-eater maw) instead of tinted humanoids — fixes the "tinted humans" gap. Behaviour + telegraph + counters UNCHANGED (shielded front-block/flank re-verified). Monster art was ALREADY in the repo (prior LPC-monsters fetch); this session copied it to public/art + loaded/animated/wired it. 0 console errors; verify green | ⏳ Van: do the monsters now look distinct + creaturely? (FLAG: shielded=pumpking, boss=man-eater are closest-available, not perfect) | proof/slice2b-marsh-real-monsters.png |
| 2026-06-07 | Stage 2b — enemy ARCHETYPE variety + Ashen Marsh combat region | Generic EnemyController renders ANY tested archetype (FSM-flag-driven telegraph language + counter resolution). Marsh is now a real ADULT combat region: 7 placed archetype encounters (charger/ranged/swarm/shielded/brute/caster/electric, distinct skins+scale+tint), M8 still plays, M9 wired (shrine -> lantern -> the DROWNED GUARDIAN boss: lantern strips the shroud, half-HP brute enrage twist -> shard_1). COUNTERS verified live: flank↔shielded, interrupt↔caster, wait-window↔electric, AoE↔swarm, projectile↔ranged, dodge↔charger/brute, lantern↔boss. 0 console errors; verify green | ⏳ **VAN: play Ashen Marsh** — does each enemy feel like a DISTINCT fight? is the boss's lantern-trick + enrage readable? (feel values unchanged) | proof/slice2b-marsh-* |
| 2026-06-07 | Dodge-roll VISUAL fix | Replaced the full-sprite CARTWHEEL spin with a low CROUCH-SCOOT (vertical squash + a little wide + the i-frame ghost; NO rotation) — reads as ducking and rolling low, not a gymnast. Checked the LPC master library (C:\GameAssets\lpc-full): it has NO roll/tumble/crouch animation (body anims = spellcast/thrust/walk/slash/shoot/hurt/idle/jump/run/sit/emote/climb/combat/1h_*). So this is the honest APPROXIMATION; proper LPC roll/tumble art is still a later art task (flagged in code) | ⏳ Van: does the duck-roll read right? | proof/slice2c-dodge-roll.png |
| 2026-06-07 | Slice S2c — controls + combat polish + options menu | DODGE-ROLL (renamed; i-frames; duck-roll crouch-scoot — FLAG: LPC has NO roll frames, proper roll art is a later task) + DASH stub (separate traversal ability). BLOCK pose = a guard-ARC (FLAG: real block frames needed) + SHIELD-SCALED (iron shield equipped, blockNegate 0.8; shields are items). KID/ADULT gate (combat gated on isAdult; the slice = a dev adult). Canonical CONTROL MAP in SSOT read by InputMap (kbd + basic gamepad + rebind). OPTIONS MENU (Esc): rebind every action, audio sliders, aim-invert, modifiers, 18+-gated adult mode. Modifiers registry (big-head visible). All unit-tested (Combat 7, Modifiers 4); verify green; 0 errors | ⏳ **VAN: confirm BINDINGS + feel** (below) | proof/slice2c-* |

## ⏰ MORNING TODO FOR VAN — CONTROL BINDINGS to confirm (canonical map — change any in-menu or tell me):
Keyboard: WASD/arrows move · **Shift** run · **E** interact · **J / left-click** attack · **Space** dodge-roll ·
**Q** use/throw · **C** block (hold; tap-at-impact = parry) · **R** aim (reserved) · **[ ]** cycle item · **Tab** game menu · **Esc** settings.
Xbox (reserved, wired basic): LStick move (far=run) · A interact · B dodge-roll · X attack · Y use · RT aim · LT block · LB/RB cycle · Start menu · Back settings.
→ Open settings (Esc) → CONTROLS → Enter on a row to rebind. Tell me your preferred layout.
NEW feel items: dodge-roll (i-frames generous enough? roll reads?), block (shield-scaled feels right? parry timing?), shields (block 0.6/0.8/1.0 — tune on the items).

## ⏰ MORNING TODO FOR VAN — feel-tune these (all in src/constants/standards.js → COMBAT):
The LOGIC is built + unit-tested; only the FEEL numbers are placeholders. Play the Greenhollow
slice (the [DEV TEST] charger) and dial:
- ATTACK: ATTACK_DAMAGE / ATTACK_RANGE / ATTACK_COOLDOWN_MS — does the swing feel right?
- DODGE: DODGE_DISTANCE / DODGE_DURATION_MS / DODGE_IFRAME_MS / DODGE_COOLDOWN_MS — does the i-frame
  dash through the charge feel reliable + fair? (Shift)
- BLOCK/PARRY: BLOCK_DAMAGE_TAKEN / PARRY_WINDOW_MS / PARRY_STUN_MS — is the parry window too
  tight/loose? is the stagger a satisfying punish? (K, hold; tap-at-the-right-moment = parry)
- CHARGER: CHARGE_SPEED / CHARGE_DAMAGE / APPROACH_SPEED — dangerous enough? telegraph long enough?
- FEEL FX: HIT_FREEZE_FRAMES / SHAKE_HIT / SHAKE_CHARGE / FLASH_MS — too much/little?
- CONTROLS: I dropped RUN to free SHIFT for dodge — tell me your preferred bindings (the real ones
  are the controller map: A=interact, X=attack, B=dodge, LB=block; keyboard is a stand-in).
- The whole RPS: does dodge↔charger / block↔frontal / flank↔shielded feel good once enemies vary?

## Combat — Van's questions to answer by playing (Stage 2a):
- Are telegraphs READABLE (charge-line + "!" — can you see the wind-up + dodge sideways in time)?
- Do hits have impact (hit-pause / white-flash / screen-shake / sound — does it feel MEATY)?
- Does the charge feel dangerous (do you respect it / want to dodge)?
- Is the punish window (cyan) clear + landing the hit satisfying?
- Tuning notes: too fast/slow? too much/little shake? damage numbers? (all in src/constants/standards.js COMBAT)

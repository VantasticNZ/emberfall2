# EMBERFALL 2 - MASTER PLAN (locked, Plan B: design-whole-then-build)
## RULE: Follow this order. Do not skip, reorder, or start building until DESIGN is 100% done +
## coherence-reviewed. Every session states which plan step it is on.
## [DONE 2026-06-09] Interactive HTML WORLD-DESIGN MAP (planning view, NOT the game; gates GREEN). docs/world-design-map.html — a single standalone file (inline CSS/JS, no deps) rendering the MASTER-WORLD-SPEC: 20 places as TRUE-scale dots/diamonds on the 24×24 grid + biomes/ocean; 23 CONNECTIONS as lines coloured by progression stage (early/mid/late) + labelled by traversal-means + gate (dashed=gated), with the progressive multi-routes (⚡electric N-gate, ❄ice-river, 🪵bridge, 🌀wind) + no-go barriers; 27 clickable QUEST/ITEM dots along the paths. CLICK a place → panel (NPCs/quests/items/enterable buildings/size/unlock + its connections); click a path → traversal/gate/length; click a dot → that quest/item. All data pulled from the spec (not invented). Verified headless: renders, 0 console errors, all 3 click types work. Committed; no force-push. (Review tool for Van — opens in a browser.)
## [DONE 2026-06-10] PROCESS RETRO + HARDEN the DoD/gates/deferred-tracking (process-only; 15→16 gates GREEN). Learn from the recurring gaps so they can't repeat. (1) docs/PROCESS-RETRO.md — honest retro: ROOT CAUSE = every gate tests DATA or flood-fill SIMULATION, none runs real Phaser physics/render, so the runtime/visual failure classes (circular-collision-test, interior-void, dash-into-void, bush-farm, doors-mid-floor, designed-not-built) had no catching gate; 8 META-RULES (gates are proxies defending a runtime behaviour; a green test contradicting Van's eyes = the TEST is wrong; "working" needs REAL-input runtime; data-correct ≠ reads-correct; designed ≠ built; deferred ≠ dropped; no tautological tests; honest checklists); + a GATE AUDIT classifying all 16 gates DATA/SIMULATION + the weak ones (collision-matches-visual-mass = the one that went green while broken; density-floor; entrance/soft-lock = proxies). (2) docs/DONE-DEFINITION.md — the SSOT for "done": a RUNTIME eyes-on checklist (A gates / B runtime: walkability·containment-walk+dash·doors-as-doorways·no-farm·visual·audio·perf / C designed==built·identity·cross-check / D honesty·no-false-green). (3) NEW GATE #16 designed-vs-built: HARD-fails on a door→non-existent-region mislink; else prints the gap (4 settlements on the walkable overworld vs 7 still board-only) so designed≠built can't hide. + the containment gate already asserts dash-clamp inputs. (4) docs/DEFERRED.md — living tracker of EVERY deferred item (other-region relocations, town silhouettes, spell-route gates, ABILITIES session, 24×24 expansion, M-map pins, deep M8-M10, frog/hen, music beds, furniture polish, bog-tiles, boss reskins, lpc-monsters licence) each with why/unblocks/when(milestone M1/M2/M3/ABIL/POLISH). (5) Standing CROSS-CHECK formalized (quests×map, assets×content, audio, enemies×difficulty, cohesion, designed×built — each eyes-on). (6) WIRED into CLAUDE.md: new HARD RULE 11 (gates are proxies; green-vs-eyes=test-wrong; REAL-input runtime; designed≠built; deferred≠dropped) + DONE-DEFINITION/PROCESS-RETRO/DEFERRED in the docs index; EXCELLENCE-FRAMEWORK points to DONE-DEFINITION as the runtime-truth bar. No game-content changes; verify GREEN. Committed; no force-push. **NEXT: apply DONE-DEFINITION + the cross-check to all further region builds; the designed-vs-built gate drives the connected-overworld pass 2 (relocate the 7 board-only settlements).**
## [DONE 2026-06-11] G2 — SHOP BUYING v1 (items + keeper stock + buy flow; gates GREEN, end-to-end + persistence verified, 0 errors; tag pre-shop-buying). Reused existing economy infra (items have `value`; REGION_PRICES/SHOPS/buyPrice/availableStock in Economy.js). ITEMS: added 5 to the SSOT + src/data/items defs — cheese(5)/apple(2)/torch(4)/rope(6)/whetstone(10) with name/value/effects + an icon concept (text-label v1). SHOPS: added `pem_store` (Pem's Store, GH: bread/cheese/apple/torch/rope/minor_potion) + whetstone into `hodge_forge`; keepers linked via `shop` field (Shopkeeper→pem_store, Smith→hodge_forge). BUY UI (_openShop/_renderShop/_shopNav/_shopBuy/_closeShop): E on a keeper opens a buy panel (HUD hidden to avoid overlap; rebuilt each open after a cached-box bug left rows uncreated) listing name·price·(have N); ↑↓ choose, E buy, Q/Esc leave; movement + J/Space frozen (_canAct + update gate on _shopOpen). buyPrice = value × REGION_PRICES.buy (GH 1.0). VERIFIED (Van's load, fresh save, pixel + data): stock correct + panel UNSQUASHED (6 rows render, screenshot shop-buy-ui.png); buy decrements gold + adds item (20→17 bread, →12 cheese); insufficient gold denied (gold unchanged); PERSISTENCE across a real page reload (saveGame + reload → gold 12, bread 1, cheese 1). npm test + verify GREEN (37 items==SSOT, 32 economy refs valid); 0 console errors. DEFERRED to the economy spec: per-item ICONS (food/ware icons exist in eliza-objects), selling, tiered/stage-scoped stock, karma/CHA pricing. Committed; no force-push. **REMAINING big-push work: G3 — SLICE STEP 3 living-world build (its own session).**
## [DONE 2026-06-11] G1.2/1.3 — R6 ROOM-COMPOSITION rules + FENCE clearances (gates GREEN, pixel-proven, 0 errors; tag pre-r6-composition). R6 added to SPEC-INTERIORS: tall storage (shelf/cabinet/dresser) backs a wall · fireplace embedded · bed wall-headed/cornered · table/bench/stool/anvil may be central · counter separates keeper-zone (behind) from customer-zone (front) · altar = chapel centre-piece · nothing else floats. REARRANGED all 15 GH interiors (homes/v2-v5/store/forge/chapel/tavern-f1-f2/forge_generic/mirefen/fenwick) so wall-backed items line the back/side walls. GATE: extended `furniture-non-collision` → R2+R6 — per-item wall-adjacency by TYPE (WALLBACK = bed/dresser/fireplace/cabinet/shelf must have top edge near the back wall OR a side/bottom edge; relaxed the top-clip so a headboard/hearth tucks INTO the back wall, which is intended). NEW gate `fence-clearance`: every prop_fence ≥2 tiles clear of every building entrance + approach (door tile derived the same way the scene carves it), fully solid. FENCE FIX: runtime audit found 21 fences → 9 too-close to entrances + 6 part-disabled by _clearDoorwayThresholds (= part-walkthrough, Van's finding); the 2 backyard runs straddled gh_home1/house_v5 doors + the field fence crossed house_v3's door → relocated the backyard runs + split the field fence around the door → now 14 fences, 0 too-close, all fully solid (runtime-verified). PIXEL-PROVEN (r6-store.png): store reads as a shop (shelves on the wall, counter central + keeper behind, stock around); furniture renders whole. Nav tests updated to the moved solid tiles. npm test + verify GREEN; 0 console errors. Committed; no force-push. **REMAINING big-push work: G2 shop buying v1; G3 living-world build (its own session).**
## [DONE 2026-06-11] BIG PUSH G1.1 — SPLIT FURNITURE root cause + fix + static gate (the only group completed this pass; G1.2/1.3 + G2 + G3 flagged as remaining). ROOT CAUSE (wrong layer again, logged PROCESS-RETRO): bed/dresser/anvil PNGs were multi-part SHEETS (two stacked objects + an all-transparent row band between) → the renderer drew the whole canvas → floor showed through the middle = "split bed". Gate #22 (footprint) passed on CANVAS DATA, never rendered pixels. FIX: cropped each to a SINGLE opaque piece (bed 46×58, dresser 62×40, anvil 32×24) + bumped interior scales (bed 0.9, dresser 0.85, anvil 1.3) + re-validated the footprint gate. NEW GATE `furniture-whole` (scripts/fitcheck/furniture-whole.test.mjs, in npm test): decodes real PNG alpha + asserts no all-transparent row between opaque rows (all 10 furniture single-piece). PIXEL-PROVEN on Van's DEFAULT zoom (bigpush-home-fixed.png; 0 floor-through samples). npm test + verify GREEN, 0 errors. **REMAINING BIG-PUSH WORK (NOT done — flagged for focused sessions, each needs its own verification): G1.2 R6 room-composition rules (tall storage backs to wall, fireplace embedded, beds wall-headed, counters zone keeper/customer) + rearrange all interiors + per-item placement gate; G1.3 fence clearances (1-2 tiles from entrances, fully solid) + gate; G2 shop buying v1 (items-as-data, keeper stock, buy UI, inventory+persistence); G3 SLICE STEP 3 living-world build (roles incl. PROTECTED kids, ULPC chore anims, 3-phase day, chore loops, personality dialog, reactivity v1 incl. repair event + guard confront/fine, deed schema). G3 alone is a multi-system build = its own session.** Committed group 1; no force-push.
## [DONE 2026-06-11] MUSIC — wired 8 of Van's Downloads beds (LOCAL/friends scope) as LICENCE DEBT + the ship-gate (gates GREEN, beds audible-verified, 0 errors; tag pre-music-wire). Van's call: local/friends scope, so use the Downloads music NOW, tracked as debt resolved before any wider release. WIRED (mood-matched, all .ogg/.mp3, sensible vol via the existing crossfade + killTweensOf guard): cemetery-eerie→mus_cemetery ("Old Cemitery", lost_cemetery) · dungeon-tension→mus_dungeon ("Mysterious cavern", cave_f1/f2 + dgn_keep) · spire-sacral→mus_sacral ("The Sage Den", Spire region + dgn_shrine + gh_chapel) · ember-tension→mus_ember ("Old Ruins", Emberwood) · coast-storm→mus_coast ("Submerged Ruins", Coast) · settlement→mus_town ("Plains (contrasting)", town/village/city scenes) · interior-home→mus_home ("Forest - Under The Great Tree", homes/huts/shops) · interior-tavern→mus_tavern ("Tea Time For Cats", tankard_f1/f2). _musicForRegion extended (interiors mapped by key/flags; already called on entry — interiors flow through it). LEDGER: the 8 marked LICENCE-UNVERIFIED in a machine-read ```unverified block (file | mood-slot | best-guess source); green/peaks stay CC0-verified, marsh royalty-free. SHIP-GATE: new no-unverified-assets-at-ship gate — INFORMATIONAL by default (reports count+files; VERIFY PASSED), HARD-FAILS at `SHIP=true npm run verify` (tested: fails listing all 8). Documented in QUALITY-BIBLE (the bar + resolve-before-distribution rule) + DEFERRED (wired-as-debt + CC0 swap candidates). AUDIBLE-STATE VERIFIED (Van's load path): GH=green; entered gh_home1=mus_home, gh_chapel=mus_sacral, tankard=mus_tavern, lost_cemetery=mus_cemetery, cave_f1=mus_dungeon, gh_store=mus_home — all map + isPlaying, crossfades ramp to ~0.49, 0 console errors. npm test + verify GREEN. Committed; no force-push. **NEXT: per Van's roadmap (interiors VAN-TEST 2 sign-off → step 3 NPCs/living-world build, or as Van directs); music debt resolves before any wider release.**
## [DONE 2026-06-11] INTERIORS TIDY (Van's interiors-test findings) + knock/occupancy + spec adds (gates GREEN, 0 errors, pixel+round-trip-proven; tag pre-interiors-tidy). (1) THRESHOLD SNAG — root cause: the entry spawn coincided with the exit-door tile (against the interior wall), so entering jammed you at the inside/outside line (_lastTile guard + wall). FIX: _enterArea nudges the entry spawn ONE tile INTO the room (land in open space; walk back to the door to leave) — verified round-trip enter→exit 0 stuck frames (was 14+); also reduced doorWallOffset 1.35→0.7 tiles (the exit door was buried a full tile in the wall). (2) FURNITURE NON-COLLISION (R2) — root cause: center-origin tall sprites bled into walls/neighbours while only 1 tile carved. FIX: interior furniture base-anchored (origin 0.5,1) + retuned scales/positions; added a MECHANICAL GATE (verify.mjs #22, per-item: no wall-clip/item-on-item/blocked door-or-approach, using logical un-inset door tiles) — fixed ALL violations across 12 GH + forge_generic/mirefen/fenwick; tables moved clear of exit approaches; nav tests updated. (3) REAL PROPS (asset-first, eliza-objects, ledgered): extracted stool/bench/shelf → chapel pews=benches (was fences), store=stocked shelves+table counter, tavern=stools. (4) APPROACH ZONES: _clearDoorwayThresholds widened (doorway + 2 front tiles). (5) KNOCK/OCCUPANCY: 'You knock. Nobody answered.' when empty; an occupant replies + footsteps; new deed entered_occupied_home (-7/-5 vs uninvited -3/-2), break-in-while-occupied worse again; _isOccupied(to) reads residents (home1/store occupied, home2/generic empty). SPEC ADDS (doc): SPEC-BACKLOG economy = real items→inventory, stock by town-tier×stage, prices by morality/purity/renown; SPEC-NPCS §5.5 DEATH CONSEQUENCES = service gap+replacement, first-kill memorial+grieving kin, delayed avenger, schema-now. Pixel-proven (Van's load): home no split bed; chapel real bench-pews. npm test + verify GREEN; 0 console errors. Committed per-group; no force-push. **NEXT: Van's INTERIORS VAN-TEST round 2.**
## [DONE 2026-06-11] SLICE STEP 3 SPEC — SPEC-NPCS-LIVING-WORLD.md written for Van's ~2-min review (SPEC ONLY, no build; doc-only, gates GREEN). Per HARD RULE 12 (decompose→Van-review→build); refs Stardew (phase schedules/chore loops), Majora's Mask (always-somewhere presence), Fable (property/reactivity). Covers: (1) ROLE PALETTES per settlement type (GH built: keeper×4/priest/guard-pair/wood-chopper/farmer/repair-worker/residents/kids?-elders — each with chore loop + named home + day-phase positions; Marsh/Peaks/Coast sketched post-slice). (2) TIME = simple 3-phase day (morning/day/evening) via tod.phase(), phase→position maps, door coherence (resident OUT → "nobody home"); full hourly = post-slice. (3) CHORE LOOPS engine (walk→act-anim→pause, extends NpcLife) + ASSET-FIRST anim audit: HAVE idle/walk/slash (fetched); GAP chop/hammer/hoe/sweep/pray/guard-spear → fetch via ULPC forge or ElizaWy expanded; GRACEFUL-DEGRADE rule (no anim → idle-at-workstation, presence preserved, never a blocker). (4) DIALOG slice scope: per-named-NPC line-set (greet+2-3 topics+bark), keeper trade hooks (stub), 2-3 interconnection lines/town, NZ-flavoured tone hooks, repeat-avoidance (cycling topics). (5) REACTIVITY: broken-door grumble + staged REPAIR-WORKER event (walk→hammer→restore, visible stages), looted-home cold lines, karma greeting variants. (6) PROPERTY/DEEDS (Fable): deed schema {owner,price,rentRate,buyable} on EVERY building NOW (slice, no-retrofit) + purchase/rent/furnish BUILD post-slice; per-town purchase unlock on quest-arc completion. (7) exhaustive-table case list + 5-min VAN-TEST + 6 OPEN DECISIONS (kids y/n · guards-on-alarm now/later · dialog UI style · chore-anim depth · property trigger · repair-worker who). SPEC-BACKLOG step 3 → spec'd; post-slice layers logged DEFERRED. npm test + verify GREEN. Committed; no force-push. **NEXT: Van reviews the spec (~2 min) + marks the 6 decisions → then BUILD step 3 to the approved spec.**
## [DONE 2026-06-11] SLICE STEP 2 — ALL Greenhollow interiors built to SPEC-INTERIORS v2; DOORS step-1 FROZEN (gates GREEN, 0 errors, pixel/karma-proven; tag pre-gh-interiors). DOORS+buildings+signs marked FROZEN in THE-SLICE.md (passed Van round 4; changes need explicit unfreeze; interiors CONSUME the door system via _enterArea, never modify it). ASSET AUDIT (rule): HAVE core furniture (bed/fireplace/table/dresser/chest/barrel/anvil/altar); extracted+ledgered cabinet+crate (eliza-objects, OGA-BY) for variety; GAP = clean per-cell counter/shelf crops (multi-variant sheets — DEFERRED). BUILT: (1) FIVE distinct generic-home variants — house_generic(v1 cosy)/house_v2(wardrobe)/house_v3(storeroom-crates)/house_v4(family-two-beds)/house_v5(hearth-front), distinct furniture+tint+floor, ROTATED across generic homes by a position hash ((tx*3+ty*7)%5) so no two adjacent homes share a layout. (2) Residents per decision (b): keepers in gh_store/gh_forge/gh_chapel/tankard (worldmap KEEP_OLD/FEM/SMITH presets, simple greet, behind counter/by altar) + a resident in gh_home1; NONE in generic homes; the LOCKED gh_home2 = resident out — all verified (movers: store/forge/chapel/home1/tavern=1, generic/home2=0). (3) Private-home looting at a KARMA cost (decision c): chests carry `private` → opening commits the new `home_looted` deed (-4 mor/-3 pur) + banner; VERIFIED morality 0→-4, gold +12, looted persisted. (4) Bespoke chapel/tavern + purpose-fit store/forge verified to R1. PIXEL-TRUTH TABLE (Van's load, fresh save, snapshotPixel): all 10 GH interiors render REAL floors (no black room), furniture/chest present, exits valid, isolation intact — CAUGHT+FIXED a world-bounds bug (variants at oty640 > WORLD_PX 20480 + v5 otx636 > the 640-tile width → camera clamped → black; relocated in-bounds at oty560/otx588). New props got interaction-classes; floors are real TERRAIN sets. AUDIO: interior bed hooks deferred (no licence-clean track yet — DEFERRED). npm test + verify GREEN; 0 console errors. Committed per-group; no force-push. **NEXT: Van's INTERIORS VAN-TEST (~5 min) → on sign-off slice step 3 (NPCs/dialog).**
## [DONE 2026-06-11] SPEC-INTERIORS v2 (rules layer) + MUSIC audit + the HEN ledgered/imported (spec+audit only, NO interior build, NO game-code changes; gates GREEN). (1) SPEC-INTERIORS.md → v2: added the RULES LAYER Van identified — R1 PROPERTY-TYPE × REGION-STYLE matrix (home/shop/forge/tavern/chapel/manor/hut × GH/Marsh/Peaks/Coast/Desert: MUST/MAY/MUST-NOT); R2 NON-COLLISION rules (≥1-tile clear path to every container/NPC/exit; door+stairs zone clear; all containers reachable; no walk-behind trap; no solid-on-walkable — as table cases); R3 STAIRS/FLOORS (multi-floor only for tavern/manor or ≥size threshold; stairs:true vs exit; what's upstairs per type); R4 THEME/STYLE (interior materials/palette per region so inside matches outside + a real TERRAIN floor set); R5 TRAVERSAL RULE (Van verbatim — passes unpassable except with the stated item/ability; paths may have side-areas/secrets/dead-ends; a solid traversable main path ALWAYS exists — reconciled with gating.js + the no-soft-locks gate, extended to interiors/world). Kept the 14 facets + case list + VAN-TEST; marked v2 for Van's ~2-min re-review. (2) MUSIC AUDIT (asset-first): HAVE/wired+SAFE = green ("Peasant Theme", nihilocrat CC0) · peaks ("Cave Theme", Brandon Morris CC0) · marsh ("Spirits Forest", HydroGene — ⚑ "royalty-free" is NOT a clear licence, re-verify before ship). IN-LIBRARY = only sfx (kenney CC0); no extra beds. IN-DOWNLOADS = ~80+ loose .wav/.ogg + ~10 music zips with NO bundled provenance → UNKNOWN licence, must NOT use until Van confirms the source per file (like the hen); mood-matched candidates exist for every gap (cemetery: Old Cemitery/Hauted Place/Spooky Stroll/Alone-at-Twilight/Paranormal · dungeon: Submerged Ruins/Old Ruins/Mysterious Cavern/Ironchest/dungeon_loops · sacral: Sacred Shrine Lp/The Sage Den · ember/tension: The Catalyst/Desert Heat/The Creature · coast-storm: Rainy Day/Submerged Ruins · settlement: Plains/Chill RPG/Forest-Under-the-Great-Tree). GAP (need licence-clean) = coast-storm · ember-tension · spire-sacral · dungeon-tension · cemetery-eerie + settlement beds → logged DEFERRED with OGA/CC0 fetch candidates. (3) THE HEN: Van-confirmed (LPC Chicken Rework, daneeklu/bluecarrot16 = Daniel Eddeland, CC-BY-3.0/GPL-2.0, OGA) → imported public/art/fauna/chicken{,_walk,_eat,_shadow}.png + LEDGERED (asset only, kicked-chicken karma wiring is later content). Licence gate GREEN (192 files / 11 entries). Committed; no force-push. **NEXT: Van reviews SPEC-INTERIORS v2 (~2 min) + the 5 decisions + confirms which Downloads music to clear → then BUILD interiors to the approved spec.**
## [DONE 2026-06-11] BUILDINGS — hanging purpose signs (forge/store/tavern/chapel) + LOCKED the asset-first cross-check rule (gates GREEN, 0 errors, pixel-proven; tag pre-building-signs). ASSET AUDIT FIRST (the new rule, applied): HAVE — eliza-structure/Signs (Sign Backgrounds A board + Sign Icons A: sword/shield/coin/INN/PUB) + lpc-medieval-decorations (hanging signs + crosses); GAP — no chapel/church icon → composed a cross on the eliza board. Extracted + ledgered (OGA-BY) sign_forge/store/tavern/chapel.png (board+icon, 32px). MOUNTED asset-owned: the building ASSET declares signAnchor:{cx,cy} (WHERE a sign hangs on its sprite); the PLACEMENT declares sign:'prop_sign_X' (WHICH — because prop_house_paneled serves BOTH chapel AND tavern). GH: forge=sword · store=coin · tavern=INN · chapel=cross; homes none. Rendered at signAnchor, depth = building base-feet +1 so the player (lower) sorts IN FRONT when walking past. Pixel-proven (Van's plain load, snapshotPixel): 4 signs render in front of their own building, sampled board/icon colours (chapel [232,224,196], store [222,167,67], tavern [191,142,64] — not grass); building-sign-tavern.png shows the tavern's INN sign mounted on the wall by the entrance. LOCKED the ASSET-FIRST rule (CLAUDE.md HARD RULE 12d + DONE-DEFINITION §D): every build session audits the library FIRST, reports HAVE/extracted vs GAP in its hand-off, ledgers everything used — a standing cross-check in every report. PLAYER-PASS (HARD RULE 12): signs read the purpose at a glance, sit on the wall sensibly, don't clip the player passing. Committed; no force-push. **NEXT: Van's DOORS+BUILDINGS VAN-TEST round 4 (now incl. signs) → on sign-off FREEZE doors+buildings → slice step 2 interiors (per SPEC-INTERIORS once approved).**
## [DONE 2026-06-11] DOORS step-1 round 3b CLOSURE — per-style door mapping (ALL types) + dialog-box layout + the orphan cliff (gates GREEN, 0 errors, overlay+pixel+data-proven; tag pre-door-closure). (1) PER-STYLE MAPPING complete: extracted door_panel.png (eliza 12-panel, OGA-BY, ledgered) → mapped prop_house_paneled (tavern/chapel) + prop_forge to doorArt:'prop_door_panel'; brick (house_a/b) stays prop_door_arched. Overlay-proven (PIL contact sheet door-perstyle-overlay.png) each building wears its matching door IN its painted opening — brick→arched, paneled/forge→panel. (Remaining: lpc-victorian doors only needed if a victorian building is used — none in GH; DEFERRED.) (2) DIALOG BOX layout: the door-choice 3rd option clipped the box bottom (screen only 432 tall; old box at y300 h120 ended at 420, opt3 at 426 was off-screen) → box moved UP + taller (y266 h158 → bottom 424), name/body/hint shifted up, options at 344+i*20 with even 20px spacing; DATA-verified all 3 options (344/364/384) sit inside the box (≤424), 2/3/4-option dialogs unsquashed. (3) ORPHAN CLIFF: identified as the M4 boarded-cave (prop_cliff_face cave-mouth STAND-IN, lantern→cave_lore) at the SE meadow amid the homes; it's a LARGE solid prop whose collider blocks its approach at every reachable open spot in town → REMOVED from GH (verified 0 cliff-faces in GH) + DEFERRED to re-home as a proper boarded-cave in the wilds (Marsh/Peaks); no soft-lock (cave_lore is a story/ending seed, not a traversal key — gates GREEN). PLAYER-PASS (HARD RULE 12): doors match their buildings; the knock/try/break box reads balanced; no stray cliff by the homes. Committed; no force-push. **NEXT: Van's DOORS VAN-TEST round 4 (expected final) → on sign-off FREEZE doors → slice step 2 interiors (per SPEC-INTERIORS once approved).**
## [DONE 2026-06-11] DOORS step-1 round 3 — style-matched arched door + open/closed/locked/BROKEN mix + inset (+ separate sword nearest-target fix); gates GREEN, 0 errors, pixel-proven (tag pre-door-mix-broken). DOOR-ASSET AUDIT: the buildings are eliza-structure assets → the matching door art is ALREADY in the library (eliza-structure/Doors: arched/12-panel/barred/doorframes; lpc-victorian doors) — NO fetch needed; extracted the arched door (public/art/structures/door_arched.png → prop_door_arched, OGA-BY, ledgered) for the brick houses; the building asset names its door via `doorArt` (brick→arched, others default prop_door). THE MIX (data per building, state ∈ open/none/closed/locked/broken): chapel/tavern/store/forge = OPEN (dark threshold only, no door — always-open public); manor/home1 = CLOSED (arched door + knock/try); home2 = LOCKED (arched + lock glyph + break) — pixel-confirmed the arched doors render in the brick-house openings (door-mix-arched.png). BROKEN + PERSISTENCE: forcing a locked door saves chunk-flag door_broken_<to> → on re-entry it renders splintered/hanging (angle 15 + dark tint) + walk straight in; verified across region reload (= save-state). Repair = future item (DEFERRED). INSET: interior exit doors recessed further (doorWallOffset 1.35 tiles). Gate states extended (open/none/closed/locked/broken). PLAYER-PASS (HARD RULE 12): open shop = straight in; closed home = knock/try; broke the locked home = splinters persist — reads right (the arched double-door's centre seam is a real seam, not a gap). SEPARATE COMMIT — sword NEAREST-TARGET: _cutSwing cut the FIRST cuttable in array order; now picks the CLOSEST in the swing arc (verified: 3 bushes 46/30/16px array-far-first → only the nearest cut). DEFERRED: per-style panel/victorian door mapping (POLISH; arched covers the brick majority); door-repair item. Committed per-group (doors / sword); no force-push. **NEXT: Van's DOORS VAN-TEST round 3 → on sign-off FREEZE doors → slice step 2 interiors (per SPEC-INTERIORS once approved).**
## [DONE 2026-06-11] PROCESS — full-expectation specs (decompose-then-build) + player-pass + reference-check, locked + interiors spec written (process/doc only; gates GREEN). Van's demand institutionalized as 3 rules in CLAUDE.md (HARD RULE 12) + THE-SLICE.md METHOD: (1) FULL-EXPECTATION SPEC pre-build — no system from a one-line intent; write the full decomposition (behavior/states/sequencing/visuals/audio/feel/edge-cases/persistence) asking "what would Stardew/Zelda/a commercial RPG do", show Van for a ~2-min review BEFORE building, build+table cover ALL of it; (2) PLAYER-PASS — after the table, play as a skeptical player on Van's load path (approach wrong, try to break, note what looks/feels off), hand-offs include "what I noticed"; (3) REFERENCE-CHECK — every spec names its genre reference, diffed against it. APPLIED: docs/SPEC-INTERIORS.md written (SPEC ONLY, NO BUILD) — interiors decomposed to 14 facets (distinct purpose-fit layouts/rotated generics; resident-fit furniture; searchable containers + once-only persisted loot + trespass-Karma; lighting/mood; entry/exit valid; resident NPC present; collision+depth; isolated render; persistence; interior audio; size-proportional; no black-floor/void; stairs; feel) + the exhaustive-table case list + a 5-min VAN-TEST draft + 5 open decisions for Van (generic-variant count, which buildings have residents, private-home looting, audio beds, bespoke interiors). **NEXT: Van reviews/edits SPEC-INTERIORS.md (~2 min) → on approval, BUILD interiors to the full spec → exhaustive pixel-truth table → player-pass → VAN-TEST. NO build until approved.**
## [DONE 2026-06-11] DOORS (slice step 1, round 2) — per-asset doorway RECTS measured from the artwork + locked-sequence + avatar-visible (22 gates GREEN, 0 errors, pixel-proven on Van's plain load; tag pre-measured-doorways). Van's DOORS test failed 3 ways: (1) door sprites mis-placed (beside porches/on walls, not in the painted opening) — one generic rule, each building's door is elsewhere; (2) one 32x44 sprite regardless of opening size; (3) locked door looked open before the choice + the avatar disappeared. FIX (asset-owned, done properly): MEASURED each building's painted doorway from its sprite PNG (PIL dark-portal scan + eyes-on) → doorway:{cx,cy,w,h} in assets.js (prop_forge centre 28x48, prop_house_a bottom-LEFT 32x56, prop_house_b bottom-RIGHT 44x58, prop_house_paneled porch 26x48); the door system PLACES the door sprite at (p.x+cx,p.y+cy) and SIZES it to (w,h) → it sits IN the painted opening + fits, per asset, for every placement; the carve splits the band around the door column and the TRIGGER tile sits at the door column on the building's FEET line. AVATAR root cause: the door at baseDepth+2 over a band-centre threshold had depth > the player's → it occluded the player; moving the trigger to the feet line makes player depth (feet+playerBase) > door depth (feet+1) → player IN FRONT (pixel-proven: skin [249,213,186] sampled during the dialog). LOCKED SEQUENCE: walking in shows the choice FIRST; the door stays shut (sprite visible + lock glyph), no sfx/open until try/break/key — verified with REAL key input (a teleport in the test triggered a re-stream that destroyed the sprite ref = a test artifact, not a game bug). VERIFIED by PIXELS (Van's plain load, snapshotPixel): doors sample wood at their MEASURED sizes; 22/22 render in front of their own building; door-in-painted-opening.png. PROCESS-RETRO: why the table missed these (never compared door pos/size to the ARTWORK, never asserted dialog-time avatar pixels, never checked sequence order) → 4 new permanent cases (door-in-painted-opening, door-fits-rect, avatar-visible-in-dialog, locked-sequence-order). DEFERRED: 1 manor-type door sampled dark-centre (renders; recheck). Committed; no force-push. **NEXT: Van's DOORS VAN-TEST round 2 → on sign-off FREEZE doors → slice step 2 interiors.**
## [DONE 2026-06-11] SETUP FOR THE REAL BUILD — fix door DEPTH (pixel-proven) + lock PIXEL-TRUTH verification + declare THE-SLICE (22 gates GREEN, 0 errors; tag pre-door-depth-slice). PART 1 (the one real bug): door visuals were created at DEPTH.FLOOR+7 (≈ −9993) but DEPTH.FLOOR=−10000 and buildings y-sort at +9600..+10242 → doors rendered at the floor layer, OCCLUDED behind their building → never visible (my prior 'doors render ✅' read the .visible FLAG, not pixels). FIX via the asset-owned door system (fixes all at once): _buildDoorVisual now sets the door + threshold to baseDepth(= the building's base-feet sort depth) +1/+2/+3 → the door renders IN the opening, in front of its own building. PIXEL-PROVEN on Van's plain load path (no scene manipulation): 22/22 doors render in front of their OWN building; the 7 doors visible at the natural spawn each sample RGB [122,82,46] (wood) via game.renderer.snapshotPixel (true framebuffer read) — doors-render-PIXEL-PROVEN.png vs the earlier vans-actual-view-natural.png (dark holes). Head-on entry confirmed (real UP-key) on store/chapel/forge. PART 2 (lock pixel-truth permanently): DONE-DEFINITION §B + PROCESS-RETRO meta-rules #11/#12 — 'renders' is proven ONLY by pixel-sampling (renderer.snapshotPixel vs the asset colour) or depth-vs-occluder, NEVER .visible/.alpha/existence; screenshots use Van's plain load path (cleared save, default camera, no teleport/centerOn/force-stream); every visual claim cites its pixel/occlusion evidence; the reusable snapshotPixel helper is documented; Node verify gates remain DATA proxies (visual truth = Playwright pixel-check). PART 3 (the robust plan): docs/THE-SLICE.md — the governing one-pager (SCOPE = the Greenhollow vertical slice ONLY, zero known bugs, Van 30-min sign-off; METHOD = build one system → exhaustive pixel-truth table → 5-min VAN-TEST → sign-off → FROZEN; ORDER = doors→interiors→NPCs/dialog→quests M1–M4→combat-feel→audio/polish→30-min playthrough; DISCIPLINE = nothing outside the slice until sign-off, park tangents in DEFERRED) + the DOORS VAN-TEST script; CLAUDE.md now reads THE-SLICE.md FIRST (the library is reference, not mandatory reading). Committed; no force-push. **NEXT (SLICE ORDER step 1): hand Van the DOORS VAN-TEST → on sign-off FREEZE doors → step 2 interiors (built + distinct generics).**
## [DONE 2026-06-11] DOOR SYSTEM — built to full spec + EXHAUSTIVELY tested (every building × every case; 22/22 PASS; 22 gates GREEN, 0 errors; tag pre-door-exhaustive). THE PRODUCTION METHOD (the fix for ~7 patch-passes): build to spec, then produce a FULL PASS/FAIL TABLE over ALL 22 buildings in GH(9)+Marsh(8)+Peaks(5) × 9 cases — door-sprite-in-portal · enter-dead-on · interior-built(not-black) · isolated · exit-valid(not-void) · stairs · knock/try/break+morality · step-away-closes-menu · avatar-visible — with REAL input on a fresh save; NOT done until 100% PASS. The exhaustive run FOUND 2 real bugs a few-building check had missed: (1) the music crossfade fade-IN tween kept running on a sound the fade-OUT then destroyed → 'set volume on null' EVERY FRAME → the TweenManager step threw → downstream updates (the open-door delayedCalls) didn't fire → ~12 buildings silently failed to enter — FIXED with killTweensOf(old) before fade/destroy; (2) house_generic floor:'wood' = a non-existent TERRAIN set → black-floor room — FIXED ('stone') + a gate. SPEC built: EVERY building renders a REAL door sprite in its portal via _buildDoorVisual (was only the forge); OPEN doors animate open then enter; tavern up/down marked stairs:true + rendered stone-tinted (floor-change = stairs, not a building-exit door); step-away closes the menu + the _lastTile guard stops it re-opening while you stand on the doorway; avatar stays visible. GATE #21 strengthened: every building enterable + every building ASSET declares doorway + every interior has a 'back' exit + every interior floor is a REAL terrain set (no black room). RESULT (eyes-on FRESH save, the FULL table): 22/22 buildings PASS all applicable cases (manor confirmed 3/3 after a harness-timing flake; stairs up→tankard_f2/down→tankard_f1; try -3 / break -10 morality; step-away closes + no re-open; avatar visible); 0 console errors. Proof: house-generic-built-floor (black→stone) · doors-render-in-portals. DEFERRED: dedicated stairs SPRITE; varied generic interiors. Committed; no force-push. **NEXT: stairs sprite + varied generics; the exhaustive-table method is now the standard for any building/door change.**
## [DONE 2026-06-11] REBUILD — ASSET-OWNED DOORWAYS (Van's governing principle: every asset declares its portal, so a door fix applies to ALL buildings) + bug sweep (21 gates GREEN, 0 errors, eyes-on FRESH save; tag pre-asset-doorways). ARCHITECTURE: each building PROP in assets.js declares `doorway: { px }` — the painted door's horizontal offset from the sprite centre (native pixels), READ OFF THE ACTUAL SPRITE ART (store house_brick_b = +36 bottom-RIGHT; paneled = +40 right porch; manor house_brick_a = -46 bottom-LEFT; forge = 0 centre — every building's door is in a DIFFERENT place, which is why per-building patching never converged). The ONE door system (_buildRegion carve) reads PROPS[key].doorway.px and builds the visible door + walkable threshold + trigger as the SAME tile, tile-aligned, automatically for every placement; removed the per-sprite DOOR_OFFSET scene hack. The placement `door` field now carries only STATE/target; the doorway POSITION is the asset's. RESULT: fixing the door code fixes EVERY building; a new building only declares its doorway once. GATE #21 STRENGTHENED: fails if any building ASSET omits `doorway` (asset-owned invariant) + asserts every interior has a 'back' exit (no trapped/void room) — in addition to every-building-enterable. BUGS FIXED via the new system (verified eyes-on FRESH save): STORE enters dead-on its bottom-right door (the long-standing 'can't enter' — it was the centred-guess vs right-door mismatch); ALL GH buildings enter + every EXIT lands on a walkable tile (no black void); tavern upstairs = stairs (up→tankard_f2, down→tankard_f1 via the return-stack, then leave f1→outside); avatar VISIBLE+active on knock (could not reproduce the disappearance — verified visible); interiors ISOLATED (only the active room renders, black around); knock/try/break + morality + visible door-open intact. Screenshots: store-asset-doorway / knock-avatar-visible / store-interior-isolated. DEFERRED: a stairs SPRITE for floor-links (cosmetic); varied generic interiors; manor/other building doorway-px fine-tune if any read off. Committed; no force-push. **NEXT: varied generics; stairs sprite; Coast/Emberwood inline buildings (their assets already carry doorways).**
## [DONE 2026-06-11] FIX — Van's numbered door bugs (interior isolation · GH dungeon-rocks · approach regression · chapel alignment); 21 gates GREEN, 0 errors, eyes-on FRESH save. (1) INTERIOR ISOLATION (most important): inside a building Van saw OTHER interior rooms in the black — the interiors share a far-band, so proximity streamed several at once → FIX: _maybeToggleRegion, when _inInterior, restricts the loaded set to ONLY the active interior (its bounds) → one room, pure black around (interior-isolated.png). (2) GH DUNGEON-WARP ROCKS: cave_f1 + the __gendungeon/__gencave dev-warps sat as rocks in the GH plaza/streets → cave_f1 RELOCATED to the north wilds edge (gx37,gy4 — probed walkable + reachable + enters), the 2 dev-warps REMOVED from GH (DEFERRED: re-home at a Peaks/Marsh cliff) → GH streets carry no dungeon rocks (gh-plaza-no-rocks.png). (3) BUILDING-4 / APPROACH REGRESSION: 2 buildings had the tile in FRONT of the doorway blocked by a neighbour → _clearDoorwayThresholds now clears the APPROACH tile too (not just the doorway tile) → 0 blocked approaches, all reachable. (4) CHAPEL TRIGGER MISALIGNED: the carve centred the doorway on the COLLIDER, but prop_house_paneled's painted door is the RIGHT-side porch → NEW per-SPRITE DOOR_OFFSET (prop_house_paneled +1 tile) shifts the doorway tile + threshold + trigger onto the VISIBLE door → you enter dead-centre through the porch you can see (chapel-aligned-porch.png). (5) SHARED GENERIC INTERIORS: DEFERRED (rotate a set of 3–5 varied generics by a position hash) — logged. Also fixed: _solidAt already ignores disabled bodies; a relocated entrance must be re-checked walkable (first cave placement landed on a tree — re-probed). PROCESS-RETRO: interior-isolation + trigger-on-visible-door as runtime/visual classes (eyes-on-verified, visual-truth pattern); relocate-then-recheck-walkable. Committed; no force-push. **NEXT: varied generic interiors; re-home the gen dev-warps in the wilds; Coast/Emberwood inline buildings.**
## [DONE 2026-06-11] DIAGNOSE + REBUILD — UNIFORM door system: EVERY building enterable, every region + visible door-open (21 gates GREEN; 0 errors; eyes-on FRESH save). DIAGNOSED (the inconsistency truth): 16 of 22 inline buildings were NOT enterable — ROOT CAUSE (a): only GH was carved last pass; Marsh/Mirefen (8) + Peaks (5) + the GH manor/2 extra-cottages were solid props with NO door spec. REBUILT as ONE coherent system (not a patch): (1) _resolveBuildingDoor(p) gives EVERY building prop (prop_house*/forge/tavern/chapel/...) a door — its explicit door, or a DEFAULT open door to a SHARED GENERIC INTERIOR (HOUSE_GENERIC / FORGE_GENERIC, reused across all buildings via the return-stack) → no un-enterable building anywhere; (2) the EXACT tile-aligned carve + the OPEN/CLOSED/LOCKED states apply uniformly to all; (3) _clearDoorwayThresholds() disables any solid that landed on a doorway tile (the procedurally-scattered Peaks rock-in-front-of-a-door, a neighbour's mass) so the threshold is always walkable — robust regardless of placement; (4) _solidAt now ignores disabled bodies; (5) VISIBLE door-open — a closed/locked door tried/broken/keyed runs _openDoorVisual (closed-door sprite hides + the dark threshold appears = the door swings open) THEN ~440ms later the player walks through (fixes 'break worked but no open door appeared'); (6) break-strength scales the force morality hit; the USE-KEY option is wired (door.key + inv.has). Gate #21 EXTENDED: asserts ALL 22 building props in EVERY region resolve to an interior (fails on any un-enterable building), mirroring the runtime resolver. Eyes-on FRESH cleared save (meta-rule 10): ALL 22 inline buildings (GH 9 + Marsh/Mirefen 8 + Peaks 5) enterable, 0 blocked doorways; sample-entered each region by REAL movement; the visible-open verified (closedSpr→hidden, opening alpha 0→0.9, state→open before enter); 165fps; 0 console errors. Building-enterability table in the session report. DEFERRED: Coast + Emberwood have no inline building props yet (marker-entered settlements → enterability comes with the inline-town conversion); a findable house-KEY item (option wired, no key placed); bespoke per-building interiors (generics are shared → repeat look); KNOCK-answering + guard/alarm spawn (flavor beats). Committed; no force-push (tag pre-uniform-doors). **NEXT: convert Coast/Emberwood settlements inline (then their buildings get doors too); a findable key; bespoke interiors as polish.**
## [DONE 2026-06-11] FIX + BUILD — exact tile-aligned doorways (consistent, no-stuck) + CLOSED/LOCKED door states (21 gates GREEN; 0 errors; eyes-on FRESH cleared save). Van's bugs: (a) inconsistent entry (some buildings enter, some don't), (b) stuck-on-the-threshold-line. ROOT (a): the ~1.1-tile FLOAT doorway gap left the walkable gap and the trigger tile (round(ccx/TILE)) MIS-REGISTERED — the trigger sometimes sat partly under a side collider → "works on some buildings not others" + a sub-tile sliver to snag on. PART A (FIX): the doorway is now EXACTLY ONE TILE-ALIGNED walkable tile — dCol=round((ccx-TILE/2)/TILE); the flanking solids end exactly at dCol*TILE and (dCol+1)*TILE → identical geometry on every building, clean unambiguous gap. PART B (no-stuck): exit spawns squarely in the YARD tile below the doorway (returnPos to _enterArea), never on the threshold line; the tile-aligned gap removes the sliver. PART C (BUILD): the CLOSED + LOCKED states on the Dialogue option system — CLOSED (gh_home1): KNOCK / TRY-THE-HANDLE → enter + entered_uninvited (−3 morality/−2 purity); LOCKED (gh_home2): KNOCK / TRY → "locked" → BREAK-IT-DOWN → enter + forced_entry (−10/−6) + a "someone heard" alarm beat (sfx+banner); wired to Karma via karma.commit; deeds entered_uninvited/forced_entry added to the SSOT; the building door spec extended string→{to,state,owner}; closed/locked render a closed-door sprite (+ lock glyph). Gate #21 strengthened (validates door state ∈ open/closed/locked). Eyes-on FRESH cleared save (meta-rule 10): all 4 open doors walkable+enter+EXIT-CLEAN (no stuck, in+out); closed shows knock/try, locked offers break-down; morality −3/−10 applied; deeds fire; states read visually (door-states-closed-locked.png); 0 console errors (fixed a duplicate-method syntax slip caught by the 500/esbuild check). DEFERRED: KNOCK-answering + real guard/alarm spawn (flavor beats now → social pass); PICK/lockpick alt; other regions' doorways; lock-glyph prominence. Wired DOOR-SYSTEM.md (states BUILT). Committed; no force-push. **NEXT: roll the doorway carve + states to the other regions; deepen knock-answer/alarm in the social pass; convert the remaining 7 towns inline.**
## [DONE 2026-06-11] SYSTEM + FIX + CHECK — the door/entrance system (inset doorways you WALK INTO) + gate #21 + the visual-truth check (21 gates GREEN; 0 errors; eyes-on FRESH cleared save). PART A: docs/DOOR-SYSTEM.md — the SET standard: a building is a SOLID footprint with ONE inset doorway GAP carved in the front wall; the doorway tile (dark threshold) is the only walkable gap + the TRIGGER (step INTO it to enter, not the yard tile in front); door STATES OPEN (built) / CLOSED (knock·try-handle·FORCE + morality hit) / LOCKED (knock·pick·force) defined with collision+interaction+visual each (the morality-entrance hook); interior exits stay embedded. PART B (FIX): the 6 GH buildings (chapel/tavern/store/forge/2 cottages) carry door:'<interior>' on the building prop → _buildRegion carves the base-band into left+right colliders with a ~1.1-tile walkable gap, draws a dark inset threshold, and registers the walk-trigger at the doorway tile (this._buildingDoors, read by _checkDoorWalk); the separate yard-door interactables REMOVED; cave/gen stay marker-doors. PART C (CHECKS): NEW gate #21 doorway-geometry (every enterable building-interior has a way in + every door leads to a real interior → no un-enterable holes, no door-to-nowhere); + the VISUAL-TRUTH CHECK PATTERN (Playwright fresh-save, real input walk-into, assert entered + doorway walkable, capture a screenshot, pixel-count the thresholds) documented in GATE-AUDIT as the way to close the eyes-on-only gaps. Eyes-on FRESH cleared save (meta-rule 10): all 6 buildings ENTER by walking UP INTO the inset doorway (not the front); doorway tile walkable; no un-enterable holes; chapel reads as a recessed porch/threshold (door-system-chapel-doorway.png); 6 dark-threshold rects rendered; 0 console errors. SPEC'd/DEFERRED: the CLOSED+LOCKED door states (morality-entrance build-out; add entered_uninvited/forced_entry to the deed SSOT) + other-region building doorways + visual polish. Wired into CLAUDE.md + DOOR-SYSTEM in the docs index. Committed; no force-push. **NEXT: build the CLOSED/LOCKED morality-entrance states; roll the doorway carve to the other regions; convert the remaining 7 towns inline.**
## [DONE 2026-06-11] PROCESS — lock "verify in Van's exact state" + stale-state preventatives + gate audit (20 gates GREEN; 0 errors; process/doc only). ROOT CAUSE named (PROCESS-RETRO): the repeated "fixed but Van still sees it broken" = VERIFYING IN THE WRONG STATE — Playwright fresh-scene ≠ Van's saved-game-in-a-long-lived-tab; "verified" meant "fixed in a state Van never sees." LOCKED meta-rule 10 (PROCESS-RETRO) + DoD requirement (DONE-DEFINITION §B): a fix is NOT verified unless checked in the USER'S EXACT STATE — cleared save (localStorage cleared) + full reload (recreate the scene) + the build Van loads; Van's eyes on a hard-reset fresh state are the truth. BUILT the stale-state preventatives (kill the class): (a) SaveManager.WORLD_VERSION guard — a save with an older world-version resets POSITION to spawn (progress kept) so an old save can't load an old/void position over a new build (legacy no-wv saves use migration; saves from now carry wv); (b) main.js HMR full-reload (vite:afterUpdate + hot.accept → location.reload) so a long-lived dev tab can't run a stale Phaser scene under a fresh build-stamp; (c) F8 fresh-game (clears save via the storage adapter + reload, two-press-confirm) + help-text + a world-reset banner. GATE AUDIT (docs/GATE-AUDIT.md): all 20 gates classified — ALL are DATA-static or flood-fill SIMULATION, NONE runs real Phaser physics/render → they're PROXIES; runtime/visual truth defended ONLY by eyes-on (meta-rule 10). Weak/proxy gates named (collision-matches-visual-mass = collider-exists-not-blocks; density-floor = counts-not-purpose; no-soft-locks/entrance = graph-not-physical; #18/#20 = visual-truth-via-data still need eyes-on). Eyes-on-only GAPS (no gate): audio-plays, enemies×difficulty, elevation/terrain-bands cohesion, professional-feel §E. CONNECTED-STATE reported (live gates): ONE connected walkable world (8 streamed regions, GH↔Marsh-W/Peaks-N/Coast-E/Emberwood-S/Spire); 9/11 settlements reachable on the overworld; 1 town inline (Mirefen), 7 enter-scenes deferred; 0 floating doors, 0 dupes, 0 overlaps, no soft-locks. Tests: SaveManager wv-guard scoped so legacy migration test still passes; storage-isolation kept. Wired into CLAUDE.md. Committed; no force-push. **NEXT: convert the remaining 7 towns inline + the structural geometry; bump WORLD_VERSION on each layout change.**
## [DONE 2026-06-11] FIX — FLOATING DOORS in Greenhollow (confirmed live, fresh-reset; NOT cache) + gate #20 (20 gates GREEN; 0 errors). Van did the full reset (restart/hard-refresh/clear-site-data, build 7df83902) and still saw doors floating on GH grass/paths — REAL, and my prior "decluttered" verification was a rationalisation (broke meta-rule 2: I dismissed them as "legit scattered entrances" + blamed a stale tab). Reproduced Van's EXACT state (localStorage cleared): 16 floating prop_door sprites in/around GH. ROOT CAUSE: _buildInteractable rendered a prop_door SPRITE at every door's trigger tile — which sits in the yard in front of buildings / on open terrain (caves, the orphaned vil_oasis fast-travel, the relocated settlement entrances). FIX: doors NEVER render a free-standing prop_door — a building entrance shows the building's OWN painted door (render NO sprite; the walk-trigger is data-driven via _checkDoorWalk), a cave/dungeon entrance shows a rock CAVE-MOUTH marker, a not-yet-inline village shows a waypost (prop_sign) marker, only interior EXITS keep the embedded wall-door (spriteDx/Dy); the orphaned Mirage-Oasis door REMOVED; the Marsh/Peaks/Coast/Emberwood relocated entrances given markers (rock for dungeons shrine/keep, sign for towns) + the Yssa's-hut door moved to a moot-hall building front. NEW VERIFY GATE #20 no-floating-doors: an overworld door must be a building entrance (a building prop within ~5 tiles → no sprite) OR carry a marker — else it's an orphaned/floating door (FAIL). Eyes-on FRESH SAVE (Van's state, build 7df83902): 0 floating prop_door sprites in GH (gh-floating-doors-BEFORE→FIXED, gh-clean-town-center reads as houses+paths); building doors STILL walk-in (chapel entered — trigger is data-driven, only the sprite removed); no door buried/unreachable; 0 console errors. PROCESS-RETRO new failure row + reinforced meta-rule 2 (when Van reports X wrong on a fresh state, X is wrong — fix it, don't explain it away). Report PLAYTEST-LOG + PROCESS-RETRO + DEFERRED. Committed; no force-push. **NEXT: the relocated-entrance markers + inline town conversions continue; the GH→Mirefen structural geometry pass.**
## [PARTIAL 2026-06-10] GH→MIREFEN CORRIDOR — feel-layer to standard; structural geometry deferred (19 gates GREEN; 0 errors; 57fps; tag pre-gh-mirefen-corridor). MAP GEOMETRY READ (world-map-vanedit.json): GH→Mirefen = via:walk, width 3, 5 zig-zag/double-back bends (chunk 9.5,10.5→9,12→8,10.5→8,12.5→6.5,11) descending elev +1(GH)→−1(bog); Mirefen 44×36, soundRadius 3, marsh-dread. DELIVERED (the §E professional-feel layer on the corridor, eyes-on): SEE-IT-BEFORE tease (pillar 2) — a strongbox on a reed-islet across the east pool, visible-not-reachable + a legible "a running DASH could clear the gap" sign; SOFT GUIDANCE (pillar 4) — waystone signs funnel the eye toward Mirefen; DENSITY-WITH-PURPOSE (pillar 5) — reeds/dead-snag/pools/enemies/townsfolk/the cache, no dead bog; ambient life + marsh-dread audio (pillar 6). CONFIRMED holding: corridor BOUNDED (WEST_BELT 276 channel-wall colliders — can't stray, off-path blocked not void); Mirefen inline/seamless (gate #19); collision/depth/navGates; perf 57fps; 0 errors. HONEST PARTIAL (DEFERRED, structural — DONE-DEFINITION "convert as much as holds, log the rest"): the ROUTED exact zig-zag (belt winds but not the 5 map-bend coords); the ELEVATION descent contours (no elevation render yet); the grass→bog TERRAIN-TRANSITION bands; Mirefen RESIZED to its 44×36 spec (inline town is smaller, 8 buildings); the dash-REACH for the islet backtrack (engine-blocked, ABIL). Legible gates: N/A on this walk-corridor (GATE_LOOK applied to the gated regions last task). Report PLAYTEST-LOG + DEFERRED (the corridor checklist). Committed; no force-push. **NEXT: the structural geometry pass (zig-zag/elevation/terrain-bands + Mirefen resize) to finish THIS corridor to full spec, then roll the corridor+town pattern across the world.**
## [DONE 2026-06-10] RPG-FEEL STANDARD — "professional open-world-but-gated" bar locked into the DoD + gate-legibility proof (19 gates GREEN; 0 errors). docs/RPG-FEEL-STANDARD.md defines the 6 PILLARS: (1) LEGIBLE TANTALIZING GATING — every gate READS as its ability (never an invisible wall) + a per-ability VISUAL LANGUAGE table (grapple=brown anchor, hookshot=teal ring, lantern=dark mouth, firefrost/fire=ice-blue, bomb=cracked wall, cut=brambles, ice=freeze-gap, wind=gust, electric=dead-switch, bridge=broken-span, shard/story=rockfall); (2) SEE-IT-BEFORE-YOU-REACH-IT (visible-but-unreachable teases that bait curiosity + pay off); (3) BACKTRACK REWARDS / layered reachability (revisit-with-new-tool rewards, never tedious); (4) SOFT GUIDANCE (sightlines/landmarks/light/terrain-funnel — open not caged); (5) DENSITY WITH PURPOSE (no empty dead space; every ~24-tile stretch has a POI/tease/resource/NPC/landmark); (6) MOMENT-TO-MOMENT POLISH (responsive controls, readable hit-feedback, smooth transitions, ambient life, responsive audio, consistent art). WIRED INTO DoD: DONE-DEFINITION §E "Professional Feel" — a region isn't done unless all 6 pillars are eyes-on-true. PROOF-OF-PATTERN APPLIED: _buildRegionGate now renders each gate LEGIBLE BY ABILITY (GATE_LOOK table: deed→tint+scale+ability-named prompt+hint) instead of every gate being the same grey rockfall — eyes-on the Coast grapple-gorge now reads as a TALL WARM-BROWN anchor (gate-grapple-legible.png) + the inspect names the grapple; hookshot=teal, lantern=dark, firefrost=ice-blue. (Note: tinted/scaled rock_crag, not bespoke anchor sprites — the bespoke per-ability gate ART is DEFERRED, tracked in DEFERRED.md; legibility delivered now via colour+label.) DEFERRED: the 6-pillar application per region (teases, backtrack rewards, soft guidance, density-with-purpose, bespoke gate sprites, ambient life). Report RPG-FEEL-STANDARD.md + DONE-DEFINITION §E + DEFERRED + PLAN. Committed; no force-push. **NEXT: build every future region/town/corridor to the 6-pillar bar; convert the remaining 7 towns to inline (Mirefen pattern) making each gate legible as built.**
## [PARTIAL 2026-06-10] SEAMLESS WORLD (Van option B, LOCKED) — Mirefen converted to INLINE terrain (the proof) + DoD/gate #19 (19 gates GREEN; 0 errors clean; 60fps; tag pre-seamless-world). STEP 0: PLAN-UNDERSTANDING locked option B (one seamless overworld; settlements = walk-through terrain; only interiors/dungeons/secrets are scenes; hybrid REPLACED). STEP 1: DONE-DEFINITION HARD rule (seamless overworld) + NEW verify gate #19 seamless-overworld — tracks town/village/city inline-vs-enter-scene + regression-guards SEAMLESS_DONE (a converted town must keep NO enter-door); made tracking-not-hard-fail during the transition (a hard fail would block the honest partial build per DONE-DEFINITION; flagged; becomes 0-tolerance when complete). STEP 2: MIREFEN converted to INLINE overworld terrain in the Marsh (Van's W position) — 8 bog-tinted houses + 3 townsfolk (Bett/Coll/Tam) + Yssa's-hut interior-door + a town sign placed AS terrain; the town's enter-door REMOVED; the bog between buildings is the walkable street (verified grid: open square + E-W streets, navigable though tight); made a no-aggro safe hub; Yssa's hut + the Shrine (dungeon) stay enter-scenes. STEP 3 runtime-verified per DONE-DEFINITION (real movement): walking west into the Marsh puts you IN Mirefen's streets with NO door/transition (region stays Marsh), it reads as a real bog-town embedded in the world, 60fps, 0 console errors on a clean load (an audio-crossfade error appears only on rapid test region-switching — pre-existing music-system, not the town). Gate #19: 1/9 towns inline. Eyes-on: mirefen-inline-seamless.png. Report PLAYTEST-LOG + DEFERRED (the seamless conversion list: 8 towns to convert via the proven pattern, M-map already pins live). HONEST DEFERRED: 8 of 9 towns still enter-scenes (Fenwick/Cemetery/Stonereach/Cragfoot/Saltbreak/Cribbins/Thornwell next pass; Oasis needs a desert region M3) — gate #19 tracks; widen the tight inline streets + clean up the orphaned town SCENES (polish). Committed; no force-push. **NEXT: convert the remaining 8 towns to inline (same Mirefen pattern) region-by-region; then the spell-route gates (abilities) + the 24×24 expansion.**
## [DONE 2026-06-10] DIAGNOSE — report-vs-Van's-screen contradiction + state the "one map" plan (no build; verify GREEN). Van on hash 1788edbb saw many doors + a wrong M-map; CC reported 1 door + correct M-map. ROOT CAUSE: the DATA on 1788edbb is correct (verified 3 ways: GH has 1 south board door [vil_oasis only]; settlements relocated; M-map pins from live REGIONS interactables) + a FULL reload shows it; but Van's long-lived browser tab runs a STALE Phaser scene — Vite injects the build stamp FRESH per HTML request (so the stamp reads 1788edbb) while HMR hot-swaps modules but CANNOT recreate a running Phaser scene → new stamp, old world (the pre-fix 11-door board + text-list M-map). My Playwright always does a full page load (recreates the scene) → fresh/correct = the screen-parity gap. The SAVE compounds it: the game autoloads a save (karma/inv/quests/time + position) so Van respawns at his old south-meadow spot — but the save does NOT store doors/regions (data), so it's not the door cause. SECONDARY real finding: even on the correct build the relocated entrances are freestanding door-sprites; near the GH hub the streamer loads ~5 adjacent regions so MANY entrance-doors render at once (counted 15: GH 10 + Marsh 4 + Emberwood 1 — all legit/unique, no board-row) → still door-heavy near the hub (presentation refinement: town-gate sprite / show-when-near). VAN'S FIX: Ctrl+Shift+R (recreate the scene); if stale, restart npm run dev + reopen; clear the save (localStorage.clear()). RECOMMENDATIONS (not built): save version/world-version invalidation; a New Game/Clear-Save UI; a dev "build changed — reload" prompt / HMR scene-recreate. docs/PLAN-UNDERSTANDING.md states the "one map as designed" understanding + the honest gap: the build is a HYBRID (8 overworld regions ARE one connected walkable world, traversal-gated; but each SETTLEMENT is a SEPARATE door-entered SCENE at its map position = walk-to-then-enter, JRPG-style — NOT seamless walk-through towns). THE QUESTION for Van: (1) seamless walk-through towns [larger rebuild, NOT built] or (2) walk-to-position-then-enter [the hybrid, ~built]? — confirm before building. Diagnosis + PLAN-UNDERSTANDING.md; no force-push. **NEXT: await Van's confirm of (1) vs (2), then re-scope.**
## [DONE 2026-06-10] FIX — what Van SEES now matches what's built (stale M-map + door-clutter) + gate #18 (18 gates GREEN; 0 errors). Van reported seeing the OLD layout despite gates saying 10/11 built (the rendered≠built / presentation-stale pattern, his eyes override green gates). TWO root causes found + fixed: (1) THE M-MAP rendered the 8-region skeleton + a corner TEXT-LIST of settlements ("door-entered scenes"), NOT pins at their built positions → looked stale; FIX: _renderWorldMapInto now PINS each settlement at its LIVE overworld-entrance position (derived from the region interactables, same data the player walks) → the map shows Marsh towns W, Peaks N, Coast E, Thornwell S = matches Van's edited map. (2) DOOR-CLUTTER: the old GH-south notice-board (11 doors to settlements) was never removed when they were relocated → DUPLICATE board+overworld entrances, so Van saw a door-board + the new entrances; FIX: removed the 10 relocated board doors (kept only Mirage Oasis as a single fast-travel marker until it gets a desert region) → GH south reads as town+meadow, not a scatter of doors (door sprites near GH-south 11→1+legit). NEW VERIFY GATE #18 rendered-vs-built / no-duplicate-entrances: fails if any settlement is reachable via BOTH the GH board AND a relocated overworld entrance (board:1, overworld:10, no dupes). PROCESS hardened: PROCESS-RETRO new failure row + meta-rule 9 (what the player SEES must match what's built — views derive from live data, remove superseded presentation) + gate-audit rows #17/#18; DONE-DEFINITION new runtime item ("Presentation matches the build — M-map + no clutter, eyes-on"); DEFERRED M-map-pins marked done. Eyes-on: mmap-built-layout (settlements pinned at positions) + gh-south-declutter (clean). Committed; no force-push. **NEXT: relocate the remaining landmark/dungeon nodes + the 24×24 expansion for Spire; the abilities session for the spell-route gates.**
## [PARTIAL 2026-06-10] CONNECTED OVERWORLD pass 2 — 6 settlements relocated to the walkable world (17 gates GREEN; 0 errors; tag pre-overworld-pass2). Relocated 6 of the 7 board-only settlements to overworld door-entrances at Van's positions (probed walkable tiles, reusing built content): Stonereach + Cragfoot + Cinder Keep (Peaks N, appended to its interactables); Saltbreak + Cribbins (Coast E, via mutating TIDEWRECK_COAST.interactables — greyboxRegion doesn't take them); Thornwell (Emberwood S). RUNTIME-verified per DONE-DEFINITION: all 6 enter the CORRECT town by walking onto the door (real input); eyes-on the Stonereach entrance on the Peaks path. GATE #16 designed-vs-built now 10/11 on the walkable overworld (was 4) — only Mirage Oasis (desert) board-only (deferred M3, no desert region). FOUND+FIXED a real LATENT BUG (DONE-DEFINITION caught it — the gate said built but walking entered the WRONG town): the far-band settlement scenes OVERLAPPED (thornwell∩stonereach, mirefen∩shrine, etc. — the oty446 towns are 26-28 tall, bled into the oty466 village/dungeon row) → regionAt picked the wrong one. FIX: moved the oty466 row (oasis/thornwell/shrine/keep) to oty480 (no overlaps) + added VERIFY GATE #17 no-overlapping-scenes (all 22 enterable scenes occupy distinct bounds) so it can't regress. Report docs/CONNECTED-OVERWORLD.md (pass 2) + PLAYTEST-LOG + DEFERRED updated. DEFERRED: the landmark/dungeon nodes (High Pass/Lighthouse/Drowned-Vault/Caught/Weeping/Ember-Hollow), Spire (needs 24×24 expansion), Oasis (needs a desert region); town silhouettes; spell-route gates (abilities session). Committed; no force-push. **NEXT: relocate the remaining landmark/dungeon nodes + Caught; the 24×24 expansion for Spire; then the abilities session for the spell-route gates.**
## [PARTIAL 2026-06-10] CONNECTED OVERWORLD pass 1 — the WEST is ONE walkable world (14 gates GREEN; 0 errors; state tagged pre-connected-overworld). Made the marsh settlements part of the walkable overworld at Van's map positions (W→E): added interactables (walk-in door-entrances) to the ASHEN_MARSH region — Mirefen (far-W tile 243), Sunken Shrine (W 242), Lost Cemetery (NW 247), Fenwick (E 273) — each on a walkable bog tile + a naming signpost, REUSING the built town content (enter the existing Mirefen/Fenwick/Cemetery/Shrine areas, not rebuilt). WALK-ACROSS PROVEN (real movement): drove WEST from Greenhollow → region transitions Greenhollow→Belt→Marsh → arrived at the Mirefen entrance → walked onto the door → entered Mirefen (overworld-mirefen-entrance.png). Corridor 38-43/45 tiles walkable per row (eerie bog + mus_marsh). GH notice-board kept as OPTIONAL fast-travel (world walkable without it). Per-settlement identity (west): Marsh bog terrain + dread music. Report docs/CONNECTED-OVERWORLD.md + PLAYTEST-LOG. DEFERRED (HONEST, HARD RULE 10): relocate the OTHER settlements (Stonereach→Peaks N, Saltbreak→Coast E, Cragfoot/Cribbins, Caught→Emberwood S, Capital→Spire N, Dustreach/Thornwell expansion) — same entrance-relocation pattern, still board-reachable meanwhile; overworld town SILHOUETTES (just signpost+door now — solid buildings tripped the collision gate); spell-route traversal GATES (need the abilities built — electric/dash-leap/etc. don't exist; Fenwick electric-spur kept walk-reachable, flagged); 24×24 world expansion (still 20×20 — far-N Capital/Spire + far-SE Dustreach don't fully fit); M-map settlement pins. Committed; no force-push. **NEXT: pass 2 — relocate the Peaks/Coast/Emberwood/Spire settlements to their overworld regions (same pattern) + town silhouettes; then the abilities-mechanics session for the spell-route gates; then the 24×24 expansion.**
## [DONE 2026-06-10] BUGFIX 2 — door placement + dash-into-void + bush-farm + overworld diagnosis (14 gates + 23 navGate/containment suites GREEN; 0 errors). (1) INTERIOR DOOR MID-FLOOR: the door sprite was centered on the walkable trigger tile (1 from the wall) → floated mid-floor; FIX: doorWallOffset() pushes the door SPRITE one tile toward the room's nearest wall (spriteDy/Dx) so it reads as a doorway IN the wall, trigger tile unchanged (cemetery door now in the wall). (2) DASH-INTO-VOID: couldn't repro via swept-dodge (it contains everywhere live), so added a DEFINITIVE hard interior containment CLAMP in update() — clamps the player center to interior bounds + cancels the dodge (a breach snaps back); extended the CONTAINMENT navGate to assert dash inputs (sealed walls + interior flag + bounds match nav). Verified: real dash L/R/U into walls all contained. (3) BUSH INSTANT-RESPAWN FARM: _cutSwing granted loot every cut + delayedCall(12000) respawned in-area → infinite farm; FIX: cut bushes recorded in _cutBushes (world-tile id→timestamp), disabled with NO in-area respawn, regrow only on area RE-ENTRY after CUT_REGROW_MS (3 min). Verified real swings: stays cut, no repeat loot, regrew=true after cooldown. DIAGNOSIS (no build): the connected west-walking world from Van's map is NOT built — the streamed overworld is the OLD 8-region skeleton (GH 288, Marsh 238 west, Peaks/Coast/Emberwood/Spire); Mirefen(442)/Fenwick(480)/Cemetery(430) are far-band board-door scenes reachable ONLY via the GH notice-board (walking west hits the streamed Marsh, not them); Van's edited map (world-map-vanedit.json) was never built into the overworld geometry. Report docs/MARSH-BUGFIX.md (pass 2) + PLAYTEST-LOG. Committed; no force-push. **NEXT (big): build the CONNECTED OVERWORLD from Van's map — place Mirefen/Fenwick/Cemetery as walk-to overworld locations at his coords (west of GH), so the world is traversable, not a hub of separate board-door scenes.**
## [DONE 2026-06-10] BUGFIX pass — doors + interior containment + Fenwick visibility (14 gates + 23 navGate/containment suites GREEN; 0 errors). Four bugs Van hit, each real-input verified: (1) DOOR "too loose" = a VISUAL mismatch — _checkDoorWalk is already exact-tile (live: walking BESIDE a door does not enter, ON it does); the tiny prop_sign on the front-yard tile read as "near the house". (2) INTERIOR VOID = griddedSettlement floored ONLY the streets → the building-blocks were unfloored black holes; FIX: floor the WHOLE settlement interior (continuous ground, colliders on the blocks) + a new CONTAINMENT navGate asserting every interior/settlement has NO walkable perimeter tile (23 sealed, regression-proof). (3) EXIT-IS-A-SIGN → drew prop_door (a wooden door + stone frame + arched lintel + knob; ORIGINAL pixel art, CC0, ledgered) and swapped all 22 door/exit markers prop_sign→prop_door (the eliza/victorian door sheets were side-view/windows, not front-facing) — the chapel exit now reads as a real door. (4) FENWICK not visible = NOT a build bug — it's built (4 houses + 2 NPCs + cottage) + its board door tile is walkable; entered Fenwick by REAL walking live; the cause was a stale browser tab (Vite HMR doesn't restart the Phaser scene → hard-refresh :5273) + needing to navigate to the south board. Report docs/MARSH-BUGFIX.md + PLAYTEST-LOG (mirefen-floored · door-closeup · fenwick-reached-walking). Committed; no force-push. **NEXT: the settlement density-fill polish + the abilities-mechanics session (dash-leap + electric) for the M1 gates + Fenwick's electric entry; then deep M8-M10 + the post-M1 triple-check.**
## [PARTIAL 2026-06-10] MARSH step 2c — Mirefen + Fenwick: greybox grids → REAL furnished marsh-towns (22 navGate suites + 14 gates GREEN; 0 errors; 154fps). ENGINE (write-once): extended griddedSettlement to accept buildings/npcs/dressing/groundTint/spawnTile (greybox grid → dressed populated town; greybox footprint-markers auto-suppress when buildings given). MIREFEN: 12 weathered bog-tinted houses + 4 forge NPCs (Bett/Coll/Mire/Tam) + dead-pine/barrel/reed dressing + eerie groundTint + Yssa's furnished hut — core reads as a real marsh-town. FENWICK: 4 houses + 2 NPCs (Pell/Sedge) + a furnished cottage (bed/hearth/table/dresser + child NPC) + eerie tint; its ⚡electric-spur entry DEFERRED (abilities session) but kept walk-reachable (GH board door) so NOT orphaned — flag: gate that door on electric once it exists. navGate CASES added (22 interiors proven). Report MARSH-BUILD.md §2c + PLAYTEST-LOG (mirefen-before/after/closeup · fenwick-before/after · fenwick-cottage-interior). HONEST (HARD RULE 10): real dressed cores, but coverage NOT uniform — bare greybox blocks remain at grid edges (density-polish pass). DEFERRED: density-fill, deep M8-M10, dash/electric ability-gates (engine-blocked) + frog. Committed; no force-push. **NEXT: density pass (fill bare edge-blocks) → the Marsh is fully finished; then the abilities-mechanics session (dash-leap + electric) for the M1 gates + Fenwick's electric entry; then the post-M1 triple-check (BUILD-PLAN §4b).**
## [PARTIAL 2026-06-10] MARSH step 2b — the LOST CEMETERY (finished) + a furnished Mirefen interior (21 navGate suites + 14 gates GREEN; 0 errors; 156fps). DONE + eyes-on: (1) THE LOST CEMETERY finished — vetted+extracted LPC Grave Markers Rework (bluecarrot16, CC-BY-SA 3.0/GPL 3.0, bundled licence), cropped 5 grave props (headstone/cross/woodcross/open/large) → public/art/furniture + ledgered; built lost_cemetery (22×16 walled graveyard, eerie dark floor groundTint, founder's stone + 2 mourning angels, rows of varied graves, a fresh OPEN grave with Mother Cray the gravekeeper, offering chest, lych-gate inscription, mus_marsh dread bed, walk-in from the GH board). (2) Yssa's Hut — a furnished Mirefen interior (bed/hearth/table/dresser + Elder Yssa) walk-in from the Mirefen street. (3) ENGINE (write-once): interiorRegion now supports npcs + groundTint (themed interior floors, applied to the floor renderTexture). navGate CASES added for both (21 interiors proven). Report MARSH-BUILD.md §2b + PLAYTEST-LOG (cemetery-built/eerie · mirefen-town · mirefen-hut-furnished). HONEST (HARD RULE 10): 🟡 Mirefen TOWN overworld still greybox grid (real marsh-buildings = next pass); ❌ Fenwick not built; ❌ deep M8-M10; ❌ dash/electric gates (engine-blocked) + frog (deferred to the abilities session). Committed; no force-push. **NEXT: finish the Mirefen town (griddedSettlement→real marsh-buildings) + Fenwick; then the abilities-mechanics session (dash-leap + electric) to place the M1 ability-gates.**
## [PARTIAL 2026-06-10] ASHEN MARSH build (M1 step 2) — ATMOSPHERE SLICE done; structural builds deferred (14 gates GREEN; 0 errors; 148fps). DONE + eyes-on: the Marsh's eerie identity — (1) re-tinted the bog olive 0x8d9a6e → murky grey-teal 0x5a6357 (water near-black 0x2e433e, grey dead trees/decals; ground mean RGB measured (39,79,22)→(25,50,17), visibly darker/colder); (2) wired mus_marsh (HydroGene "Spirits Forest" dread bed) in BootScene + fixed _musicForRegion (R.bog/key 'Marsh' → mus_marsh); (3) bog ambient = amb_wind (desolate, no birds); (4) +8 grey dead pines + 4 reed clumps = a denser brooding dead mire. Holds: Yssa+Hagga (M8 hook + Insight/deception social demo already wired), 10 NPCs, 7 enemies, collision/depth/morality-HUD. Report docs/MARSH-BUILD.md + PLAYTEST-LOG (shots marsh-before/after-eerie/after-dressed). DEFERRED (honest, HARD RULE 10) → next sessions: Mirefen full furnish (greybox→GH-pipeline), the Lost Cemetery 30×24 vignette (new region: grave-marker crops+def+door+navGate), Fenwick build-out, deep M8-M10 wiring, the frog stopgap reskin. ENGINE-BLOCKED (HARD RULE 2 flags): the DASH ability-gate needs a dash-leap-over-gap mechanic (swept-dodge is anti-tunnel, stops at solids — can't cross a gap); the ELECTRIC ability-gate needs the electric ability built (doesn't exist in-game). Still tinted-grass (bog-tile gap — true mud/black-water tiles would finish the look). Committed; no force-push. **NEXT: a focused Mirefen-furnish + Lost-Cemetery build pass; and a mechanics session to build the dash-leap + electric abilities so their M1 ability-gates can be placed.**
## [DONE 2026-06-10] DESIGN-LOCK — "every traversal ability gates ≥1" (Van's rule) + a verify gate (13→14 gates GREEN). AUDITED the locked map (gating.js + Van's edits): walk/lantern/grapple/hookshot/firefrost gated ✅; ice→Dustreach, wind→Lighthouse, electric→Fenwick (Van's map edits) ✅; but cut/dash/fire/bomb gated NOTHING (decorative). ADDED them as required ABILITY_GATES in gating.js (additive optional secrets, no-soft-lock): cut=GH orchard brambles, dash=Marsh broken-boardwalk gap, bomb=Shrine cracked crypt wall (SA3), fire=Peaks frozen alcove (fire-melt). Now ALL 11 traversal abilities gate ≥1. NEW VERIFY GATE (14th): ability-coverage — reads gating.js (GATES tool-requires + TEASES + ABILITY_GATES) and FAILS if any ability gates nothing (so it can't regress). Per-ability "where required" table in BUILD-PLAN §1b. M1 BUILD PLAN HOLDS — 4 of the new ability-gates land in Milestone 1 (cut GH · dash+electric Marsh · bomb Shrine — built alongside the Marsh/Shrine, all optional caches/spurs). PLANNED MILESTONE GATE (BUILD-PLAN §4b, NOT now): after M1 is BUILT (GH+Marsh+Shrine), run the full TRIPLE-CHECK cohesiveness + EXCELLENCE pass against the BUILT game (real cohesion judged on built regions) before M2 scales. Spec/gating/verify/BUILD-PLAN/PLAN; no force-push. **NEXT: build Milestone 1 step 2-3 — the Ashen Marsh + Mirefen + the Sunken Shrine (with the cut/dash/electric/bomb ability-gate secrets) on the proven pipeline.**
## [DONE 2026-06-10] SOLIDIFY — Van's edited map LOCKED as truth + unified BUILD PLAN (design/reconcile; gates GREEN). Imported Van's editor export → docs/world-map-vanedit.json; locked into MASTER-WORLD-SPEC §0.5 (his node moves [shrine→N (5.5,10), mirefen/dustreach/fenwick/cragfoot/highpass/weeping], per-settlement music + elevation, +14 music zones + 2 terrain zones [dark forest, mountain streams], a NEW node "The Lost Cemetery" village 30×24, and traversal edits [Mirefen↔Fenwick=electric, Thornwell↔Dustreach=ice, Saltbreak↔Lighthouse=wind, NEW Shrine↔Cragfoot=fire + Cragfoot↔GH=firefrost, GH↔Mirefen wide-winding-road w3/5bends]) = AUTHORITATIVE, spec reconciled TO them. docs/BUILD-PLAN.md (the deliverable): (1) TRAVERSAL-GATING table from the DAG order (lantern→grapple→hookshot→firefrost→spells) reconciled with Van's edits — critical spine same acyclic key-before-lock DAG, spell-routes ADDITIVE → NO soft-locks; 2 FLAGS (Fenwick electric-only + Lighthouse wind-only = late SIDE spurs, Van's call). (2) SETTLEMENT IDENTITY: every settlement has music+terrain+elevation; 6 music beds to SOURCE (marsh-dread/storm-surf/sacral/fire-frost/dungeon-tension/cemetery-eerie — CC0 from Van's RPG-music zip), HAVE mus_green+mus_peaks, villages reuse region beds. (3) CROSS-CHECK MATRIX (aspect × locked map): quests/story/NPCs/standouts/jandal-cop(Wiremu)/raids/cameos/gags/philosophy all HOMED + reachable in valid order; PLACED difficulty t1→t5 maps to geography; assets buildable (rich library + forge); only GAPs = the 6 beds + frog/hen sprite + a few deferred bespoke VFX — everything FITS. (4) ORDERED BUILD PLAN + MILESTONES: M1 the SHIPPABLE 5h core (Greenhollow [built] → Ashen Marsh+Mirefen+Cemetery+Fenwick → Sunken Shrine) → M2 replicate the 6-region spine (Peaks/Coast/Emberwood/Spire, the 5 endings) → M3 expansion + the spell-route web; per-region build checklist + tiny asset-gaps each. (5) door-knock/morality entrance = a build-phase feel-layer (hook _enterArea). Docs + PLAN; no force-push. **NEXT (action it): build Milestone 1 step 2-3 — the Ashen Marsh + Mirefen + the Sunken Shrine — on the proven pipeline; grab marsh-dread bed + a frog sprite first.**
## [DONE 2026-06-10] ENTRANCE bugfix (chapel) + WALK-IN/WALK-OUT transitions (13 gates + 19 navGate suites GREEN; 0 errors; stamp 7c920496). BUG: the chapel door + its press-E approach tile were buried under the 5×5 prop_house_paneled collider → unreachable (the forge/store/cottages worked because they're smaller / press-E reached from an adjacent tile). Walkability-probed every GH door tile → broken (non-walkable door tile): gh_chapel (worst), gh_store, gh_home1, gh_home2 + 3 world-board doors. FIX: moved each to the walkable FRONT step (chapel gy11→gy15, store gy16→gy17, home1 gy26→gy28, home2 gy30→gy31, all 10 board doors gy36→gy37). FEEL: switched ALL door/stairs transitions from press-E to WALK-THROUGH — _checkDoorWalk() in update() edge-triggers when the player steps ONTO a door tile (reads the current region's via:'door' interactables); rising-edge + _lastTile set on transition + 180ms debounce → spawning on a door tile doesn't re-fire. via:'door' no longer registers press-E. Applied uniformly overworld↔interior↔floor-stairs; the Phase-0 return-stack + state-persistence intact. VERIFIED real-input (drove movement): walk UP into the church enters (bug fixed); all 6 GH building doors walk-in; walk onto the exit → outside at the door; walk up/down the Tankard stairs; no instant-re-enter (stood on the spawn-door, stayed). interiors.test 19 pass. Shots: walkin-chapel-reached (church reached by walking) + walkout-back-at-church. Committed; no force-push. **NEXT: a doorway sprite (the door markers are still prop_sign placeholders) + carry walk-in to the other regions' buildings; then the Ashen Marsh build.**
## [DONE 2026-06-10] GREENHOLLOW first REAL build — FURNISHED interiors via the rich library + forge (13 gates + 19 navGate suites GREEN; 0 errors; 60fps; stamp 309c4d03). Stopped greyboxing — closed the furnished-interiors gap. Wired 6 REAL furniture props (cropped single sprites from the ledgered library → public/art/furniture/, registered in assets.js PROPS + interactionClasses.js): prop_altar (LPC Statues, CC-BY/CC-BY-SA), prop_anvil/bed/table/dresser/fireplace (ElizaWy LPC Objects, OGA-BY) — ledgered (public/art/furniture/ row; licence gate now 175 files/10 entries). Re-furnished the GH interiors (worldmap.js): chapel = a REAL stone altar at the nave head (the fountain-altar FIXED) + pews; Hodge's forge = a real anvil + a smith's workbench; 2 cottages = bed/hearth(fireplace)/table/dresser (lived-in); the Tankard = hearth + tavern tables + casks, upstairs = beds + table + dresser. Furniture placed solid → interiorRegion carves the nav tile + adds the collider → navGates STILL validate (all 19 interiors re-proven; furniture never blocks the spawn→door/chest paths). Town stays populated (14 forge-composed LPC NPCs — hero + NPCs the same paper-doll), warm pastoral, safe-mode + morality HUD live. 9-FACTOR DoD all green (composition now REAL furniture, not greybox; nav/readability/art-dir/density/collision-depth-interaction/audio mus_green+birds/perf 60fps/no-soft-locks). LIVE (real input, 4 shots, 0 errors): the chapel altar, a furnished cottage (bed/dresser/hearth/table), the populated town. NOTES: the altar+anvil are quick single-frame crops (precise extraction = small polish follow-up); more furniture variety + wiring the new Victorian/Colonial building sets into the town = next finish steps; the crop→PROPS+class→interiorRegion→navGate pipeline is PROVEN + repeatable per interior/region. Report: docs/GH-FIRST-REAL-BUILD.md + PLAYTEST-LOG row + gh-before/after + 2 interior shots. Committed per-group; no force-push. **NEXT: Van judges the FEEL; then expand furniture variety + wire the Victorian/Colonial buildings into the town composition, and proceed to the Ashen Marsh + Sunken Shrine (the next 5h-core region) on the same proven pipeline.**
## [DONE 2026-06-09] GROUNDED DESIGN v2 — KEEP-FIRST/optimistic reconciliation vs the RICH library (corrects prior pessimism; design/audit; gates GREEN). docs/GROUNDED-DESIGN.md rewritten: assets are NOT a wall. The forge (Universal-LPC compose/recolour, wired) gives UNLIMITED humanoid NPCs + enemies + BOSSES; interiors CLOSED (eliza furniture); buildings (victorian-city/colonial-church/adobe/desert-ruins/blacksmith) + terrain (vale/stone/snow/desert/coast/lava/cave; bog 🟡) covered; crypt/cavern dungeons + altar. SURVEY: most of the world FULLY-supported NOW. AREAS: all 6 KEEP (Emberwood light RE-THEME to burnt-vs-frozen via lava+ice; Spire god-reveal VFX + Forest/Desert expansion DEFER) — NONE cut. QUESTS: the 5h core (M1-10) + all GH/town/morality quests + the 5 named BOSSES (render as imposing LPC humanoids — genuinely buildable, not 're-skins') + the standouts (Wiremu/Hagga/Pellamy/Sela/gunhand via forge recolour) all KEEP; a few EDIT for the frog/hen/Wanderer-slab; optional beast-bounties (Crag Beast/Cinder Stag/Wreck Wraith) deferrable; CUT: NONE. SCOPE: GENEROUS — the rich library supports much of the 30-40h vision; the 5h core ships on current assets + 3 tiny items. THE TINY GAP LIST (3): (1) confirm lpc-monsters.zip licence [DEFERRABLE — humanoid bosses + 8 monsters cover the core] → unlocks beast bounties; (2) 4 region music beds [marsh NEEDED for the 5h core, coast/ember/spire DEFERRABLE]; (3) a frog + hen sprite [NEEDED — heart-beats: SA4 Frog + the chicken callback]. NEEDED-BEFORE-FIRST-BUILD = frog + hen + marsh music bed; everything else for the core is in-library. Doc + PLAN; no force-push. **NEXT: grab the frog + hen + marsh bed (+ confirm the monster licence when convenient); then WIRE the supported assets (terrain atlas + the building/dungeon/altar sets + interior furniture) + the forge NPCs/bosses into the first real build (the 5h core) — the library supports the ambition.**
## [DONE 2026-06-09] GROUNDED DESIGN — story/areas/quests reconciled against the ACTUAL asset library (design/audit; gates GREEN). docs/GROUNDED-DESIGN.md grounds the build in what we can RENDER (post-intake), keeping the story's soul. ASSET-REALITY SURVEY: terrain (vale/stone/snow/sand/lava/coast/cave) + buildings (village/CITY-victorian/church-colonial/adobe/desert-ruins/forge-blacksmith) + full interiors + altar/statues + ANY LPC NPC + crypt/cavern dungeons = WELL-SUPPORTED; the real CONSTRAINTS = enemy/boss variety (only slime/snake/bat/worm/eyeball/ghost/flower/pumpking + LPC skeleton/orc; no frog/drowned/crag/elemental/bosses) + region music (only GH+Peaks beds). AREAS: all 6 KEEP — Marsh/Oracle-Capital/Emberwood RE-THEME to renderable looks (bog=tinted-grass+water+reeds; Capital=temple-city via colonial+desert-ruins+statues; Emberwood=burnt-vs-frozen forest via lava+winter tiles), Spire-binding-art + Forest/Desert expansion DEFER; NONE cut. QUESTS: the 5h core (M1-M10) + the GH/town quests all KEEP (buildable now); at-risk heart-beats KEPT via EDIT/RE-PLACE — the Frog (reskin/source), the bosses (LPC-humanoid reskins for now), the kicked chicken (a hen sprite), Wiremu (LPC recolour), the wrecks (re-placed as ruined hulls), SE2 split (lava+ice tiles); the off-theme police/cars Downloads PNGs NOT used (UNKNOWN+off-theme). CUT: NONE. SCOPE: the 5h slice is SHIPPABLE on current assets + a 3-item shopping list; the full 30-40h stays intact (re-themes, not cuts); bespoke boss/god VFX = deferred polish, not scope. TARGETED GAP-SHOPPING-LIST (most leverage): (1) confirm lpc-monsters.zip licence [staged-UNKNOWN] = the biggest enemy/boss unlock; (2) 4 region MUSIC beds (marsh/coast/ember/spire — CC0, read the readme of the RPG-music zip Van has); (3) a FROG + a HEN sprite (signature beats). Doc + PLAN; no force-push. **NEXT: Van rules the re-themes + the shopping list; then WIRE the supported assets (terrain atlas + the new building/dungeon/altar sets + interior furniture) into the build, and produce the enemy reskins — the bottleneck is wiring + enemy art, not environments.**
## [DONE 2026-06-09] MASTER WORLD SPEC (the SSOT) — every place·contents·connections·progression (DESIGN ONLY; supersedes prior world docs; existing game preserved; gates GREEN). docs/MASTER-WORLD-SPEC.md + docs/master-world-map.png (from scripts/worldgraph/gen-master-world.mjs — node/edge data = the spec). THE AUTHORITATIVE world design the build references, holding all the existing content. 8 complete sections: (1) PLACE-SIZE TABLE — 20 places, tier + exact tile-size (cities Saltbreak 120×96/Capital 104×88/Stonereach 96×80; towns GH 64×52/Caught 56×44/Mirefen 44×36; villages 26-34; dungeons 36-44/floor ×2-4F; secrets 14-20) + chunk positions. (2) PER-PLACE CONTENTS MANIFEST — each place's NPCs (incl. standouts Wiremu/Hugh/Pellamy/Sela/gunhand/ninja-gran/the Wanderer), quests (which M-beats + sides), items/loot (the 4 tools + 5 shards + gear/relics), enterable buildings → interiors; CROSS-CHECK: every quest M1-20/SG/SA/SP/ST/SE/PH/CAM/EDG/GAG/NIGHT + 30+ NPCs + standouts HOMED, nothing unplaced. (3) CONNECTION TABLE — every link's traversal MEANS (walk/lantern/grapple/hookshot/firefrost/cut/dash/the spells electric-switch/ice-freeze/fire-melt/wind-gap/a bridge) + min/avg/max path-length + gate (ability AND/OR story). (4) PROGRESSIVE INTERCONNECTION (Van's key insight) — each place has 2-3 routes that open across EARLY(walk/lantern/cut)/MID(grapple/hookshot)/LATE(firefrost/spells/story) toolkit; the same overworld reveals NEW links as abilities/story grow (⚡electric N-gate→Peaks, ❄ice river→Coast, 🪵repaired bridge→Caught, 🌀wind→Forest) — a web, not a linear chain; backtracking with new tools opens old regions (metroidvania). (5) ACCESSIBILITY ORDER — unlock per place (ability+story); reconciles gating.js (same acyclic spine, progressive routes ADDITIVE so no soft-lock); confirmed valid + progressively interconnected. (6) EXPLORATION SPACE — region wilds ~120-160 tiles + forking connectors 40-70 wide, populated by placed encounters/secrets/landmarks/the Wanderer; TOTAL 768×768 overworld + ~55-70 separate scenes. (7) CROSS-CHECK MATRIX (facet × map): quests ✅, NPCs ✅, monsters/PLACED difficulty tier 1→5 maps to geography ✅, combat/abilities/spell-routes ✅, audio designed-but-only-GH-recorded 🟠, ASSETS the big GAP 🔴 (biome terrains/dungeon tilesets/furniture/standout NPCs = major sourcing). (8) MAP IMAGE — true-relative-scale shapes, stage-coloured traversal-labelled connections, geography+grid+dims. Supersedes WORLD-GRAPH/SCALE-DOD/MAP-PLAN (adds contents+connections+progression). FLAGS: greybox build used HALVED sizes (the SSOT full sizes are the build target); Forest/Desert+spell-routes not yet in gating.js (D-geo/D-spell); assets 🔴. [DECISIONS] approve SSOT sizes + progressive-route design + 768×768 · rule D-geo/D-spell · greenlight asset sourcing. Map+generator+spec+PLAN committed; no force-push. **NEXT: Van APPROVES the SSOT (sizes/contents/progression/map); on approval → build to it (resize overworld to 24×24, author cities/dungeons as separate scenes with the progressive multi-route connections, then the asset-fill).**
## [DONE 2026-06-09] WORLD LAYOUT BUILT — the halved-scope world, greybox + WALKABLE (13 gates + 19 navGate suites GREEN, 27 regions; 0 errors; stamp 50ab969f; preserved @ tag pre-world-layout). BUILT (not planned) the content-sized halved world: 1 CITY (Saltbreak 56×40 — a street grid of building-block DISTRICTS, 999 walkable street tiles, 4 district chests: docks/market/mansion/underworld) + 2 TOWNS (Stonereach 36×28, Mirefen 34×26) + 5 VILLAGES (Fenwick/Cribbins/Cragfoot/Oasis/Thornwell 20×16) + 2 DUNGEONS (Sunken Shrine, Cinder Keep 24×18). New griddedSettlement() builder auto-blocks-out each place at its content-sized footprint: a STREET GRID (walkable lanes every `pitch`) with building-footprint blocks (solid) between — city dense, village open. Per the approved WORLD-MAP-PLAN structure: overworld + SEPARATE enterable scenes (Phase-0 door system), so a 56-tile city doesn't bloat the streamed overworld. CONNECTED + WALKABLE: reachable from a GH WORLD-LAYOUT board (10 signposts → walk in, walk the streets, exit); the open-world BETWEEN = the existing 8-region overworld skeleton (forking paths + barriers + the tool-gates, GH→all walkable). navGates PASS every place (interiors.test: the street grid connects spawn→every district chest+exit, blocks hold, no prop on a street; 19 interiors+settlements proven; no soft-locks). TOTAL: a 640×640 overworld (20×20 chunks, the walkable open-world) + 10 content-sized scenes + 9 interiors = 27 regions. Gate change: interiors/settlements EXCLUDED from seam-coherence (separate door-connected scenes, not overworld seams — validated by navGates, like entrance-coherence). M-MAP FIXED + updated: was stretched by the far-band interiors since Phase 0 — now overworld-focused + lists the 10 content-sized places with sizes. LIVE (real input, 0 errors, 3 shots): the city blockout (streets/districts), a village (smaller/open), the M-map. HONEST FLAGS: greybox only (placeholder dirt + building markers — NO final art, the fill-pass is next); the city is a regular auto street-grid (organic shapes + building art = the art pass); doors clustered on the GH board (distributing them across the overworld at grid positions is a wiring follow-up — overworld traversal already works); overworld stays 640² (resize to the plan's 768²/24×24 is the next step if approved). Report: docs/WORLD-LAYOUT-BUILD.md + wl-1-city/wl-2-village/wl-3-worldmap. Committed per-group; tag pushed; no force-push. **NEXT: Van WALKS + approves the laid-out world (the city/towns/villages scales + the layout); on approval → the ASSET-FILL pass (real terrain/buildings/furniture per place) and/or distribute the doors across the overworld + resize to 24×24.**
## [DONE 2026-06-09] WORLD MAP PLAN — gridded, content-sized, cross-checked → derived dimensions (DESIGN ONLY; existing game preserved @ tag pre-world-restart 517a8f59; gates GREEN). Van's circles-and-lines method: every place = a circle SIZED BY CONTENT LOAD on a SQUARE 24×24-CHUNK GRID, gated paths, geography → read off the dimensions. DELIVERABLE = docs/world-map-plan.png (from docs/world-map-plan.svg via scripts/worldgraph/gen-worldmap-plan.mjs — the sizes/positions ARE the spec) + docs/WORLD-MAP-PLAN.md. STRUCTURE (key): overworld + SEPARATE enterable scenes (NOT one giant map) — the overworld holds region wilds + settlement exteriors + connectors + roaming; cities/dungeons/interiors are separate scenes (Phase-0 system) reached by a door, so a 120-tile city doesn't bloat the overworld. PER-PLACE SIZES (derived from content): 3 HUGE CITIES multi-district — Saltbreak 120×96 (the densest: docks/underworld/market/mansion), Oracle Capital 104×88 (Sela seat + libraries/temples), Stonereach 96×80 (mine-city + Order seat); towns 44-64 (GH 64×52, Caught 56×44, Mirefen 44×36); villages 26-34; dungeons 36-44/floor ×2-4F (Hollow Spire ×4F, Cinder Keep ×3F, Sunken Shrine ×3F, etc.); secrets 14-20. DERIVED TOTAL: a 768×768-tile (24×24-chunk) OVERWORLD (up from 20×20) + ~55-70 SEPARATE enterable scenes (3 cities multi-district + ~10 dungeons ×2-4F ≈ 25 floor-maps + ~30-50 interiors). Engine-feasible: proximity-streaming unchanged (~25 chunks live), separate scenes load on enter (proven). CROSS-CHECK MATRIX (every facet × map): QUESTS every quest/cameo homed ✅; ENEMIES the PLACED difficulty curve maps to geography (tier 1→5 GH→Marsh→Peaks→Coast→Emberwood→Spire) ✅; COMBAT/skills/safe-zones/tool-gates fit ✅; AUDIO designed per region but only GH recorded 🟠; ASSETS the big GAP 🔴 (most biome terrains + dungeon tilesets + interior furniture + standout NPCs need sourcing). SUPERSEDES WORLD-GRAPH (no sizes) + WORLD-SCALE-DOD (~30×30) — fixes dims at 24×24 (tighter because cities/dungeons are separate scenes). FLAGS: Forest/Desert not yet in gating.js (D-geo to adopt; spine unchanged); assets = a major licence-vetted sourcing workstream. [DECISIONS] approve 24×24 + separate-scenes structure + city identities/sizes · rule D-geo · greenlight asset sourcing. Map + generator + spec + PLAN committed; tag pushed; no force-push. **NEXT: Van APPROVES/ADJUSTS the gridded map + the 24×24 dimensions; on approval → build to this plan (resize the overworld, author the cities/dungeons as separate scenes via the Phase-0/1 systems) OR continue Phase 3 (Marsh + Sunken Shrine) on the current world.**
## [DONE 2026-06-09] PHASE 2 — Greenhollow town to finished quality + enterable interiors (the showcase; 13 gates + 15 suites GREEN, 17 regions; 0 errors; 57fps; stamp 6848b65f). GH built into a lived-in small town with 7 ENTERABLE interiors (Tankard f1/f2 from Phase 0 + 5 new: Hodge's forge, Pem's store, the chapel [a real nave — altar-font + 2 pew rows + aisle], 2 cottages), each via a working building door (via:'door' → interiorRegion). Extended interiorRegion() with FURNITURE (a solid piece carves its tile out of the nav so navGates still validate — furniture never blocks spawn→door/chest; proven in interiors.test). + a non-solid FLORA enrichment (14 varied-scale/tint undergrowth clumps framing the lanes — the variety rule, zero nav impact). LIVE (real input, 0 errors): all 5 building doors enter their interior (forge/store/chapel/2 cottages); forge + chapel screenshotted (furnished, walkable, enclosed view); BEFORE/AFTER town shots. 9-FACTOR DoD: 8 ✅ (nav-first/readability/art-dir warm-pastoral/density[315 props,14 NPCs]/collision-depth-interaction/audio[mus_green+amb_birds playing]/perf 57fps/no-soft-locks+no-prop-on-walkable), composition 🟡 GOOD (greybox furniture flagged). All 9 interiors navGate-valid (interiors.test). Permanence carries over (same Phase-0 chest/SaveManager path). HONEST FLAGS (HARD RULE 9): interior furniture is greybox (barrels/fountain-as-font/fences-as-pews — proper beds/tables/shelves/anvil/hearth/altar are an ART need; chapel reads best); the overworld is a good small village not a dramatic sprawl (GH small by design; the 3 huge cities are the full game); NPC presence only (full AI/dialogue = Phase 4). Report: docs/PHASE2-GREENHOLLOW.md + PLAYTEST-LOG row + p2-before/after + p2-interior-forge/chapel. Committed per-group; no force-push. **NEXT (Phase 3): the Ashen Marsh + the Sunken Shrine — region nav-first + the shrine HYBRID (hand-craft key rooms + the Phase-1 generators for fill), the lantern puzzle, all navGate-validated.**
## [DONE 2026-06-09] PHASE 1 — the room-template library + procedural GENERATORS + the generate→validate loop (the multiplier; 13 gates + 15 suites GREEN; 0 errors; stamp cb3fca4b). The automation that fills rooms/dungeons, built once for 5h + 30h. src/data/gen/: (1) templates.js — 8 hand-designed 9×7 room templates (entrance/normal×4/treasure×2/boss) the generator stamps + connects (Spelunky/Isaac crafted-pieces model; the connection cross row3+col4 kept clear). (2) index.js generators: DUNGEON (a connected grid of template rooms — Isaac-style growth from the entrance, boss=graph-farthest cell, treasure=dead-end, 2-tile doors carved between adjacent rooms, connected-by-construction); CAVE (cellular-automata ~45%+4-5 rule+4 passes + flood-fill → keep largest region = all floor reachable, spawn near-corner, objective chest at BFS-farthest); constrained Poisson-ish PROP-SCATTER (min-spacing + density roll, clear of spawn/chest, only non-solid on walkable — X pillars carve the nav); data-driven LOOT (chests from room roles). (3) THE GENERATE→VALIDATE LOOP — every output run through navGates (bodyWalkReachability + noSolidPropOnWalkable), RETURN only on pass, else reject+regenerate (retry cap) → guaranteed-valid fallback. Output = the Phase-0 interiorRegion()/region shape, so collision/depth/interaction-classes/save/streaming all apply unchanged; generated regions injected into REGIONS at runtime (reserved gen slot → no static-gate impact). HEADLESS (scripts/fitcheck/gen.test.mjs, in npm test): 10 dungeons + 10 caves ALL pass navGates (0 fallbacks — reliable); REJECT-on-fail PROVEN (a walled-off objective is caught by bodyWalkReachability — what the loop rejects); FALLBACK proven valid (maxTries:0). LIVE (real input, 2 screenshots, 0 errors): GH 'Enter a GENERATED dungeon' door → 36×21 3-room dungeon (crafted rooms + 2-tile doors + 2 chests + corner pillars + spaced decoration, validated try 1); GH 'Enter a GENERATED cave' door → 26×18 organic CA cavern (one connected blob, spawn→far objective chest, validated try 1). Each door re-rolls a fresh seed per entry. Also: extended the Phase-0 enclosed view to hide chunk props/decals (no overworld bleed in interiors). NOTES: greybox art (prop_sign doors, prop_bush/rock decoration — Phase 2/3 swaps tilesets); 8-template starter set (grows in Phase 2/3); enemy/encounter tables stubbed (chests=loot path; enemies ride Phase 4); cyclic/grammar layouts a future upgrade. Report: docs/PHASE1-GENERATORS.md + p1-1/p1-2 screenshots. Committed per-group; no force-push. **NEXT (Phase 2): the full Greenhollow TOWN (hand-crafted, nav-first) + its template interiors — the finished-quality showcase (EXCELLENCE region DoD; Van's eye gate).**
## [DONE 2026-06-09] PHASE 0 — the INTERIOR / AREA-TRANSITION + MULTI-FLOOR system (the foundation; 13 gates + 14 suites GREEN; 0 errors; stamp af4235a0). Enter a building/cave via a door → load its interior as a separate area → move between FLOORS → exit back to the EXACT spot, with state persisting across every transition + save/reload. Built by REUSING the proven pipeline: an interior is just a REGION in a reserved far corner (tiles 520-604), so streaming + _buildRegion + pixel-truth collision + depth-sort + interaction-classes + navGates + SaveManager deltas all apply UNCHANGED. interiorRegion() builder (worldmap.js) = an enclosed room (floor patch + per-tile wall colliders, walkable inside, a spawn, doors/stairs as via:'door' interactables + chests). _enterArea(to) (OverworldScene) = teleport via a return-STACK: `to`=interior key (enter, push) or 'back' (pop → exit/descend/ascend); re-streams (far interior loads by proximity, overworld unloads); a dark backdrop + dim bg = the enclosed view. Floors = more interior regions (stairs push/pop) — the stack nests overworld→f1→f2→f1→overworld, returning to the exact tile. LIVE-PROVEN (real input, 4 screenshots): GH tavern door→tankard_f1 (enclosed, overworld gone) → upstairs→tankard_f2 → exit→back at GH tile (321,304); GH cave door→cave_f1→descend→cave_f2→ascend→exit. PERMANENCE: looted the f2 chest (22→52g) → re-entered, stays looted → F5 + full page reload + auto-load → still 52g, chest still looted (0 re-spawn) — SaveManager deltas work for interiors identically to regions. navGates (body-walk + no-prop) PASS on all 4 interiors (scripts/fitcheck/interiors.test.mjs, in npm test). One gate guard: interiors excluded from overworld entrance-coherence (they connect via the runtime door system, validated by navGates). NOTES: greybox look (door=prop_sign markers; real art is Phase 2/3); reload-while-inside restores position but an empty stack (minor save-the-stack follow-up; chest/state permanence unaffected). Report: docs/PHASE0-INTERIOR-SYSTEM.md + p0-1..4 screenshots. Committed per-group; no force-push. **NEXT (Phase 1): the room-template library + the procedural generators (BSP/cyclic layout, cellular caves, constrained scatter) + the generate→navGate-validate loop (BUILD-TOOLING) — the multiplier; then Phase 2 the Greenhollow full town.**
## [DONE 2026-06-09] 5-HOUR GAME SCOPE + BUILD-TOOLING (design/plan; gates GREEN). Two docs define the shippable slice + how to build it efficiently. docs/GAME-5HR-SCOPE.md: a COMPLETE ~5h game = Act 1 (Greenhollow childhood M1-M5 → M6 the burning) → M7 the adult return → the Ashen Marsh + the Sunken Shrine dungeon (M8-M9, lantern + Drowned Guardian + shard 1) → M10 Hagga's truth (the flame is a bound god) + the first PERMANENT choice = a real beginning-middle-END with a hook north. IN: 3 regions (GH grown to a real small town w/ ~6-8 enterable interiors + West Belt + Marsh w/ Mirefen), 3 dungeons (Sunken Shrine + Boarded Cave + Drowned Crypts), ~M1-M10 + ~8-10 curated sides/cameos (incl. SA4 the Frog good-monster + CAM7 Wiremu the pie-cop taste), ~13 NPCs, the lantern tool+puzzle, bounded gear economy, combat + the boss, the Morality+Purity HUD live + the aura-transformation TASTE + safe-mode, ~5h, replayable on the M10 choice. DEFERRED: M11-M20 + the other 4 regions + the 5-ending finale + the 3 cities + new biomes + the elemental spells + the full transformation sprites/law/standouts. It's the literal first 1/6 on the FINAL systems/world (expandable, not throwaway) + a 5h DoD. docs/BUILD-TOOLING.md: the HYBRID pipeline — HAND-CRAFT the town + story-critical rooms + overworld nav; TEMPLATE/PREFAB room library (shop/tavern/home/crypt/treasure/boss/corridors) the generator places+connects; PROCEDURAL for dungeon LAYOUTS (BSP + graph/cyclic mission-graph), CAVES (cellular-automata + flood-fill), constrained PROP-SCATTER (Poisson-disk, respecting no-solid-prop-on-walkable + variety rule), LOOT/ENCOUNTER tables — with GENERATE-THEN-VALIDATE (every generated space re-checked by navGates body-walk + no-prop, reject+regenerate so nothing broken ships). All flows through the proven tiledToRegion shape + autotiler + interaction-classes + entrances. NEW SYSTEMS: the interior/area-transition + multi-floor system (DW1, foundational) + the room-template format/library + the generator module (src/data/gen/). BUILD ORDER: Phase 0 interior system → Phase 1 the template-library + generators (the multiplier) → Phase 2 Greenhollow full town → Phase 3 Marsh + Sunken Shrine (hybrid) → Phase 4 quests/NPCs/combat/alignment → Phase 5 polish. Research cited (RogueBasin/Red Blob/Boris the Brave/Dormans/Spelunky/Isaac). 13 gates + 13 suites GREEN. Docs + PLAN; no force-push. **NEXT: Van approves the 5h scope + the tooling split; then Phase 0 — build the interior/area-transition system (the foundation).**
## [DONE 2026-06-09] WORLD GRAPH — the content-sized 2D node+edge MAP (design + a drawn image; gates GREEN). Van's graph-first method: the world sized FROM the content so it can't be too small. DELIVERABLE = a DRAWN map image docs/world-graph.png (rendered from docs/world-graph.svg, generated by scripts/worldgraph/gen-worldgraph.mjs — the node/edge data IS the spec) + docs/WORLD-GRAPH.md (the spec table). 54 nodes / 55 edges, each node SIZED BY CONTENT LOAD (huge city = big star, single-quest spot = small dot), region-coloured, paths drawn (dashed+🔒 = tool-gated per gating.js; dashed-grey = new biome), the river spine + biome tints for geography. PLACE-COUNT: 3 huge cities (Saltbreak/Stonereach/Oracle Capital) · 8 towns · 5 villages · 2 hut-towns · 9 dungeons + 5 caves (14 underground) · 9 secrets · 13 landmark/interior groups + new Forest & Desert biomes — ≈ Van's commercial ceiling (towns 8 vs 10, secrets 9 vs 10 → +2/+2 to fully reach). CONTENT-FIT CHECK: EVERY quest (M1-20, all sides, PH, CAM, EDG, GAG, NIGHT), cameo, gag, and 30+ NPC has a HOME node — nothing unplaced (table in the doc). HOURS: ~30-45h placed (meets 30+). The graph is sized to hold the whole game by construction (fixes the abstract-too-small map). FLAGS: the new Forest/Desert/river biomes are NOT yet in gating.js (drawn dashed-grey as expansion — D-geo to adopt; the critical spine GH→Marsh→Peaks→Coast→Emberwood→Spire is unchanged); the interior-system + at-scale assets (WORLD-SCALE-DOD) remain the foundational next builds. Rendered the SVG→PNG via a temp public/ wrapper (removed after). Map + script + spec + PLAN committed; no force-push. **NEXT: Van REVIEWS + ADJUSTS the graph (move/resize/rename nodes, confirm the 3 cities + new biomes); on approval → build the interior system (Phase 0) then the nav layer from the approved graph.**
## [DONE 2026-06-09] WORLD-SCALE DoD — sized BACKWARDS from the content (doc/audit only; gates GREEN). New docs/WORLD-SCALE-DOD.md fixes the fundamental issue: the map was sized abstractly + NEVER cross-checked against the content. CONTENT-TO-SPACE AUDIT of all 66 quests (20 main M1-M20 · 34 side · 12 cameo/edge/gag/night · 6 philosophy), 30+ named NPCs, 40+ named places → required place → exists today (✅/too-small/missing/needs-interior). FINDINGS: the content is ALREADY ~30h commercial-scale, but (1) ~30-50 INTERIORS are named + 0 are enterable (NO interior system = the #1 gap), (2) every 'settlement' (Mirefen/Saltbreak/Peaks town/Emberwood) is 5-10x too small for its NPCs/factions/quests, (3) the 5 dungeons + Spire are greybox grant-nodes (need real multi-room/multi-floor maps), (4) dozens of set-pieces unbuilt (the Frog fen, lighthouse, Weeping Tree, marble hall, High Pass…). DERIVED WORLD (counts/sizes/positions): ~3 huge cities (Saltbreak/Stonereach/a new Oracle Capital) · ~10 towns · ~7 villages/hut-towns · 5 main + 5+ sub dungeons · 10+ secrets · 30-50 interiors, on a ~30x30-chunk overworld (grow from 20x20) WITH interiors/dungeons as SEPARATE SCENES (so the overworld stays navigable — the Zelda/Pokémon scaling model, not a giant overworld). HOURS: 30-45h when built to scale (the too-small map crams it into ~12h). KEY INSIGHT: the fix isn't a huge overworld — it's the INTERIOR/AREA-TRANSITION + MULTI-FLOOR SYSTEM (DW1, the foundational #1 build), city support (DW2), wayfinding (DW3), + the at-scale ASSET pipeline (DW5 — an order of magnitude more art, all licence-vetted to the lock). PHASING (large multi-phase, 'finish not build-then-break'): Phase 0 interior system (prove on the Tankard + upstairs + one dungeon floor) → Phase 1 ONE full-scale Greenhollow slice → Phase 2 replicate region-by-region (assets first) → Phase 3 fill to Van's ceiling (extra towns/biomes/secrets) → Phase 4 polish. CROSS-IMPACT reconciled: gating.js (spine unchanged, interiors within regions), the 5 endings (deed-gated, MORE content = deeper), pacing (cities add the civic/cultural depth — libraries/temples/civilizations), difficulty (placed not scaled), + the BIG asset-GAP flag. [DECISIONS] DW1 build interior system first · DW5 start the at-scale asset hunt now · D-geo confirm new biomes (desert/river/forest) + the 3 cities' identities · confirm the 30x30 grid. Doc + PLAN; no force-push. **NEXT: Van rules DW1/DW5/D-geo; then Phase 0 — build the interior/area-transition system (the foundation the whole scale depends on).**
## [DONE 2026-06-09] WORLD-SKELETON — the whole-world navigation layer, GREYBOX, nav-first (v2 §8.3; 13 gates + 13 suites GREEN; 0 console errors; stamp 5f85cbd7). The ENTIRE world now walks end-to-end (8 regions) so Van can WALK THE WHOLE MAP + check layout/distances/forks/gating BEFORE art. Built the 3 missing regions from scratch as greybox nav skeletons via a reusable `greyboxRegion()` builder (worldmap.js): a spec of walkable corridor rects → a route region with per-tile colliders filling all non-walkable tiles (the channel walls the gates expect), an entry GATE keyed to a tool, a grant-point (records tools/shards so the chain stays walkable), greybox dirt terrain, + a `nav` grid for the runtime navGates. TIDEWRECK COAST (E of GH, seam x340, grapple-gated gorge → harbour + Drowned Vault[hookshot+shard3] + secret), EMBERWOOD (S, seam y328, hookshot chasm → camp + Ember Hollow[firefrost+shard4]), HOLLOW SPIRE (N of Peaks, seam y218, firefrost switchback ascent → summit Binding Chamber[shard5]). Entrances made two-sided + the verify entrance-gate relaxed to allow a SYMMETRIC link-barrier (a gorge/chasm to the open hub — the reciprocal can never soft-lock; ungated-into-gated now flagged as a hole). RUNTIME GATES (scripts/fitcheck/worldskeleton.test.mjs, in npm test): a real 16×8 body walks entry→town+dungeon+secret in all 3 regions; blocked zones hold; no prop on path. NO SOFT-LOCKS proven: entrance-coherence GREEN (14 entrances, all 8 regions reachable from GH; chain GH→Marsh→Peaks→Coast→Emberwood→Spire) + seam/channelled/density all GREEN. EYES-ON (live, real input): Coast greybox seamed onto GH renders + walkable (332px); the gorge gate BLOCKS without grapple (tile x339) + OPENS with it; the Spire switchback ascent renders; 8 regions stream; 0 errors. SCOPE (honest): greybox = STRUCTURE only (corridor + town-junction + dungeon grant-node + secret spur) — NOT final art/density/town-interiors or the vast CONNECTORS.md zones; grant-points are placeholders; navGates run on the 3 NEW regions (existing 4 already walk-proven). Report: docs/WORLD-SKELETON-REPORT.md + ws-coast-greybox/ws-spire-ascent screenshots. Committed; no force-push. **NEXT: Van WALKS THE WHOLE MAP + flags any mis-placed/mis-sized region; then per v2 §8.4 the GH→Peaks ART pass on the proven nav layer, then replicate art+density+connectors across the greybox.**
## [DONE 2026-06-09] NPC WORLD + SAFE-MODE + ELEMENTAL MAGIC design (doc-only; gates GREEN). New docs/NPC-WORLD.md + extended docs/ALIGNMENT-SYSTEM.md §6. SAFE-MODE LETHAL TOGGLE: by default you CANNOT kill people (non-lethal scuffles + consequences); killing requires a deliberate Options toggle-off with a warning — prevents accidental evil + soft-locks (DA8). NPC REACTION ARCHETYPES + combat-AI (consistent-by-type + light variety): Coward (flee) / Defender (fight back, weak humanoid combatant via the reserved EnemyController path) / Alarm-raiser (run for the law) / Lawkeeper (confront+subdue) / Bystander-propagator (witness→react+remember). LAW ESCALATION = graduated Heat/notoriety (warn → 2-3 guards + bounty → the standout Law-Enforcer), always escapable, never gates the story. STANDOUTS ('mess with the wrong person'): the unassuming GUNHAND, the NINJA GRANDMOTHER, and the escalating dignified LAW-ENFORCER wielding a TAONGA weapon carried with MANA (DA13 — Van's exact steer was REFERENCED BUT NOT SUPPLIED; I designed the ROLE respectfully + deliberately did NOT invent the cultural specifics/name/te reo — flagged for Van's steer + a sensitivity check). CHILDREN HARD RULE (inviolable): kids unkillable by ANY path/toggle/bug — child:true⇒unkillable, harm caps at knock-out, HP floored at 1 via one damage chokepoint + a verify gate (DA11); a tough kid can be KO'd never killed. RICH NPC WORLD: types/families/groups/relationships + reaction propagation + disposition memory. ELEMENTAL MAGIC (DA4 RESOLVED → ship): FIRE/ICE (= existing firefrost tool) + ELECTRIC (+ optional WIND) spell-items, each a COMBAT verb AND a puzzle/nav verb (melt ice/freeze a stream/power a switch/turn a windmill); MP-gated; spells gate OPTIONAL secrets only (never the critical path → no soft-lock); a SPRINKLING of puzzles woven through dungeons (2-3/dungeon)/overworld secrets/quests. CROSS-IMPACT reconciled: no-soft-lock (triple safety: safe-mode + essential-NPC-incapacitate + kids-unkillable; law escapable), the morality axes (all feeds the transformation), the 5 endings (deed-gated, untouched), gating.js (spells gate optional content; firefrost reconciles fire/ice), difficulty (puzzles logic-not-stat; combat spells MP-bounded). [DECISIONS] DA7-DA13 build-time. ART GAPS: transformation layers, standout sprites + the taonga, spell VFX + puzzle props. Docs + PLAN + cross-links; no force-push. **NEXT: Van supplies the DA13 Law-Enforcer steer + confirms DA1/DA8 NPC-harm scope; then per v2 §8.3 author the world nav layer in Tiled (or build the alignment/NPC slice on GH→Peaks).**
## [DONE 2026-06-09] ALIGNMENT SYSTEM design — the full two-sided soul (doc-only; gates GREEN). New docs/ALIGNMENT-SYSTEM.md: go FAR both ways (saviour ↔ monster), the game COMMITS to evil not just punishes it. TWO AXES (existing Karma HUD): MORALITY = how you treat PEOPLE (mercy/help/fair ↑ vs harm/kill/cruelty ↓), PURITY = the SOUL (integrity/sacrifice ↑ vs greed/extortion/taint ↓); precise deltas + the −100..+100 tiers/gates. PHYSICAL TRANSFORMATION (Van's headline) — each axis maps to a BODY FEATURE: morality → angel WINGS (≥+60) / devil HORNS (≤−60); purity → HALO+radiant glow (≥+60) / TAIL+dark miasma (≤−60) — so the 4 corners (Saintly+Radiant=full angel, Cruel+Corrupt=full devil, + the rich mixed 'dark saviour'/'righteous monster') are visible AND NPCs react. ART: an AURA/TINT fallback ships now (no new char art); the full per-facing/per-anim wing/halo/horn/tail LPC layers = [GAP DA3] to source/commission. CONSEQUENCE WEB woven across NPC reactions, SHOP PRICES (re-point off the removed CHA onto morality — good=discount, evil=fear-markup/refusal + black-market, DA2), quest availability/branches/outcomes, world state, gear, and which of the 5 endings open. NPC-HARM fully supported but GATED: non-essential NPCs killable (real evil); essential/critical-path NPCs INCAPACITATED-not-killed + the path REROUTES; a new no-soft-lock VERIFY GATE (DA6) asserts no critical quest is severable by killing an NPC. GOOD-MONSTER beat (good_monster_spared/_slain) = the 'monsters guilt-free EXCEPT this one' exception that proves the rule. MAGIC [DECISION DA4] — recommend RESERVE (keep MP bar+hooks, build later), Van to confirm. CROSS-IMPACT reconciled: gating.js (alignment gates content not areas), the 5 endings (alignment FEEDS them via existing deed/karma gates — transformation = a live ending-path preview, never power-gated), the economy (implements the CHA→morality price re-point), emotional pacing, difficulty (alignment combat-flavour capped, never a power/stealth cheat). [DECISIONS] DA1 NPCs attackable (net-new), DA2 price re-point, DA3 transformation art GAP, DA4 magic reserve/ship, DA5 new deed ids, DA6 no-soft-lock NPC gate — all build-time; design locked. SCOPE: build on the GH→Peaks slice first, then replicate. Doc + PLAN + cross-link; no force-push. **NEXT: Van confirms DA4 (magic) + DA1 (NPC-harm scope); then per v2 §8.3 author the world nav layer in Tiled.**
## [DONE 2026-06-09] MECHANICS RATIFIED (Zelda-style) + dead-content FIX (docs + minimal tested code; 13 gates + 12 suites GREEN). Van chose: growth = tool-unlocks (each a traverse + combat verb) + HEARTS + bounded gear; NO XP, NO STR/DEX/CON/INT/CHA stats, NO skill tree. Difficulty PLACED per-region (never auto-scaling — a returning hero feels powerful; specified the M1-M20 placed curve). LEVEL = the story ACT. MAGIC = MP-gated spell-ITEMS (reserved lean set; the one open CONFIRM = ship-or-defer). MORALITY deepened Fable-style (monsters guilt-free; people/greed/cruelty vs mercy/fairness move Morality+Purity with visible NPC/world/ending consequences) + the TALKING GOOD-MONSTER beat (killing it = a cruelty hit — the exception that proves the rule). D4/D6/D7 ratified. CODE FIX (the real bug): the unearnable `level` gated bounty(L3)/house(L5)/shop(L10) = dead content; added `Economy.actOf(karma)` (Act 2 = time_skip/M7, Act 3 = 4 shards), re-keyed those gates to act → reachable as the story advances; Economy.test rewritten to prove it (act 1 locked → act 2 opens bounty+house → act 3 opens shop). CROSS-IMPACT: reconciles with gating.js (tools = the growth spine) + the 5 endings (stay deed/morality-gated, never power-gated) + the emotional pacing; FLAGGED follow-ups (re-point CHA prices onto morality; retire the Inventory stat/skill stubs; build hearts + good-monster content). docs/MECHANICS-DESIGN.md = RATIFIED v2. Committed; no force-push. **NEXT: Van confirms D2 (ship magic?); then per v2 §8.3 author the world navigation layer in Tiled.**
## [DONE 2026-06-09] MECHANICS DESIGN — design hierarchy level 3b (doc-only; gates GREEN). New docs/MECHANICS-DESIGN.md locks the core loop + progression model so combat content has a spine. CORE LOOP: explore → encounter (fight OR choice) → reward (loot/truth/deed) → grow (tool/gear) → unlock → explore; ~70% exploration-story / ~30% combat, intensity follows the Act 1→3 arc (story-driven action-RPG, combat is PUNCTUATION not grind). PROGRESSION (recommended): "reach (tools) + bounded gear, NO XP grind" — tools = primary (new reach + a combat trick), gear = the only (bounded) vertical axis, level repurposed as the ACT, deep stat/skill trees dropped. Power across M1-M20 = 4 tool-unlocks + 5 gear tiers + boss-trick mastery, tuned by encounter design not farming. COMBAT: keep the rich engine (8 archetypes + 2 tool-trick bosses); design legible one-counter-at-a-time hand-placed encounters; the "unsatisfying slime" = electric-ooze invuln-charge readability (telegraph + ping fix); CORRUPTION-vs-MERCY principle reconciles mandatory combat with the mercy-gated endings (monsters = guilt-free cleansing; morality = people + the god). ECONOMY: gold→gear (bounded) + optional property prestige; no crafting; loot is a trickle. 7 [DECISION]s for Van (esp. D1 level=act [fixes dead content: bounty/house/shop gates are never reachable today since level is never earned], D3 the mercy principle, D4 re-space the Marsh's all-7-archetypes-at-once into a teaching curve). Reconciles with gating.js + karma/endings (endings stay deed-gated, never power-gated). Doc + this line; no force-push. **NEXT: Van rules on the 7 [DECISION]s; then per v2 §8.3 author the world navigation layer (Tiled, real-body-walk proven).**
## [DONE 2026-06-09] WORLD-CORRECTNESS bugfix — 3 confirmed bugs (13 gates + 11 suites GREEN; 0 errors; commits 63bd5051/e4b3aba3/5bb49685). **B1 DASH-TUNNEL** (real, NOT a 4fps artifact — prior explanation was WRONG): swept collision `_sweptDodgeVel` casts the feet-box along the dash path in 4px steps + clamps at the first solid + ends the dodge → real-input dash U/D/L/R into boulder/tree-trunk/hedge/building all BLOCKED. **B2 PEAKS PATH walkable** (honest: prior "walks" claims were INVALID — teleported INTO the Peaks; a real cell-walk stuck at the fork because last session's class-solidity flipped the decorative scatter (placed solid:false) to solid + fragmented the corridor — walk couldn't thread it, dash tunnelled). Fix: isSolid() lets an explicit solid:false WIN (class=default only); verified real-input WALK to BOTH Peaks forks, no dashing. **B3 WHITE SQUARES = signs**: prop_sign readable:true + text on every sign + press-E READ (verified GH notice board). **NEXT: Van real-speed confirm (dash-vs-rock + walk both forks); decide which standalone scatter rocks should block; ⚑ sign sprite is a blank-white placeholder (art GAP).**
## [DONE 2026-06-09] INTERACTION-CLASS TABLE — consistent rules per asset type (Van: bushes/cut/dash inconsistent; 13 gates + 11 suites GREEN; 0 errors). SSOT src/data/interactionClasses.js: every solid/interactable asset declares solid/cuttable/throwable/pushable/searchable, applied uniformly in every region (no per-object/per-region ad-hoc). CUT is now CLASS-driven via the SWORD SWING (any bush cuttable in EVERY region — verified GH 471 + Peaks 184 cut identically). SOLIDITY from the class (isSolid(key,placementSolid); bushes honour small-vs-big placement flag). SAFE-ZONE RULE: Peaks town = SAFE HUB (combat.safe ring) — sword swings + cuts foliage but NO combat damage; combat OUTSIDE the town. DASH-OVER: dodge blocked-check cancels the dash push into a solid it touches (real-fps fix); the 4fps Playwright throttle still shows creep (artifact) → flag for Van's real-60fps confirm. GATE: verify FAILS if a placed asset lacks a class (no undefined behaviour). FLAGGED: prop_bush dual-role + big_bush/reed/stump are GAP art; prop_sign set non-solid; building porch-base outline NOT done (the base band = full opaque width; a porch holds you out — needs a per-building base shape, eyes-on). Committed; no force-push. **NEXT: Van confirms dash-vs-solid at real fps + the hedge-cut/sign calls; then the GAP foliage art (small/big bush, reed, stump) + the porch base shape.**
## [DONE 2026-06-09] DEPTH-SORT fix — hero rendered UNDER buildings when standing in FRONT (Van: collision correct but y-sort wrong; 13 gates + 9 suites GREEN; 0 errors; 61fps). Diagnosed (depths logged): depth-sort tracked ACTORS (hero+NPCs) at offset 18, but their feet (collision base = body.offset.y+body.height) = 30, so the sort key was ~12px above the feet. In front of the forge: hero feet 8120 (below base 8112=in front) but sortKey 8108 < building 8112 → drew behind. Props already sort by their opaque base, so only actors diverged. FIX: track hero+NPCs by body.offset.y+body.height = the feet line = the SAME ground anchor the pixel-truth collider + props use. Re-tested (depths + eyes-on, stamp 88082d7e in-frame): building FRONT 8120>8112 on top / BACK 8064<8112 occluded (walk-behind kept); tree FRONT on top / BEHIND occluded by canopy; rock front on top; NPCs sort by feet too. Committed (fix; proof+docs); no force-push. **NEXT: Van hard-refreshes (stamp 88082d7e), confirms hero draws on top in front / behind when walking up behind; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] VERSION STAMP + collision re-verify on a confirmed-current build (Van: still broken after refresh, but couldn't confirm he's on latest code; 13 gates + 9 suites GREEN; 0 errors; 61fps). PART 1: always-visible on-screen build stamp (bottom-left) via a Vite transformIndexHtml plugin that re-runs `git rev-parse` on EVERY page load → always reflects the served code (defeats browser cache). Stamp = `build e1a1b361 · 2026-06-08 23:00` (Van matches this hash to confirm latest). PART 2: re-verified collision WITH the stamp in-frame on e1a1b361 — pixel-truth test CLIPPED-IN=0 (GH 618 + Peaks 321 objects); drives: house FRONT base 0px / BACK walk-behind 80px / LEFT 0px / RIGHT clean; rock edge 0px; mountain keep-road→keep reached + flank dash blocked. Honest note: trees stop ~10px short of the trunk (small approach GAP, not a clip — cosmetic). VERDICT: collision CORRECT on current build; if Van still clips, his browser has a STALE bundle → hard-refresh + check stamp=e1a1b361. PART 3: dev server (npm run dev :5273) serves live working-tree source; only staleness = browser cache → hard-refresh + confirm stamp. Committed per-group (stamp; re-verify proof); no force-push. **NEXT: Van hard-refreshes, confirms stamp e1a1b361, re-judges collision feel; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] PERSPECTIVE-AWARE VERTICAL collision — base ground band for buildings (Van: L/R correct, up/down clips/odd; 13 gates + 9 suites GREEN; 0 errors; 61fps). Diagnosed: the full-opaque-silhouette collider was right horizontally but vertically blocked the hero at the ROOF TOP from behind (couldn't walk behind a building; diag2-back-clip-forge = perched on roof). FIX: solidBox now perspective-aware — horizontal = opaque silhouette WIDTH (preserves the L/R fix); vertical = GROUND FOOTPRINT anchored to the opaque base, by class: trees→trunk band; BUILDINGS→BASE GROUND BAND (depth ≈ opaque-width×0.5, ≥24px → stop at front base + walk behind the roof, y-sorted); compact rocks→full silhouette (unchanged). Forge re-drive: FRONT base 0px, BACK walks 80px behind to the band top, L/R at opaque sides. Boulder front/back unchanged (0px). Pixel-truth test updated to assert the GROUND-BASE truth (buildings: BACK at band top, L/R at band level; full-height-front FAILS); re-ran 4 faces GH+Peaks → CLIPPED-IN=0. Gate #10: buildings must be a base band (full-height FAILS), every solid's width = opaque silhouette. Keep-road + GH still walkable. Committed; no force-push. **NEXT: Van re-tests front/back/side collision feel; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] PIXEL-TRUTH COLLIDERS + non-circular test (implement the diagnosed fix; 13 gates + 9 suites GREEN; 0 errors; 61fps). Replaced frame-based solidBox with OPAQUE-PIXEL derivation: scripts/build_opaque_bounds.mjs (minimal zlib PNG decoder) precomputes src/data/opaqueBounds.js (SSOT read by scene + gate). Rule: trees→opaque trunk band (walk behind canopy); everything else→FULL opaque silhouette (rocks solid top-to-bottom; buildings solid to the visible walls). Collider + y-sort anchor to the opaque bounds, not the frame → no padding overhang, no clip-in. Fixed a real flipX bug (off-centre pine trunks now mirror their collider). NON-CIRCULAR TEST (scripts/collision-pixeltruth-test.browser.js): ground truth = texture opaque pixels (canvas, independent of collider); drives the hero into every solid 4 faces, asserts the body edge stops within ±8px of the OPAQUE edge. RESULT: GH+Peaks → 939 objects, 2,556 cleanly-driven face-tests, CLIPPED-IN=0 (residual = a few dense-scatter stop-shorts, all GAP never CLIP; colliders verified exact by probe). Eyes-on: overlay shows collider now fills the whole rock + trunk-only on trees; hero stops exactly at a boulder's visible top (0px vs 24px buried before). Gate #10 now asserts pixel-truth (collider inside opaque silhouette, bottom at visible base; frame-padding FAILS). Keep-road + GH still walkable. Removed the old circular auto-test. Committed per-group; no force-push. **NEXT: Van re-tests collision feel; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] COLLISION DIAGNOSIS — the auto-test passes but the game visibly clips (the test is CIRCULAR); diagnosis-only, NO fix (13 gates GREEN; 0 errors; no collision code changed). Proved the auto-test checks the hero against the COLLIDER's own geometry (the collider IS where the hero stops → always passes) instead of the sprite pixels. VISUAL PROOF (collider RED vs sprite visible-bounds CYAN overlays + drove the hero in): diag-clip-rock = hero buried to the waist in a boulder; diag-clip-house = hero walked 58px into the forge + occluded behind it; diag-overlay-forge/-town = building/rock upper mass above the collider. QUANTIFIED: boulder collider top 24px below visible top (top ~45% walk-into-able); forge 58px; eliza houses (house_a/b) + fountain have transparent frame padding (24px sides / 23px bottom) so the frame-based collider floats in empty space. ROOT CAUSE: solidBox derives the collider from the PNG frame × a fixed fraction (lower-55%), NOT the opaque pixels nor the true solid extent → padding≠wall, "55%" wrong for compact rocks (should be ~100%), varies per house by each PNG's padding. Player body is correct (feet-box). PROPOSED FIX (next session): pixel-truth collider+test — derive ground truth from each texture's opaque-alpha bounds (probe built this session), anchor the collider to the silhouette (compact=full opaque box; walk-behind=opaque ground band), assert the hero stops within ±~8px of the OPAQUE edge (not the collider). Full writeup: docs/COLLISION-DIAGNOSIS.md. Committed diagnosis (doc + 5 debug screenshots); no force-push. **NEXT: implement the pixel-truth collider + non-circular test; re-verify every solid in GH+Peaks stops at its visible edge.**
## [DONE 2026-06-08] COLLISION OVERHAUL — ONE rule + auto-test EVERY object from 4 directions (Van: "houses off to different degrees" = per-object guessing; still clips + can't climb path; 13 gates + 9 suites GREEN; 0 errors; 61fps). STOPPED hand-tuning per object. ONE rule (solidBox in assets.js) derives every solid's collider from its sprite by CLASS: CANOPY (trees→trunk, walk behind canopy), MASS (rocks+buildings→~full-width × lower-55%, base-anchored: stop AT the front, walk behind the top), SMALL (barrel/sign/fence→base footprint). Applied UNIFORMLY in _buildRegion (collider + y-sort); removed the per-building hand-tuned footprints. AUTO-TEST (scripts/collision-autotest.browser.js, manually-stepped real physics) drove the hero into EVERY solid from all 4 faces: GH 2027 obj/6628 tests + Peaks 1454 obj/5258 tests = 3,481 objects / 11,886 directional tests / ZERO pass-throughs. MOUNTAIN: real DASH-test at W/E perimeter + cross-wall flank = all blocked, no tunnel; BOTH foothill forks (QUARRY NW + KEEP NE) real-input-walk GH→Peaks→keep (Shard-I gate opens); keep-road mouth→Cinder Keep clean. Gate #10 now asserts the rule (MASS collider ≥60%×≥40% of frame). Eyes-on screenshots: house/rock/flank clean stop + reached keep (walk-behind). WORKING ROUTE UP: GH north trailhead → hold UP up the foothill trunk to the fork → UP+LEFT (quarry, NW) or UP+RIGHT (keep, NE) — both reach the Peaks town → UP up the central dirt keep-road through the cleft pass → Cinder Keep. Committed per-group (collision rule + gate + autotest + screenshots); no force-push. **NEXT: Van re-tests collision feel + both forks; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] PEAKS COLLISION — PROPERLY fixed + proven with a REAL-INPUT test (Van: still walk into house fronts + dash up cliff sides + can't find the way up; 13 gates + 9 suites GREEN; 0 errors; 61fps). Built a real-input test helper (window.__drive: holds the ACTUAL movement keys + chains real dashes, awaits frames) — used for EVERY assertion (prior fixes used body.setVelocity = zeroed each frame = fake). THREE fixes: (1) HOUSE FRONTS — building colliders were thin BASE strips → front/door above the base walkable. FIX = enlarged forge/house footprints to the FULL mass (bottom-aligned, y-sort unchanged); real-input drove the hero into all N/S/E/W faces = blocked. (2) CLIFF FLANKS — real DASH (chained dodges) into W/N perimeter + up the W flank = all BLOCKED, no tunnel (cross-wall + 2-tile perimeter hold). (3) NO PATH UP — town hall + fountain sat ON the keep-road corridor (x29-31) → moved them to the flanks; real-input walked the hero from the S mouth straight N up the keep-road through the cleft pass to Cinder Keep ✅. GATE #10 extended: a building footprint covering <70%×<50% of its frame now FAILS. THE ROUTE UP (for Van): GH → north onto the foothill trail → up the lane → ROCKFALL opens with Shard I → into the Peaks town → straight N up the central dirt keep-road through the cliff gap (cleft pass) → Cinder Keep. Committed per-group (collision fix); no force-push. **NEXT: Van re-tests collision + the route; then close the slice DoD (music beds + stone-town).**
## [DONE 2026-06-08] PEAKS COLLISION BUGFIX + DOWNLOADS INTAKE-TRIAGE (Playwright eyes-on; 13 gates + 9 suites GREEN; 0 console errors). PART 1 — Van: walk through houses + dash up the mountain sides. Live diagnosis (my earlier "blocked" tests were INVALID — body.setVelocity is zeroed each frame by the input loop; re-tested by holding the real keys) found THREE holes: (a) solid props had DYNAMIC bodies in the STATIC `solids` group → never entered the static collision tree → player walked through EVERY house/boulder/fountain (only cliff rects blocked); FIX = each solid prop gets a STATIC collider RECT at its footprint (the cliff-wall pattern) — fixed GH/Marsh too; (b) the riven-cleft shoulders left the W/E flanks of the upper bowl OPEN → bypass the path; FIX = full cleft CROSS-WALL with a pass-only gap (x27-33) = the keep-road is the ONLY way up; (c) Peaks pines were solid:false; FIX = solid + widened the road/plaza skips so obstacles don't block the path. Extended verify gate #10: a SETTLEMENT (non-route) region must have solid:true mass props (route regions exempt). Re-tested LIVE (key-driven): pass open to the keep ✅, W flank blocked ✅, town hall blocks ✅, GH still walkable ✅. PART 2 — triaged Van's 100+-file itch.io download dump (docs/DOWNLOADS-INTAKE.md): read ACTUAL licences, did NOT trust the "free" tag. KEY: our repo is PUBLIC → "no-redistribute" licences are UNUSABLE (commit = redistribution). Verdicts: SAFE=1 (Icons_Essential, CC-BY 4.0 — not a current gap, catalogued); UNSAFE/REJECT = JDSherbert (LICENSE.pdf: commercial-ok but "no redistribute, all rights reserved" → breach), CraftPix (~30+, banned), Free Inventory (ElvGames, no-resell), Dafydd-Harvey ambient (informal); OFF-FORMAT = ~45 PBR/material texture zips (3D, fail the 32px lock); UNKNOWN = ~60+ music tracks + ~15 pixel zips (per-pack licence read needed). IMPORTED NOTHING (nothing SAFE-and-conforming fills a current gap; bulk-importing no-redistribute/off-format art would breach licences + the lock). Recommendation: Van triages 2-3 wanted packs; the real music gap = the Tallbeard CC0 bundle (still pending). Committed per-group (collision fix; intake doc); no force-push. **NEXT: Van re-tests Peaks collision + names any music/packs to vet; then close the slice DoD.**
## [DONE 2026-06-08] ASSET GOVERNANCE — attribution + support/koha POLICY + CREDITS.md backfill (doc-only; 13 gates GREEN). Made it POLICY (ASSET-LEDGER.md header) that no asset is "used" (committed under public/ or baked) without: artist name + source URL + licence (+ its attribution string) + a SUPPORT/contact link where one exists (Patreon/Ko-fi/donate, else the creator's canonical OpenGameArt profile) — so a credits screen AND a koha to creators are both possible. Kept the machine `ledger` block as the 5-col gate map (unchanged); the AUTHORITATIVE per-creator record lives in new **docs/CREDITS.md**. AUDIT: all 8 public/ ledger entries already named artists (ElizaWy, Sharm, bluecarrot16, Daneeklu, Jetrel, Zabin, Hyptosis, Redshrike, Kenney, SketchMan3, Spring Spring, CharlesGabriel, bagzie, sanderfrenken, …) → 0 missing names; BACKFILLED support links for all (VERIFIED ⭐: ElizaWy = patreon.com/DeathsDarling, Kenney = kenney.nl/donate + patreon.com/kenney; the rest = canonical OGA user profiles, with "(verify handle)" flags where the exact handle wasn't individually confirmed — honest, not invented). CC0 creators (Kenney/SketchMan3/Spring Spring + bird recordists isaiah658/syncopika/pauliuw) credited anyway with links. None of the NAMED creators were undeterminable; the LPC atlas's long sub-contributor tail is credited as a group + points to the bundled Credits.txt rolls. CREDITS.md ends with a KOHA short-list (the verified direct support links). ⚑ Self-caught + fixed a gate trap: a literal triple-backtick-"ledger" in the new policy prose broke the licence-gate parser (split on '```ledger') → reworded to `ledger`; gate GREEN again. Committed ledger + CREDITS.md + this line; no force-push. **NEXT: Van's slice feel-verdict + the music beds; thereafter credit any newly-shipped asset in CREDITS.md per the policy.**
## [DONE 2026-06-08] GH→PEAKS VERTICAL SLICE — composition + interaction + audio pass (live BEFORE/AFTER; 13 gates + 9 suites GREEN; 0 console errors; perf 61fps/max21.7ms). HEADLINE (Van's #1 note: monotonous tree-corridor): reworked vegetation for VARIETY+COMPOSITION — vary tree size (per-prop scale), species (oak/pine), spacing (off-grid jitter + random H-flip), layered undergrowth, low-silhouette gaps, CLUSTERED woodland with CLEARINGS (clump-centres per chunk); added scale/flip support to the streamed-chunk + region prop renderers (worldmap.js chunkContent + _buildFoothillProps; OverworldScene _spawn + _buildRegion). BEFORE=identical-pine hedge → AFTER=varied composed forest path (R-VIS path still parses). INTERACTION INTEGRITY (tested live): object PERMANENCE — added a `looted` guard + isOpened check to the chest (closed a same-session re-loot hole); verified open=+45g, re-press=no re-grant, region-cycle=no respawn; COLLISION — drove N into a Peaks cliff → blocked at 2px (881 solids). AUDIO (minimal layer): region ambient beds swap on region change (amb_wind Peaks / amb_birds green, OGA CC0) + Kenney CC0 UI/pickup SFX on chest-open + grant; music = hook pending Van. GAME-FEEL: the 2D item-get flourish (glow+scale+fade+confirm-sting) on chest + the Cinder Keep grapple grant (tool 2D-fallback). STONE-TOWN C4 TRIAL: side-by-side vs the ElizaWy hero → Allacrost stone set REJECTED (C4 fail, higher-detail painterly hand clashes with flat ElizaWy); slice keeps cohesive cool-tinted ElizaWy houses (+ eliza-structure stone kit for future). 9-FACTOR DoD self-check: F1 visual ✅(headline landed) · F2 audio ✅ambient+SFX / ❌music-pending-Van · F3 terrain-cohesion ✅ · F4 game-feel ✅ · F5 readability ✅ · F6 story ✅ · F7 quest-placement ✅ · F8 gating ✅ · F9 density ✅. DEFERRED (honest): the 2 music beds (Van's itch download), a true stone-town set or eliza-kit assembly, advanced interactables (cut/push/search = separate engine features per INTERACTABLES-DESIGN). Committed per-group; no force-push. **NEXT: Van's feel-verdict on the slice; then music beds + (stone-town assembly / advanced interactables) to close the full region-completion DoD, then replicate the bar to Coast.**
## [DONE 2026-06-08] SLICE-PREP — IMPORTED the slice assets autonomously (fetch+vet+ledger; 13 gates GREEN incl. the licence-ledger gate on new entries). Network was reachable, so Claude fetched+vetted+imported the certain assets and flagged only the genuinely-gated ones. IMPORTED to public/ (CC0, licence-evidenced, ledgered): 4 Kenney Interface UI/feedback SFX (public/audio/ui/ ui_confirm/select/deny/pickup.wav + bundled CC0 LICENSE — via the Kenney GitHub mirror) + 2 OGA ambient loops (public/audio/ambient/ wind_ambient.ogg [SketchMan3 CC0, Peaks wind] + birds_ambient.ogg [Spring Spring CC0, GH birdsong]). Ledger: added public/audio/ambient/ (OGA CC0) + broadened public/audio/ to Kenney RPG+Interface; verify licence gate GREEN (167 files/8 entries). STAGED (vetted, NOT public-baked — honest C4 call): the Stone Home Exterior Tileset (Roots/Jetrel, CC-BY-SA 3.0, 32px genuine stone walls/slate-roofs/stone-arch-doors) → asset-library/2d/buildings/allacrost-stone/ + CREDITS; it is "LPC-compatible not pure-LPC" (warmer rendered hand) so baking it next to the ElizaWy hero risks a C4 style clash — deferred to the wire-time in-scene C4 trial (cool-tint for cold Peaks; fall back to the in-repo eliza-structure stone kit if it clashes). MANUAL-DOWNLOAD for Van (itch.io is interaction-gated, no direct link): the 2 mood-music beds — Tallbeard Music Loop Bundle (CC0) pastoral-GH + wind-drone-Peaks (exact URL+drop-dir in SLICE-ASSETS.md); the slice is NOT blocked on music (the imported wind/birds ambient gives a real soundscape start). No ffmpeg in-env → SFX imported as .wav (Phaser-valid). Nothing rejected outright. Committed per-group (audio import; staged stone + docs); no force-push. **NEXT: build the GH→Peaks slice — wire the UI/ambient audio + the tool 2D-fallback, run the stone-set C4 trial (bake-or-fallback), and Van downloads the 2 music beds.**
## [DONE 2026-06-08] SLICE-PREP — sourced the GH→Peaks slice's HIGH-confidence assets + scoped living-world (doc-only; npm test + verify GREEN, no code/art/ledger touched). PRINCIPLE: source ONLY to fill confidence-ranked gaps, conforming to the locked 32px/64px LPC standard (reject non-conforming). New **docs/SLICE-ASSETS.md** with licence-evidenced candidates: (1) STONE-TOWN exterior ⭐ **Stone Home Exterior Tileset** (Roots/Jetrel, CC-BY-SA 3.0, 32px, stone walls/roofs/doors/windows — replaces the brick-house stand-ins) + [LPC] Thatched-roof Cottage (CC-BY-SA/GPL) + [LPC] Medieval Village Decorations (CC-BY-SA, the dressing + the outdoor-bench gap); (2) MOOD MUSIC beds ⭐ Tallbeard Music Loop Bundle (CC0, pick 2 — pastoral GH + wind-drone Peaks) + Not Jam (CC0) + OGA CC0 Music; (3) AMBIENT/FEEDBACK SFX — Kenney Interface Sounds (CC0, UI/pickup) + OGA CC0 chest-open + wind/birds ambient loops (CC0). All AI-safe (CC0/CC-BY-SA/GPL, no anti-AI), conform to the lock, ledgered per-asset at import. Get-it steps per candidate (fetch→stage→vet→bake→ledger→wire→verify). SCOPED the living-world life gaps (rank+plan, don't source): chicken (LPC Chicken Rework — canon Henrietta), cat/dog ([LPC] Cats and Dogs), farm animals (LPC farm animals), crop growth-stages ([LPC] Crops — exact 5-frame 50-crop match), outdoor bench (Medieval Village Decorations) — **ALL HIGH-confidence, none LOW/risky** → a dedicated living-world session is low-risk. Copyleft note: several CC-BY-SA/GPL → keep credits on a commercial ship (ledger policy). RESULT: every GH→Peaks slice gap is now HAVE or HIGH-confidence with a named licence-evidenced candidate; no [LOW]/risky asset remains for the slice (tool-held uses the 2D fallback). In-repo confirmed: no animals/crops/bench, stone-town = brick stand-ins, audio = 3 SFX. Committed the doc + this line; no force-push. **NEXT: build the GH→Peaks vertical slice to the region-completion DoD — import the stone-town set + 2 music beds + the few SFX (per-asset, ledgered), wire the tool fallback + a minimal audio/ambient layer, then Van's feel-verdict.**
## [DONE 2026-06-08] SLICE-PREP — resolved the GH→Peaks slice's one [LOW]-confidence asset (tool held-item sprites) by DECISION (doc-only; npm test + verify GREEN, no code touched). CHECK: the live ElizaWy rig has only idle/walk/attack — NO thrust/tool/cast/held-item animation rows; the staged LPC working set's `accessories/tools` = only mining/farming overlays (smash/thrust) + `weapons` = swords/polearms/bows (NO grapple/hookshot/lantern held item). SOURCE: OGA/LPC search confirms NO rigged grappling-hook/hookshot held-item exists licence-clean (only related push-carry/grab/extended-weapon sets) → a conforming rigged held-grapple = custom art + new rig states = out of slice scope. VERDICT: the rigged held-tool stays [LOW]/GAP; the "item-get" beat done in 2D is HAVE/HIGH. DECISION = **USE THE FALLBACK**: represent the tool with no new rig layer — GRANT at Cinder Keep = existing idle + banner + item-get STING + record tool_grapple/shard_2/save (already built) PLUS a 2D overhead flourish (a 32px item icon tweened up above the hero + ow_orb glow/flash; icon = lantern HAVE / grapple-hook HIGH-trivial crop / orb-glow floor); USE (grapple a ledge) = a drawn Graphics line+hook VFX + a dash/tween gated by the deed, NOT a rigged swing. Conforms to the locked 32px/64px LPC standard, 100% in-hand. Deferred: the fully-rigged held-tool animation (future polish, [LOW]). RESULT: no [LOW] asset blocks the GH→Peaks slice anymore — only the stone-town set [HIGH] + standing up the audio system remain (both known/HIGH). Wrote the verdict+decision into PEAKS-ART-CANDIDATES.md ("Slice tool-representation decision"); marked EXCELLENCE-FRAMEWORK §6 gap RESOLVED-via-fallback. Committed docs + this line; no force-push. **NEXT: build the GH→Peaks vertical slice to the region-completion DoD (tool fallback + source stone-town [HIGH] + stand up audio + Van's feel-verdict).**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Layer 2 COMPLETION: the EXCELLENCE + CROSS-VERIFICATION framework (doc-only; npm test + verify GREEN, no code touched). New **docs/EXCELLENCE-FRAMEWORK.md** — the governing quality structure: every FACTOR gets its own DoD + excellence bar, cross-checked against every other. (1) ART DIRECTION per region (theme/emotional-tone/palette+complementary-accent/readability rule R-VIS): Greenhollow warm-peaceful/green+hearth-orange · Marsh eerie-desaturated/teal+lantern-amber · Peaks stark-cold/grey+ember-rust · Coast bittersweet-windy/sea+lighthouse-gold · Emberwood tense-uncanny/the fire-orange↔frost-blue opposition · Spire awe-dread/ice+shard-glow. (2) AUDIO PLAN per region (music mood mapped to the art tone + ambient bed + leitmotif; the tension-state system + scripted stings + narrator [local CC0 TTS Piper/Kokoro; BANNED XTTS/F5/Fish weights] + A-SFX "no action without feedback") — audio is part of region-done; ⚑ flagged that only 3 SFX exist today (whole audio system = GAP). (3) DoD MATRIX — 9 factors (visual/audio/terrain-cohesion C1–C5/game-feel/readability/story/quest-placement/gating/density), each with its DoD checklist + excellence bar + enforcer (GATE vs [EYE]). (4) CROSS-VERIFICATION MATRIX (the key structure) — 10 factor×factor checks (audio-mood×visual-tone, visual-theme×story-beat, quest-placement×gating, readability×density, terrain-mood×emotional-tone, game-feel×audio…) run 3× (design→build→region-done), tagged GATE vs [EYE]. (5) EMOTIONAL PACING — the M1–M20 mood rhythm (warm→shock→mystery→struggle→bittersweet→tragic→awe; no two adjacent regions share a dominant mood = anti-monotony check). (6) CONFIDENCE-RANKED GAPS (updated ASSET-MANIFEST): HAVE/HIGH/LOW per gap + THE RULE — no region builds until its [LOW] gaps are in hand (5 LOW: shipwreck, lighthouse, volcano-cone, spire-tower, tool-held-item sprites). (7) REGION COMPLETION DoD — the single 13-point checklist (all factor-DoDs + all cross-checks + Van's feel-verdict) + committed **VERTICAL-SLICE-FIRST: GH→Foothill→Peaks to this full DoD before Coast/Emberwood/Spire**. CONTRADICTIONS FLAGGED: WORLD-BIBLE "Marsh as Region 1" superseded (slice-first GH→Peaks); Peaks tool "strength"→canonical tool_grapple; Coast tool=hookshot (gating.js canonical); audio absent yet part of done (biggest cross-cutting gap); C1/C2 [EYE]-enforced until generalised to verify gates. Committed the doc + ASSET-MANIFEST confidence update + this line; no force-push. **NEXT: finish the GH→Peaks vertical slice to the region-completion DoD — source the stone-town set [HIGH] + tool-held-item sprites [LOW] + stand up the audio system, then Van's feel-verdict.**
## [DONE 2026-06-08] WORLD-BUILD — PEAKS RE-FIX (C1+C2 cohesion, WIRE-not-source) + ENTRANCE-CONTRACT connection model (live-verified BEFORE/AFTER; all 13 verify gates + 9 suites GREEN; 0 console errors; perf IMPROVED). PART A — the Peaks read improvised: ground = stone-TINTED green grass (C2 fail), cliffs = scattered single props w/ green showing (C1 fail). FIXED by wiring in-repo assets: (C2) added rock(Rock_Gray)/snow(Snow_1)/stone(Stone_Tan)/ash/lava to the terrain autotiler (build_terrain_autotile.mjs OVERLAYS → regenerated terrainTiles.js; all already in the shipped terrain-v7 atlas) + laid a rock patch under the whole Peaks bowl + snow cap + stone town floor → ground is now real GREY ROCK, zero tinted grass. (C1) extracted a seamless cliff FACE + rock-LIP top from eliza cliff_summer.png (public/art/terrain/cliff_wall.png+cliff_top.png, OGA-BY, ledgered) + new OverworldScene._buildRegionCliffs RenderTexture rendering cliff MASSES (R.cliffWalls) as ONE continuous face → continuous cliffs (cleft + W/E/N perimeter), 0 orphans/gaps. + Ice tarn, denser scree/pine to the wild density band. LIVE (1280×720): green→grey-rock ✓, scattered→continuous cliffs ✓, snow cap ✓, green-foothill→stone-Peaks seam ✓; PERF max-frame 36.7→20.0ms (one RenderTexture ≪ ~640 props), 60-61fps, 0 errors. ⚑ FLAGGED (not faked): terraced-stone-town EXTERIOR set (brick-house stand-ins remain) → LPC hunt. PART B — formalized the modular CONNECTION MODEL as data (src/data/entrances.js: each region's entrances = {pos, connects-to, gate} for GH/Belt/Marsh/Foothill/Peaks + reserved Coast/Emberwood/Spire stubs) + verify gate #13 ENTRANCE-COHERENCE (two-sided handshake + gate matches gating.js + reachability from Greenhollow); demoed it catches an injected mismatch, reverted. Region INTERIORS stay free to rebuild; only ENTRANCES are the contract. GH/Belt/Marsh + the Peaks entrance untouched/coherent. Committed per-group; no force-push. **NEXT: source the stone-town building set, then Tidewreck Coast (E) — art-hunt dock/wreck/lighthouse first.**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Step 2b: the story/quest/gate OVERLAY (doc-only; npm test + verify GREEN, no code touched). New **docs/STORY-MAP-OVERLAY.md** — places the EXISTING M1–M20 + all side quests + cameos + PH onto the 2a physical MASTER-MAP (extracted verbatim from src/data/quests/* + constants/{deeds,flags,endings} + gating.js; PLACES, does not redesign). (1) CRITICAL PATH: M1–M20 each mapped to region·spot(world-tile)·giver·gate-in·earns — the walked route GH→Marsh[lantern,shard_1]→Peaks[grapple,shard_2]→Coast[hookshot,shard_3]→Emberwood[firefrost,shard_4]→Spire[shard_5]. (2) SIDE+CAMEOS placed per region with giver/spot/trigger (SG/SA/SP/ST/SE, PH1–6, CAM1–7, EDG1/2, GAG1, NIGHT1, the roaming Wanderer) — distributed to route the player across the whole map; the cross-region Pem hunt (SA1/SP1/ST8/SE1→SG2) mapped. (3) GATE SET-PIECES: each gating.js gate → its physical enforcement + concrete tease→payoff (boarded-cave→lantern, belt-island→hookshot, Peaks rockfall→shard_1, river-gorge→grapple, chasm→hookshot, gated-ascent→4shards+firefrost, + in-region tool loops SA3/SP4/ST2/SE3). (4) NPC DISTRIBUTION verified: givers in safe hubs/roads, never among monsters (sanctioned exceptions tagged: SP5 stranger town-edge, ST3 keeper@lighthouse, bounty/dungeon = encounters not givers). (5) JOURNEY REACHABILITY VALIDATION = the key check: walked start→end, every region's entry key earned in the PRIOR region → **NO SOFT-LOCKS** (agrees with gating.js); linear first-clear, non-linear revisit. FLAGGED (not overridden): M14/ST2 tide-time = [ENGINE]; CAM5 rare-spawn = engine; SG2 needs waystone backtrack; unbuilt-region givers get NPC positions at build time; Peaks boss-flow deferred. Committed the doc + this line; no force-push. **NEXT: the integrated master design is foundation-complete (locked standard + asset manifest + physical master map + story overlay) — ready for the Peaks RE-FIX build (wire rock-ground C2 + cliff-autotiler C1) or the next region (Coast, art-hunt first).**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Step 2a: the detailed physical MASTER MAP (doc-only; npm test + verify GREEN, no code touched). New **docs/MASTER-MAP.md** — the WORLD-BLUEPRINT fleshed from coordinate-blocks to BUILD-READY content per region (so build sessions PLACE, never improvise — the missing layer that made Peaks come out disjointed). Covers all 7 regions (Greenhollow, West Belt, Marsh, Peaks [reconcile], Tidewreck Coast, Emberwood, Hollow Spire) + the world skeleton (watershed river spine N→E, the 5 connecting route bands, the gating DAG, the global Spire vista). PER REGION: (1) PATH layout — channelled forking routes with world-tile anchors + what terrain channels them; (2) TERRAIN masses — every biome ground mapped to a specific terrain-v7 autotile (Rock_Gray/Stone_Tan/Snow/Sand/Water/Lava/Ice — all shipped, WIRE-not-source) + eliza cliff autotile; (3) LANDMARKS; (4) SECRETS + TEASES with their gate key/payoff; (5) ENCOUNTER zones + safe hubs (contextual, no monsters in settlements); (6) DENSITY plan vs the QUALITY-SPEC bands (town 15-25 / wild 6-12 / lull ≥3); (7) per-region BILL-OF-MATERIALS = every element → [HAVE] / [HAVE⚙ wire] / [GAP source]. Consolidated GAP list by build-order: Peaks-refix = terraced-stone-town exterior (+ wire rock/cliff); Coast = lighthouse/shipwreck/dock; Emberwood = volcano-cone/burnt-trees; Spire = tower/altar; cross-cutting = animals/crops/bench/tool-held-items; engine-deferred = tide/lava-hazard/climb/overworld-boss. CONTRADICTIONS FLAGGED (not overridden): Peaks built 60×60 inside reserved 75×72 + its C1/C2 cohesion defects (ground=tinted grass, cliffs=scattered props) — §4 is the fix spec; stale Crossroads coords (blueprint "x200 y153" A-coords → corrected x330,y298 live B); Marsh ground=tint not terrain set (C2 reconcile); living-world assets absent. PHYSICAL only — story/quest/gate placement = Layer 2b (next). Committed the doc + this line; no force-push. **NEXT (Step 2b): the story/quest/gate OVERLAY on this map (M1–M20 + side quests + gate set-pieces placed per region).**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Step 1 ADDENDUM: LOCKED the single canonical asset standard (doc-only; npm test + verify GREEN, NO code touched). Van's decision: the ENTIRE game uses ONE standard — **LPC · 32px tiles · 64px character frames · LPC palette · scale ratio 1:2.7:4.2** — for every region/screen/asset, current and future. No tiers, no mixing. Non-conforming assets are REJECTED (recoloured/resized to conform, or not used) — the game is never adapted to the asset. The ONLY sanctioned exception = a deliberately "out-of-place character" gag, explicitly tagged `outOfPlace:true` so it never counts as a cohesion violation. **This SUPERSEDES + RETIRES the prior same-day "growth-ready tier registry" addendum** (the reserved T1/T2 slots are withdrawn — whole-game continuity wins; reconciled explicitly, not silently). QUALITY-SPEC.md: rewrote §0 as THE LOCK (locked-standard table + the law: conform-or-reject, no mixing, the one tagged exception); new §0.3 NEW-ASSET CONFORMANCE GATE (every sourcing session checks grid/style/palette/scale/licence BEFORE import → conform or reject); C1–C5 cohesion now enforce against the ONE standard (not per-context); R1/R2/C4 de-tiered; §7 reframed — the hardcoded TILE=32/FRAME=64 are now CORRECT/intentional (the lock made physical, do NOT make configurable), the only code item is deduping the duplicate TILE (assets.js↔worldmap.js) to one SSOT. ASSET-MANIFEST.md: replaced the TIER field with a **CONFORMS field** (yes/no/exception); tagged every group `[conforms: …]`; flagged the current NON-CONFORMING assets = legacy `public/art/kenney/` (16px off-grid) + `public/art/lpc/` (superseded universal sheets) = `conforms:no`, never on a map; reserved the `exception` slot for the gag char. STYLE-GUIDE.md: added a LOCK banner (governed by QUALITY-SPEC, authoritative on conflict) + corrected its stale ENVELOP/0.85 scale line to the canonical RESIZE+1.25. Committed 3 docs + this line; no force-push. **NEXT (Step 2): the detailed integrated master map (every asset conforms to the locked standard).**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Step 1 ADDENDUM: made the asset/quality system GROWTH-READY (doc-only; npm test + verify GREEN, NO code touched — flag-only). Encoded the growth PRINCIPLE: the pipeline must ACCEPT future assets of different size/resolution/style/quality, but any single VISUAL CONTEXT (region/biome/screen) stays COHESIVE — flexibility ACROSS contexts, uniformity WITHIN (also why the deliberate "out-of-place character" gag reads as intentional). QUALITY-SPEC.md: reframed as TIERED not a single fixed standard — new §0 TIER REGISTRY (T0=LPC-32 baseline stated AS a tier {tileSize,frameSize,family,palette}; reserved slots T1 LPC-64 / T2 alt-style; adding a tier = a config row, not a rewrite); `region.tier` declared per context; cohesion gates C1–C5 now check WITHIN-CONTEXT consistency (against the region's tier) NOT a global hardcoded 32; scale ratios restated as dimensionless tile-units (1:2.7:4.2 holds at any base resolution); `outOfPlace:true` escape hatch for the gag; new §7 LOCATES (does NOT refactor) the 7 hardcoded-32 spots to soften only when a non-T0 tier actually lands (assets.js TILE/FRAME SSOT, worldmap.js DUPLICATE TILE, AssetLoader lpc_terrain frameWidth:32, terrainTiles cols:32 + build_terrain_autotile.mjs, OverworldScene single TILE/'tile_grass'/'lpc_terrain' streamer, SaveManager _CHUNK_PX=1024, discrete/spike local 32s) — bottom line: growth-ready = add tier registry + region.tier, dedupe TILE to one SSOT, make 5 atlas/loader/streamer spots read the tier's tile size; small because geometry already flows from constants. ASSET-MANIFEST.md: added a TIER/FAMILY field + taxonomy table (T0-terrain 32px / T0-actor 64px / legacy-off-tier / reserved T1/T2) and tagged every asset group `[tier: …]` so the cohesion gate can verify "everything in region X is one tier"; +§I growth-readiness flag. NO speculative engineering (no refactor for assets we don't have — constraints LOCATED only). Committed both docs + this line; no force-push. **NEXT (Step 2): the detailed integrated master map.**
## [DONE 2026-06-08] INTEGRATED MASTER DESIGN — Step 1 FOUNDATION (doc-only; npm test + verify GREEN, no code/SSOT/scene touched). Produced TWO measured docs Van reviews before the detailed master map. (1) **docs/ASSET-MANIFEST.md** — every asset MEASURED (read from the files, not guessed): 491 tile/prop/building/FX/character-sheet/audio files measured individually via PIL + the 625-part / 34,569-PNG LPC character working set summarized = ~35,060 assets inventoried, grouped by type (characters/enemies/terrain/buildings/props/FX/audio) with exact px dims, tile-grid, licence (quoted from ledger + per-pack Credits), style tags, and a one-line "builds". GAPS TABLE cross-refs WORLD-BLUEPRINT per-region needs vs HAVE: **11 distinct GAP items** needing sourcing/assembly (Coast dock/wreck/lighthouse ×3, Emberwood volcano-cone/burnt-trees ×2, Spire tower ×1, Peaks terraced-stone-town-exterior ×1, cross-cutting animals/crops/bench/tool-held-items ×4) + ~8 HAVE-but-UNWIRED (rock/snow/lava/ocean/sand all already in the terrain-v7 atlas — a WIRING gap, not a sourcing gap). Gap count by region: GH 0 · Belt/Marsh 0 · Peaks 1 · Coast 3 · Emberwood 2 · Spire 1 · cross-cutting 4. (2) **docs/QUALITY-SPEC.md** — "amazing+cohesive" as a MEASURABLE bar from the LPC-32px / LttP reference: tile 32px / char-frame 64px / zoom 1.25; scale ratio character:tree:building ≈ 1 : 2.7 : 4.2 (visible heights, from measured dims); density TARGET bands per screen (town 15–25, wild 6–12, lull ≥3) above the existing floor; 5 explicit COHESION pass/fails — C1 cliff=one continuous autotiled face (0 orphan tiles), C2 biome-ground=own tile not tinted grass, C3 seam=feathered transition band, C4 resolution/palette unity, C5 scale; + a verify.mjs gate roadmap (R1 grid/family, R2 scale, R3a density-full, C2 biome-ground, C3 seam-transition = gateable; C1 cliff = later). FLAGGED CONTRADICTIONS (reconciled, not overridden): STYLE-GUIDE's "ENVELOP / zoom 0.85" is the discrete era — live = RESIZE + 1.25 (QUALITY-SPEC sets canonical, STYLE-GUIDE to update); off-grid legacy kenney 16px + superseded lpc/ universal sheets (keep off maps); terrain-v7 under-wired (latent capability). Both docs cross-linked to ASSET-LEDGER/STYLE-GUIDE/QUALITY-BIBLE-2.6/WORLD-BLUEPRINT/PEAKS-ART-CANDIDATES. Committed both docs + this line; no force-push. **NEXT (Step 2): the detailed integrated master map design, then the Peaks-fix build (wire C2 rock-ground + C1 cliff-autotiler per PEAKS-ART-CANDIDATES).**
## [DONE 2026-06-08] WORLD-BUILD — **SUNDERED PEAKS** built into its reserved N block (the first region after the re-centre; Playwright BACK → LIVE-verified; all 12 verify gates + 9 suites GREEN; Greenhollow/Belt/Marsh untouched). Two new worldmap.js region adapters (Marsh/Belt pattern): `SUNDERED_PEAKS` (x288–348/y218–278, 60×60) = a mountain BOWL of perimeter+internal CLIFF WALLS (real licence-safe cliff/rock art — extracted `cliff_face`+`rock_crag/boulder/small` from ElizaWy eliza-terrain OGA-BY → public/art/terrain/ + PROPS + ledger) framing a terraced stone TOWN (no-aggro hub; the canonical peaks.js cast Miner/Hunt/Stranger/Bounty/Stonewright with M11/SP2/SP3/SP5 wired into the Overworld QuestEngine), the looming CINDER KEEP (grapple+shard_2 grant point), the riven CLEFT + keep-road pass, a high-pass tarn, scree slopes with the 6 mountain combat archetypes (live outside the town ring), a quarry cache; `FOOTHILL_ROUTE` (x303–333/y278–288) = a channelled, LEGIBLE, FORKING lane (trunk→quarry-road/keep-road + spur cache) bridging GH's N edge to the Peaks mouth (West-Belt collider technique). GATING (gating.js exact, NO soft-lock): a ROCKFALL seals the Peaks mouth until `shard_1` (earned in the open Marsh) — collider gate keyed in data + `_buildRegionGate`; entering Cinder Keep records `tool_grapple`+`shard_2` (`_buildPeaksKeep`). SEAM: Peaks.S=278↔Foothill.N=278, Foothill.S=288↔GH.N=288 (exact; seam-coherence GREEN) + a green→stone ground-tint bleed across the foothill band (stoneness(), mirror of bogness). Minimap: per-region `mapColor` (Peaks stone-grey). LIVE-VERIFIED: walked GH→foothill→(gate blocks w/o shard_1, collider confirmed)→shard_1→Peaks→town→cleft+keep→grapple grant FIRED (tool_grapple+shard_2 recorded, banner). 0 console errors; perf 61fps/avg16.7/max31.7ms with 25 chunks + Peaks streaming. ART OMITTED+FLAGGED (HARD RULE 9, not faked): real SNOW-cap tiles, a true terraced-stone-town building set, a stone GROUND tile (the bowl grass is stone-TINTED = muted-green, not grey) — each wants a targeted OGA/LPC pass. DEFERRED: the full M12 Keep-Sentinel boss fight (as Marsh's Drowned-Guardian) into the Overworld boss flow — grant taken at the Keep entrance for now. Blueprint §1/§9 marked Peaks BUILT. Proof: peaks-ow-greenhollow/foothill-route/foothill-gate/town-entrance/keep-cleft-combat/seam/grapple-grant-banner. Committed per-group; no force-push. **NEXT: Tidewreck Coast (E) — art hunt first (shore/dock/wreck), then build to the river-road/gorge seam.**
## [DONE 2026-06-08] WORLD-BUILD QUALITY AUTOMATION — wired 6 captured [OBJECTIVE] gates into scripts/verify.mjs so the build SELF-ENFORCES the blueprint + world-design rules (a drifting session FAILS verify + can't commit). All pass on CURRENT content (GH/Belt/Marsh); demoed each catches an injected violation then reverted. NEW DATA: src/data/gating.js = the WORLD-BLUEPRINT §3 gating DAG as machine-checkable data (6 areas requires/grants + 2 teases, keyed on SSOT tool/shard ids); + `route:true` flag on WEST_BELT. WIRED GATES (verify checks 7-12): (7) NO-SOFT-LOCKS strict — topo-sort the gate DAG: every required key obtainable, acyclic (no circular gate), no key-behind-itself, ≥1 start area, every tease pays off [proven END-TO-END: injected 'Peaks requires tool_grapple it also grants' → real verify FAILED → reverted]; (8) CHANNELLED-NOT-OPEN — route regions may not contain an open >8×8 walkable block (belt max=3); (9) DENSITY FLOOR — every ~24-tile sector of every region has content (no empty sector found); (10) COLLISION-MATCHES-VISUAL-MASS — a region with non-solid solid-mass props (trees/buildings) MUST have colliders else walk-through (the recurring Ph5 bug); (11) SEAM COHERENCE — adjacent regions share EXACT edge coords (Marsh.E=Belt.W=278, Belt.E=GH.W=288); (12) PROP-KEY INTEGRITY — region prop keys ∈ PROPS manifest. Gates 8-12 demoed via a throwaway harness (each passes clean, fails on injected break). DEFERRED-with-reason (need un-captured pixel/per-region metadata): Part 2.6 A-proxy/B/C/D/E/J/K + WEB-NOT-LINE — flagged in QUALITY-BIBLE, not faked, wire as each region golds. CURRENT-CONTENT FINDINGS: none violate (all GREEN); soft note — Marsh is genuinely sparse (lowest sector content=2) but passes the floor. NO new art (existing ledger unchanged). 9 suites + verify GREEN (now 12 checks); QUALITY-BIBLE marked WIRED vs PROPOSED. Committed per-group; no force-push. (CRAFT-AND-PROCESS-GATES.md referenced in the brief does not exist — noted.)
## [DONE 2026-06-08] WORLD-BUILD DECISION B — re-centred + enlarged the world (coordinate RELOCATION, not a content rebuild; 9 suites + verify GREEN; discrete fallback untouched). ⚠ Playwright DOWN → headless-verified only, live confirmation handed to Van. Applied WORLD-BLUEPRINT §0 decision B while only GH/Belt/Marsh exist: `WORLD_CHUNKS 16→20` (world 512→640 tiles / 20480px) + `GH_ORIGIN` chunk(5,5)→(9,9)=tile(288,288), so Greenhollow centres at (314,308) ≈ world-centre (320,320) and Marsh(W)/Peaks(N)/Coast(E)/Emberwood(S)/Spire(far-N) radiate symmetrically (was NW-biased). Marsh + West Belt are defined RELATIVE to GH_ORIGIN → the whole built cluster moved as ONE coherent +4-chunk(+128-tile/+4096px) shift; internal layouts/quests/cast/combat/safe-zone untouched; the GH↔Belt↔Marsh seam stayed EXACT (Marsh.E=Belt.W=tile278, Belt.E=GH.W=tile288). SAVES: SaveManager v2→v3 migration shifts a pre-recentre save's world position (+4096px) + remaps chunk-delta keys (+4 chunks) so a playthrough is PRESERVED (relocated, not reset); coord-independent systems (karma/quests/inv) untouched; added a SaveManager v2→v3 test (now 6 checks). No hardcoded coords anywhere outside worldmap.js (everything derives from the 2 constants — camera/world bounds, both-regions loader, minimap WORLD_PX, NpcLife schedule world-tiles, interaction/chest coords all auto-shift). The 4 unbuilt regions are coordinate-RESERVED per the blueprint (Peaks x278–353 y206–278; Spire x303–338 y152–202; Coast x388–473 y278–343; Emberwood x288–363 y363–433) — empty but allocated so future builds drop in. Blueprint updated (§0 marked B APPLIED, §1 table = live post-recentre coords). HEADLESS PROOF: coord/seam/spawn/bounds/regionAt all correct; SaveManager 6/6. **NEXT: build SUNDERED PEAKS (N) to the reserved block (art ~80% ready) once Van confirms the re-centre live.** Committed per-group; no force-push.
## [DONE 2026-06-08] WORLD-BUILD PHASE A — authored the full-world BLUEPRINT (doc-only; npm test + verify GREEN, no code/SSOT/map-gen touched). New: docs/WORLD-BLUEPRINT.md = the DELIBERATE master layout every later build session PLACES to (ends the generate-then-patch loop). Reconciles WORLD-MAP + WORLD-STRUCTURE §2.1/2.1a(watershed)/2.1b(channelled+LEGIBLE per the Ph5b lesson)/2.4/2.5 + QUALITY-BIBLE 2.6 + TRAVERSAL + the BUILT coords in worldmap.js. CONTENT: (1) coordinate blocks in TILES for all 7 areas on the 512×512-tile world — Greenhollow x160–211 y160–199 [BUILT] · West Belt [BUILT] · Marsh W [BUILT] · Peaks N x150–225 y78–150 · Spire far-N x175–210 y24–74 · Coast E x260–345 y150–215 · Emberwood S x160–235 y235–305. (2) AUTHORED path network (forking channelled-but-legible routes: West Belt[built], foothill-route→Peaks, river-road→Coast, ashen-road→Emberwood, gated Spire ascent) + the Crossroads waystone. (3) Metroidvania gating DAG with NO soft-locks PROVEN: GH(open)→Marsh(open, earn LANTERN)→Peaks[story shard_1, earn GRAPPLE]→Coast[grapple-gate, earn HOOKSHOT]→Emberwood[hookshot-gate, earn FIREFROST]→Spire[4 shards+firefrost]; every tease has a named payoff key (belt pond-island = HOOKSHOT, resolving the Ph5 'water-crossing' tag; boarded cave M4 = LANTERN). (4) landmarks/secrets/density-floor per region; (5) biome seams reconciled with the watershed; (6) M1–M20 critical-path overlay; (7) ASSET reality-check vs the ledger/catalogue. GATE SELF-CHECK: channelled+legible ✅, density floor ✅, web-not-line ✅ (Spire ascent = the one flagged chokepoint), NO-SOFT-LOCKS ✅ proven, A/B/C cohesion designable ✅. FLAGGED CONTRADICTION (needs Van at lock): Greenhollow is built at chunk(5,5) = NW-of-centre, NOT central as WORLD-MAP says → resolved as option A (keep coords, hub-biased-NW, bulk of exploration E+S, Peaks/Spire in the N strip); alternative B (re-centre or enlarge world WORLD_CHUNKS 16→20) flagged for decision. BUILD ORDER: Peaks next (art ~80% ready) → Coast → Emberwood → Spire, each preceded by a targeted LPC/OGA art hunt (Coast/Emberwood/Spire have flagged art GAPS — CraftPix lib is licence-blocked per the catalogue). Van LOCKS this before the next build phase. Committed doc + this line; no force-push.
## [DONE 2026-06-08] PHASE-2 ASSET-SOURCING: catalogued + licence-vetted the shared library at D:\GameAssetLibrary (CATALOGUE/VET ONLY — nothing imported, no scene/ledger change; 9 suites + verify GREEN, no code touched). Output: docs/ASSET-LIBRARY-CATALOGUE.md. HEADLINE: the library targets low-poly-3D party games + side-scroll pixel platformers, NOT LPC top-down — and the **entire tree ships ZERO licence files** (the manifest's licence tags are curator notes, not evidence). VERDICTS (by pack): ~26 model packs + 7 HDRI skies = CC0 but **3D → UNUSABLE** (HARD RULE 4, parked); ~8 CC0 2D packs (Kenney audio×3/UI×2/medals, Ansimuz parallax, Quaternius 2D-render) = SAFE licence but **~none LPC-style-fit** (UI off-family / parallax wrong-format / low-poly render); **~70 packs BANNED = CraftPix** (proprietary free licence: attribution-required + **NO redistribution** — curator's own README confirms; tell-tale COUPON.png in 35 folders; not a whitelisted CC0/OGA-BY/CC-BY[-SA]/GPL family → can't go in our public repo); ~5 UNKNOWN (no licence, unattributable → unusable). GAP→CANDIDATE payoff: every named gap (rock/cliff, animals, crops, caves, trees, water/slash FX, bog/mountain) is either CraftPix-blocked or **already covered by our staged LPC art** (asset-library/2d eliza-terrain/lpc-terrains/eliza-water/lpc-magic, OGA-BY/CC-BY-SA). The ONLY genuinely-actionable items: **Kenney CC0 audio** (could fill the flagged sfx_death/sfx_hurt cues, ledger-on-import) + Kenney CC0 UI (only if a deliberate off-family UI restyle is ever wanted). RECO: real art gaps (animals/crops/bench) need a targeted LPC/OGA hunt, not this library. Committed the doc + this line; no force-push.
## [DONE 2026-06-07] DESIGN CAPTURE: FATLEY redesign + marker-onboarding RESEQUENCE (doc-only). AUDIT: SG1 today is act-2 adult-return hub, force-grants skill_see_markers on completion (greenhollow.side.test asserts it); nothing earlier needs markers (M1-M6 NPC-given; Pem hunt uses separate pem_clue_*). REDESIGN: Fatley = very fat + SITTING in a quiet out-of-the-way spot, a "?" marker (not "!"); comedy beat = forgot he had a quest → stutters + improvises one on the spot, the mug is right in front of him (sample dialogue written); resequenced to LATE CHILDHOOD (~M5, before M6), not early onboarding; REWARD = an UNLOCK not a grant (completing it makes the marker trait buyable/learnable — opt-in preserved, Fatley = the lazy-guy-teaches-you-buy template). ONBOARDING resequence: early childhood runs MARKER-FREE (M1 wake→find-Bram→greet teaches move/talk/interact diegetically), markers become a late discovered reward-option. Docs updated: PERSONALISATION-DESIGN §6.5 (character+dialogue), QUESTS-SIDE-FLESH G1 (quest+data/asset/test notes), STORY-AND-QUESTS (marker onboarding), GAPS-AND-DEPTH §4 (onboarding). skill_see_markers id stays; PROPOSED ids (marker_sight_offered, child_late) LISTED not added; ASSET FLAGS (sitting/fat pose + "?" marker). Build session will need to update SG1 data + greenhollow.side.test. npm test + verify green; no code touched.
## [DONE 2026-06-08] WORLD-MIGRATION PHASE 5 — west belt rebuilt as a terrain-CHANNELLED route (fixes the §2.1b open-placeholder VIOLATION; additive, discrete + 9 suites + verify GREEN/untouched). ⚠ Playwright dropped MID-build — final live walk re-confirmation deferred to Van/next session (headless-proven instead). Replaced the open-field `chunkContent` scatter belt with an authored **WEST_BELT route region** (src/data/worldmap.js): a 10×34-tile corridor (Marsh E edge ↔ Greenhollow W edge) carved into a winding dirt LANE walled by treelines/bush/water, FORKING at a junction into a NORTH scenic branch (skirts a pond) + a SOUTH direct branch (to the Mirefen hub) = **two viable routes** (WEB-NOT-LINE §2.4); a short SPUR hides a **reachable cache** (belt_cache +30g, reward-for-looking); a chest on a pond **island = see-but-can't-reach TEASE** (§2.5 tease→payoff, gated on a future water-crossing tool [ENGINE]); a trail WAYSTONE landmark + a path-side TRAVELLER (Joss). Green→bog bleed preserved. **ENGINE:** (1) new `R.colliders` in OverworldScene._buildRegion = contiguous invisible 32×32 tile-COLLIDERS, the channel walls — DECOUPLED from prop footprints because a prop's solid body is only ~12px tall in a 32px tile (leaves ~20px vertical seams the hero slips through; the live bug observed = player walked through the bush walls). Bushes/trees are now pure visuals. (2) NpcLife.setPhase guard for scheduleless (static) NPCs. **VERIFIED LIVE (before the drop):** BEFORE=open field, AFTER junction=channelled trail, pond sealed off the lane, player blocked walking off-lane. **HEADLESS-PROVEN (after the collider fix, browser down):** foot-box flood-fill over the real collider rects → 893 lane-cells, GH-entry reaches the Marsh edge at BOTH branch exits (N rows 11-13 + S rows 18-20), tease pond SEALED (unreachable), cache REACHABLE. **NOT re-confirmed on screen (Playwright down):** full GH→Marsh walk after the fix, the GH-trailhead↔belt seam handoff, perf w/ 276 colliders, full visual tour. FLAG: no rock/cliff prop → channelling = treelines+bush+water (true verticality = [ENGINE], deferred); far-field wilds N/S of the corridor remain open scatter (future routes); a proper autotiled trail + the water-crossing tease gate are follow-ups. Proof: proof/p5-00-belt-BEFORE + p5-01-junction + p5-03-pond-sealed. **PHASE 6 (next): re-confirm the belt LIVE when Playwright's back (walk both branches, perf, visual tour) + the next-seam migration (Peaks N).** Handed to Van to walk-judge the channelled route.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 4b — LIVE-VERIFY the Phase-4 UI + fix what's broken (Playwright RESTORED; additive, discrete + 9 suites + verify GREEN/untouched). Ran it LIVE + screenshotted all 9 (HARD RULE 9/10). FIXED: (1) Esc MENU was genuinely broken — `Options` is registered BEFORE `Overworld` in main.js, so the paused-but-still-RENDERING Overworld drew OVER the active Options scene (pause halts update, not render) → menu invisible; FIX = `OptionsScene.create()` now `this.scene.bringToTop()` (no-op for the discrete callers below it) → SETTINGS panel now shows + Esc-closes/resumes. (2) T TRACKER had NO handler — added `keydown-T` cycling full→title→off (mirrors RegionScene `(n+2)%3`) + `_trkState` honoured in `_updateWorldHud`. CONFIRMED working-as-designed/already-working: SWORD (Greenhollow `safeZone:true` = no combat by design → no sword in village; Marsh `safeZone:false` → sword equips+SWINGS+damages, screenshotted the slash landing); HUD stable; O map + H hide work on screen; BORDER no pop (both regions render at the belt — bog LEFT, village RIGHT, no empty footprint); PERF 166fps/avg6.06/max6.1ms w/ both regions+25 chunks+9 NPCs. ADDED the 3 deferred-blind visuals: (7) DIALOGUE PORTRAIT (ported RegionScene's face-RT from `this._faces`; Mara's face renders), (8) OBJECTIVE ARROW (gold chevron toward the active quest's NPC via `_npcByQuest`; confirmed pointing at Hodge for SG3), (9) uiCAMERA + 1.25 ZOOM (zoom-1 `uiCamera` renders ONLY the HUD; main camera at 1.25 ignores it; `_reconcileCameras()` ignores every STREAMED/combat object on the uiCamera each frame → verified 0 leak during Marsh combat + Esc menu still composes on top). 0 console errors; discrete fallback untouched (OptionsScene change is additive/safe). FLAG: Overworld dialogue box + bottom help-bar still use fixed 432-era Y coords → sit mid-screen on the 720 canvas (functional; bottom-anchor to scale.height is a small follow-up, deferred to avoid regressing working code). Proof: proof/p4b-*. **PHASE 5 (next, per Phase-4 entry): the creative route-rebuild — channelled Greenhollow→Marsh path per WORLD-STRUCTURE §2.1b (carved forking routes + blocked spurs, replace the open-field belt placeholder) + the next-seam migration (Peaks N).** Handed to Van to play-judge the FEEL (1.25 zoom vs 1.0, arrow helpful-or-clutter, portrait, mid-screen dialogue box).
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 4 — Overworld UI port + both-regions border loader (⚠ browser MCP DOWN ~4 sessions — VISUALS UNVERIFIED, headless+reasoning only; additive, discrete + 9 suites + verify GREEN/untouched). FIXED: sword (player.equip('sword') — was missing → "no sword"); HUD drift (zoom 1.0 pins the scrollFactor-0 HUD; uiCamera-for-1.25 deferred); ported stats HUD (HP/MP/gold/morality/purity/time) + WORLD-COORD minimap (regions+player) + full-map(O) + tracker(T) + hide(H); Esc→Options (audio/mod toggles; rebind/big-head no-op flagged); BOTH-REGIONS-NEAR-BORDER loader (loads every in-margin region so both interiors render across a border = pop fix; primary=regionAt drives safe/combat, _activeRegions=full set; suppression/attack/respawn/boss guards generalised). Headless-verified: loader picks both regions at belt + minimap maps in-box. DEFERRED (visual, blind): dialogue portrait, objective-arrow, 1.25-zoom-via-uiCamera. Van must confirm on load: sword shows, HUD stable, Esc/O/T/H work, border no pop, perf holds. **PHASE 5 (when tooling back): watch the Phase-4 UI live + fix layout, add the dialogue portrait + objective-arrow + uiCamera(1.25 zoom); THEN the creative route-rebuild (channelled Greenhollow→Marsh path per §2.1b) + next-seam migration (Peaks).**
## [DONE 2026-06-07] DESIGN CAPTURE: terrain-channelled routes + density floor (doc-only; reconciled, no duplication). CORRECTED the model: the seamless world is explorable-at-leisure but TERRAIN-CHANNELLED, NOT an open walkable field — ref Link to the Past / Link's Awakening / Adventure Time (terrain carves space into ROUTES that fork), not BotW Hyrule-field. Added WORLD-STRUCTURE §2.1b TERRAIN-CHANNELLED ROUTES (routes-that-fork + tempting blocked spurs = §2.5 gating made physical + geography-as-puzzle/verticality cross-ref TRAVERSAL/Pillar-4 = [ENGINE] + no-large-open-expanse) and two §2.4 gates: CHANNELLED-NOT-OPEN [OBJECTIVE] (no open traversable area > N tiles without a channelling feature; ≥1 forking junction per stretch; [EYE] flow-routes-not-field) + DENSITY FLOOR per SECTOR (every screen-sized sector has something; no empty useless terrain). Folded an E2 TERRAIN-CHANNELLED TRAVERSAL gate into QUALITY-BIBLE Part 2.6. Explicitly RECONCILED (no dupe): density/web-not-line already in §2.2/§2.4, gating in §2.5, contextual monsters/no-NPC-by-monsters + themed biomes in 2.6 Pillar 2/ecology/Gate D, verticality=ENGINE in Pillar 4/Gate F — added ONLY channelling + the no-open-field gate. APPLY-NOTE flags the CURRENT overworld violation: the green belt + Greenhollow→Marsh space is OPEN PLACEHOLDER scatter (a field, not channelled routes) — must be rebuilt as carved forking routes with blocked spurs (next world-build target). npm test + verify green; no code touched. AUDIT first confirmed BUILT systems (don't re-spec): Economy (buy/sell, CHA haggle, hours/gated stock, jobs, property+passive rent, food/body), Social (CHA price mults, trust over deed-memory, check resolver, bracket gating), items (food/junk=material+trinket/gold/property), and the chest pattern in code (open→dropLoot→_opened-Set empties). NEW: §9 SEARCHABLE CONTAINERS = a `via:'search'` interactable subtype generalising the chest (drawers/cupboards/barrels/in-house chests; data loot table + searched-flag empties; locked option; onSearch quest hooks e.g. pantry-raid; reuses Interaction+dropLoot+deed-memory = [FREE NOW] mechanic; open/search ANIM + depth-sort flagged as asset need, don't fake). §10 ECONOMY WIRING reminder (systems EXIST — needs world placement+wiring: shops/haggle-dialogue/buy-rent-house/breakables-drop-coins = [FREE NOW] build-wiring not engine; cross-ref ECONOMY-BALANCE for faucet/sink). §11 REACTIVE-WORLD HOOKS reusing Karma+Social.trust: reactive greetings/prices/doors [FREE NOW], notice/bounty board surfacing existing side-quests [FREE NOW], helped/harmed place looks different [FREE NOW logic · ASSET flag], reactive crowds [partly ENGINE], owned-house base/storage ([FREE NOW] persistent stash · ENGINE free-form decor). §12 proposed ids LISTED not added (searched_<id>, loot_* tables, town_<region>_helped/_harmed, board_<region>; reuse existing quest/deed/theft ids — no synonyms). Cross-links ECONOMY-BALANCE/Social/Karma. npm test + verify green; no code touched.
## [DONE 2026-06-07] DESIGN CAPTURE: WORLD GEOGRAPHY folded into WORLD-STRUCTURE-DESIGN §2.1a (doc-only, reconciled with WORLD-MAP). One continuous landmass via NATURAL SYSTEMS (hydrology+elevation): N highest (Spire/Peaks = headwaters) → snowmelt streams feed the canon Greenhollow brook → widens to a river crossing the vale → runs E downhill to a delta meeting the Tidewreck OCEAN (one river stitches 3 regions); Ashen Marsh (W) = the LOW WET BASIN (so it's wet by cause); Emberwood (S) = a VOLCANIC massif under the wood with separate warm meltwater; minor variety (sea cliffs/sundered clefts, passes, vale lake + high tarn, wetland fringes) follows the elevation skeleton. RECONCILED every feature with region identities — ocean=Tidewreck, bog=Marsh, mountains=Peaks all match; the one tension (WORLD-MAP says Emberwood is a WOOD, not volcano) RESOLVED additively: the volcano = Emberwood's fire ENGINE / physical vessel of the god's fever (lore-consistent), wood stays the surface identity, no WORLD-MAP edit needed unless Van names it. TAGGED [FREE NOW] (painted courses/lakes/ocean/volcano cone/elevation read) vs [ENGINE FEATURE] (water-crossing/swim/raft, climbable-volcano/real-verticality, lava-ice hazard-gates, tide-timed water) cross-ref TRAVERSAL — did NOT promise engine terrain as free. Tied to §2.5 gating (lantern/climb-grapple/hookshot/fire-frost = the water+elevation gates) + Part 2.6 (watershed/contour continuity = Level-B seam believability). npm test + verify green; no code touched.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 2b — Ashen Marsh migrated onto the overworld + green→bog SEAM + LIVE combat (additive; discrete + 9 suites + verify GREEN/untouched; Greenhollow Phase-3 + Peaks intact). worldmap.js: ASHEN_MARSH adapter placed WEST of Greenhollow (east edge 10 tiles from GH west = the belt), enemies/safe-hub/boss at absolute world tiles, bog tint/pools/reed decals; the green→bog SEAM = a continuous groundTintAt(x) gradient (grass→BOG_TINT, no step) + bogness-driven prop/decal shift; REGIONS list + regionAt + distance-to-box region picker. OverworldScene: generalised _loadRegion (handles combat regions), ported the full combat stack (EnemyController+PlayerCombat+J/Space/C+hit/HP/boss), enemy spawn-on-load skipping kill-deltas, save link, suppression-only-under-loaded-region + restream-on-toggle. VERIFIED LIVE: Level A (7 archetypes+counters, kill, safe-hub 0-aggro, boss+lantern, Hagga/Yssa dialogue) + Level B (continuous walk, smooth green→bog tint gradient, combat NOT in Greenhollow) + save/reload preserves kills (7→6, 0.3/2.3ms) + perf 166fps/6.08ms; 0 errors. FLAG: one-region-at-a-time → adjacent region's interior empty at the very border (polish: both-regions-near-border loader); deep Marsh art wants its own gold pass. Proof: ow-p2b-marsh-play/seam. **PHASE 4 (next): HUD/dialogue/minimap re-wire on the overworld; then migrate the next seam (Peaks N, or Coast/Emberwood). Optional polish: both-regions-near-border loader + Marsh gold-standard art.** Handed to Van to play-judge the seam + streamed combat.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 3 — Greenhollow gold-standard ART restored on the overworld (additive; discrete + world.js + 9 suites + verify GREEN/untouched). Closed the function-first art gap to discrete-v3: (1) autotiled feathered terrain — ported the v3 RenderTexture corner-autotiler onto the region (gravel/road/dirt/soil interpenetrate grass), killed the per-chunk tint-step (uniform grass); (2) depth-band → seamless BLEED — green-belt tree density ramps from the village outward (dense edge → thinning belt; trailheads = road gaps), no longer a map-edge wall; (3) lived-in decals — 210 village decals (tufts/grass/flowers/ferns/dirt, seeded, FLOOR-pinned) + the SW brook + light belt flowers. All RESIDENT. VERIFIED LIVE: terrain RT + decals present, function intact (cast 7, M1 completes), seam seamless (region+art unload→reload, no leak), perf 163fps/6.67ms, 0 errors. DEFERRED (next session): HUD/dialogue UI + minimap; depth-band edge-tuning still slightly wall-like at non-trailhead edges. Proof: ow-p3-terrain/belt-bleed/village-play. **PHASE 4 ENTRY (next): the HUD/dialogue/minimap re-wire on the overworld (port the polished dialogue box/portrait + the stats/minimap/full-map/quest-tracker from the discrete HUD to world-coords), THEN Phase 2b = migrate the Marsh seam (W green→bog bleed).** Handed to Van to play-judge the gold-standard match + flag what's below the bar.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 2 — Greenhollow migrated onto the seamless OverworldScene (FUNCTION first; additive, discrete regions + 9 suites + verify GREEN/untouched, still run via boot/M/N/G fallback). Greenhollow = a world-coord REGION unit (src/data/worldmap.js GREENHOLLOW adapter: WORLD props/cast/chests offset to world-coords, central per the geography spec; schedule tiles → absolute world tiles so NpcLife runs unmodified). OverworldScene composes the real engines (Karma/Quest/Time/Inventory/NpcLife/Interaction/Dialogue/Social), loads/unloads the settlement by proximity, wires interaction→dialogue (minimal box), chests→SaveManager chunk-delta, safe-zone (world-rect), and link()s the systems into SaveManager. VERIFIED LIVE: LEVEL A (cast 7; M1→M2→M3+SG1/SG3 quests open+complete+chain-unlock; Bram social; chest +40g+delta; safe-zone; NpcLife schedules) + LEVEL B seam (walk out→region unloads→back→reloads w/ chest looted + quests intact) + save/reload from storage (pos+quests+gold+deltas; save 0.4/load 9ms) + perf 165fps/6.15ms; 0 errors. DEFERRED to the POLISH pass: Part 2.6 v3 autotiler terrain + depth-band + decals, polished dialogue/HUD/minimap, ambient FX. FLAG (pre-existing, not a regression): M4 cave trigger unwired in discrete too. Proof: overworld-greenhollow. **PHASE 3 ENTRY (next): the POLISH pass — restore Greenhollow's Part 2.6 gold-standard art on the overworld (autotiler/depth-band/decals/HUD/minimap), then Phase 2b = migrate the next seam (Marsh, the W green→bog bleed).** Handed to Van to accept the function-first migration + GO for the polish pass.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 1 — OverworldScene shell + SaveManager (the seamless foundation; ADDITIVE, real regions + 9 suites + verify GREEN/untouched, still run via M/N/G). Built: src/scenes/OverworldScene.js (production seamless scene — one world-coord space, object-streaming around the real Character hero w/ RESIDENT real art per the spike, continuous fixed-zoom follow camera, depth-sort over the streamed set), src/data/worldmap.js (the world-coord chunk-content contract regions slot into; Phase-1 = placeholder grass+props+test orbs), src/systems/SaveManager.js + .test.js (v2 versioned save: world-position + per-chunk deltas opened/killed/picked/flags + forward-migration + link() composition + corrupt-save survival; 5 tests green, added to npm test). Engine: added DepthSort.untrack (flagged minimal gap for stream-unload). VERIFIED LIVE: 164fps/avg6.06/max6.7ms, 30/256 chunks, 0 errors; SAVE→RELOAD round-trip restores position EXACTLY + per-chunk picked-deltas don't respawn (save 0.2ms/load 0.6ms); fresh SaveManager from storage = durable. EMPTY-but-WORKING (no region content). Proof: overworld-shell. **PHASE 2 ENTRY CRITERIA (next): Phase-1 green (✓). Migrate the FIRST real region (Greenhollow) onto the OverworldScene — author it as world-coord chunk content in worldmap.js, wire NpcLife/Interaction/combat-safe-zones/minimap to world-coords per the impact matrix, compose the per-system saves into SaveManager via link(), re-verify Level A (the region passes its gates) + Level B (its seam) + keep the discrete scenes as the fallback. Handed to Van to accept GO for Phase 2.
## [DONE 2026-06-07] WORLD-MIGRATION ASSET-LOAD SPIKE (throwaway AssetSpikeScene; closes the one Phase-0 unknown; real regions + 9 suites + verify UNTOUCHED/green). Tested real PNG/atlas decode+GPU-upload mid-game (Phase 0 used generated textures): streamed the real lpc_terrain 8MB-RGBA atlas per chunk under cache-busted keys (genuine re-decode+upload), auto-sprinting. RESULTS (60fps=16.7ms budget): STREAM budget-1 = 162fps/avg6.5/max11.6ms/0 drops; STREAM budget-99 WORST CASE = 162fps/avg6.1/max10.9ms/0 drops; RESIDENT baseline = 165fps/flat 6.06ms. A single 8MB upload ≈5ms on its frame, never a dropped frame. VRAM: 2.8MB art on disk → resident ≪100MB vs 8GB (<2%). 0 console errors (after a recycled-null-frame fix). STRATEGY = RESIDENT for Phase 1 (load once, flat 6ms; 2D art fits VRAM trivially); streaming proven safe as fallback (budget uploads ~1/frame). GO/NO-GO = **GO** — last Phase-0 unknown CLOSED; proceed to Phase 1 (OverworldScene + SaveManager) with resident art. Proof: asset-spike-hud. Handed to Van to accept GO + strategy.
## [DONE 2026-06-07] WORLD-MIGRATION PHASE 0 — seamless-streaming PROTOTYPE (throwaway, de-risk; real regions UNTOUCHED + tests green). Built isolated PrototypeScene (src/scenes/PrototypeScene.js, registered in main.js but NOT in boot flow — start via __EMBER.scene.start('Prototype'); generated placeholder textures, imports no real data). PROVED on a 256×256-tile/256-chunk/16384px world: one world-coord space, chunk streaming (20-36/256 loaded, world never fully resident), seamless handoff (load ring ≫ viewport → no pop), continuous follow camera (no seam jump), POOLING (full W→E traverse → ever-created plateaued at 36, zero churn). PERF uncapped: 165 FPS, frame avg 6.06ms / max 6.1ms traversing, 0 drops, 0 load-hitches — even worst-case all-at-once + fresh 25-chunk jump stayed flat ~6ms (only cold-boot = one 21.8ms frame). 0 console errors. HONEST OPEN ITEM: mid-game asset-BUNDLE load from disk NOT tested (used generated textures = the one remaining hitch risk); real chunks heavier (autotile RT bake + NpcLife + enemies). GO/NO-GO = **GO** (full seamless viable w/ huge headroom; connected-scenes middle path not needed on perf; remaining work is engineering not feasibility). Next: Phase 1 (OverworldScene + SaveManager) after an asset-bundle-load spike. Handed to Van to play-judge feel + accept GO. Proof: proto-seam-grid + proto-seamless-region.
## [DONE 2026-06-07] DESIGN CAPTURE: docs/WORLD-STRUCTURE-DESIGN.md — ONE BIG WORLD plan (committed architecture; doc-only). DECISION: one seamless huge overworld in a single world-coord space (walk across boundaries, no hard cut, fast-travel between discovered points); interiors/caves/dungeons STAY discrete loaded scenes (door→load, reuses RegionScene = scope reducer). THREE pillars: (1) BUILD — target arch (OverworldScene + streaming), chunk/area streaming + memory/draw budget (4060/32GB: RAM not the constraint, draw-calls/objects/textures are), seamless handoff (shared edges/coord continuity/transition bands), the SYSTEMS-IMPACT MATRIX (RegionScene split, transitions, save/load[=SaveManager prereq], NpcLife, depth-sort, camera[cover-zoom removed], minimap+full-map, combat/safe-zones, asset-streaming, interaction — what changes/breaks/protected), PHASED MIGRATION with **PHASE 0 throwaway prototype** (prove streaming+handoff+camera+budget on a sacrificial world BEFORE touching real regions = the go/no-go) then one-seam-at-a-time w/ full re-verify + rollback. (2) FILL — world layout (reconcile WORLD-MAP: 5 regions + Spire radiating from Greenhollow), DENSITY/PACING (POI-density floor, deliberate-lull-not-dead-space) as [OBJECTIVE] gates, cohesion (Part 2.6 at WORLD scale), filled-nicely gates. (3) VERIFY — Van's three-level ladder: LEVEL A each-area-alone, LEVEL B seams (the level big maps fail — 5 explicit seam gates), LEVEL C whole-world (the "wander the whole world with no quest + enjoy it" headline), tied to HARD RULE 9. PROMINENT cost/risk: largest/highest-risk change, many sessions, biggest never-regress threat, an honest mid-migration not-fully-playable window; Phase-0 + the ladder are the safeguards. AUDIT of the current discrete-scene baseline included. Proposed SSOT ids LISTED not added. Cross-links WORLD-MAP / QUALITY-BIBLE 2.6 / TRAVERSAL-EXPLORATION. npm test + verify green; no code touched. ADDENDUM (2026-06-07): folded into Pillar 2 — committed GEOGRAPHY (Greenhollow central + green belt; regions radiate out; between-settlement traversable WILDS easy→tricky; WEB-not-line many-routes), new §2.5 PROGRESSION GATING design-law ("fully explorable but only when ready" — story/item/ability/time gates + permanent secrets, Zelda see-it-early/reach-it-later + tease→payoff + bombable-wall returns, with [OBJECTIVE] soft-lock-safety gates: clear eventual key/no circular gate, tease→remembered payoff, web-not-line, no accidental early access), §2.6 ECOLOGY note (region monsters + pets by source: wild/shop/quest — reserve, detail later), and a LEVEL D forward-ref = queued GRAND COHERENCE REVIEW capstone (map+quests+stories+time+abilities+items checked individually/against-each-other/physically/as-a-timeline once world built). Gating SSOT ids listed not added.
## [DONE 2026-06-07] COMBAT/LIFE TUNING (3 play-judged fixes; logic/counters UNCHANGED, tests green): (1) THREAT ARROWS off by default — gated the enemy directional aim-line on this.indicators (ENEMY-INTUITION perk/toggle); wind-up readability (tint/pulse/rings/guard-arc) stays = Gate E; verified off-default + on-with-perk (own commit). (2) BOSS (Drowned Guardian) — slowed re-face (GUARD_TURN_FRAMES 22 = flankable, ~4 face-changes/2 circles) + raised threat (speed 2->3, guard advance 8x->13x = 39px/s, dmg 14->16); fixed the duplicate inline stat table; phase/trick/twist intact. (3) ROAMING — pre-aggro idle now a gentle patrol near home (NpcLife-style seeded jitter, ROAM_RADIUS_PX 80, bosses excluded, de-aggro returns to roam); verified idle enemies move 12-19px then aggro normally. standards.js NPC_LIFE/COMBAT knobs. No Greenhollow regression; 0 errors. Proof: proof/arrows-off-default + arrows-on-perk.
## [DONE 2026-06-07] COMBAT JUICE PASS (Marsh first fight; feel/feedback only, logic+counters+balance UNCHANGED, all combat tests green): + INPUT BUFFER (140ms, no dropped presses), enemy HIT-POP (1.16× squash-punch), KILL emphasis (9f freeze + .013 shake), DEATH POP (kill-flash + shock ring + debris + death cue), DODGE reads as a real escape (110px/265ms i-frames). All tuning in standards.js NPC... COMBAT (re-tunable; deltas in PLAYTEST-LOG). Verified live on Marsh (hit/pop/death-FX/buffer/dodge) + inherited by Peaks; Greenhollow safe unaffected; 0 errors. FLAG: dedicated sfx_death/sfx_hurt CC0 cues. Proof: proof/juice-death-pop. HANDED to Van to play-judge (weight/escape/telegraph-fairness/kill-satisfaction).
## [DONE 2026-06-07] NpcLife DE-SYNC (kill clockwork lockstep): per-NPC SEEDED jitter so a crowd reads as individuals — staggered departure delays (0.3-2.5s), persistent ±18% speed multipliers, ±12px target jitter (paths differ), eased facing (turn-cooldown, no synced pivot), out-of-phase chore loops, + a tempo PERSONALITY trait (brisk/ambler/dawdler). Tuning in standards.js NPC_LIFE; deterministic (mulberry32, no Math.random). Verified: speeds 50-93 all differ, delays fan 0.43-3.37s, mid-transition states mixed (never lockstep), all reach home, quests/social/chests intact, no Marsh/Peaks regression. New Part 2.6 Pillar 2 rule "crowds are noisy, not uniform". npm test + verify green; 0 errors. Proof: proof/npclife-desync-transition.
## [DONE 2026-06-07] NPC-LIFE system (reusable, Part 2.6 Pillar 2): src/systems/NpcLife.js + RegionScene wiring. DATA `schedule:[[phase,tx,ty,activity]]` drives NPCs by TimeOfDay phase (day-work / night-home), looped chore activities (hammer/tend/chat/sleep), seek+collision movement (movable actors separate from buildings/dividers; unstick nudge; FLAG navmesh for cross-divider routes). Ambient hook (cfg.world.ambient: birds FX + inert/flagged critters slot — no animal sprites). Interaction follows moving NPCs (talk mid-routine), depth-sort by base, minimap/objective live, dialogue freezes the village. Greenhollow cast given routines — all reach day+night spots, quests/social/chests intact; NPCs without schedules stay static (Marsh/Peaks unchanged). Schedule format documented in REGIONSCENE-SPEC. npm test + verify green; 0 errors. FLAG: night-darkening (dusk/dawn tint lerp) still ENGINE → day/night contrast subtle. Proof: proof/npclife-*.
## [DONE 2026-06-07] GREENHOLLOW nudge (small): BUG1 big-head defaults ON → root-caused (GreenhollowScene devModifiers:true used modifiers' devDefault) → set devModifiers:false (off by default; toggle on→1.8/off→1 verified). BUG2 person clips behind building → root-caused (DepthSort track() passed footprint CENTRE not BASE, contra its own header) → sort by footprint base (offY+h/2) props+chars; verified occlude behind + render in-front. DISTRICT DIVIDERS: hedges (solid bushes) breaking plaza/homes/smithy/farm sightlines, all routes re-verified passable. TASTE OF LIFE: chimney smoke FX on 3 homes (animal sprites flagged not-loaded). Cast/quests re-verified; no Marsh/Peaks regression; npm test + verify green; 0 errors. Proof: proof/greenhollow-v3b-*.
## [DONE 2026-06-07] DESIGN CAPTURE: World-Design Constitution. (A) EXPANDED QUALITY-BIBLE Part 2.6 into the full SETTLEMENT rule-set — the FOUR PILLARS at all scales (hamlet/town/city): P1 spatial structure (enclosed rooms / diegetic district walls / route hierarchy / reveals-concealment / landmarks / desire-paths), P2 LIFE & movement (NPC routines + chores + animals + ambient motion + sound pockets — flagged the BIGGEST gap, mostly ENGINE), P3 variety & storytelling (wealth gradient / buildings-hint-occupants / edge variety / wear-age / secrets), P4 SCALE tiers (hamlet→town→city + verticality[ENGINE] + density gradient) + a "how to build a settlement" checklist. Each rule rule·why·[OBJECTIVE]/[EYE]·[FREE NOW]/[ENGINE]·scale-tag. (B) CREATED docs/TRAVERSAL-EXPLORATION-DESIGN.md — the OoT world-traversal pillar (reward-for-looking, vistas, terrain-as-puzzle, jumps/ledges, tool-gated metroidvania nooks, unlocking shortcuts, layered secrets, landmark nav, biome-as-mechanic, environmental storytelling, pacing, the "Fun Without a Quest" headline gate) with a prominent ENGINE-PREREQUISITES summary (jump/height=heaviest, water, swappable exits, distant-vista, NPC pathing — each its own scoped session). Cross-linked both ways; shared BUILD-ORDER (capture → small Greenhollow nudge[bugs+dividers+life] → NPC-life reusable system → cut/lift bushes → traversal engine sessions → Marsh to the bar). Proposed SSOT ids LISTED not added. Doc-only; npm test + verify green; no code/SSOT touched.
## [DONE 2026-06-07] GREENHOLLOW v3 — SPACE-FIRST + real autotile transition tiles (Van play-judged v2 not-quite-right). Refined QUALITY-BIBLE Part 2.6 (space-first / enclosure / off-centre node / districts / path hierarchy / boundary DEPTH BAND / diegetic trailheads / real transition tiles via Gate D upgrade). Built: the [LPC] Terrains corner-autotile atlas loaded + a parser (scripts/build_terrain_autotile.mjs → src/data/terrainTiles.js) + RegionScene autotiler (grass base + RenderTexture of feathered transition tiles) — gravel/dirt/soil interpenetrate grass (no hard squares); space-first relayout (off-centre enclosed plaza, districts, farm margin, path hierarchy); 3-deep boundary depth band w/ scale+tint gradient + trailheads (spillCount 0); SOLID undergrowth (fixed prop_bush footprint, player verified blocked) to channel movement; camera deadzone. Cast/quests/social/secrets re-verified live; no Marsh/Peaks regression; npm test + verify green; 0 errors. Proof: proof/greenhollow-v3-*. FLAGS: bench + crop-growth-stage sprites.
## [DONE 2026-06-07] DESIGN CAPTURE: docs/GAPS-AND-DEPTH.md — the "good design → great game" register (13 items, each rule·why·[FREE NOW]/[ENGINE FEATURE]). SCOPE NOTE corrects the record: 40+ quests / ~30h is the SUPPORTED TARGET (not shrunk); defines a POLISHED MILESTONE (Greenhollow+Marsh+first shard to the Part 2.6 bar w/ tuned combat) as sequencing insurance. Items: (1) game-feel/JUICE = highest-leverage next session [free]; (2) XP/levels/SKILL TREE = biggest gap (new abilities not just numbers) [free design, some abilities engine]; (3) earned/diegetic unlocks (modifiers bought not default; red/green social-sight skill) [free]; (4) onboarding ~30min childhood [free]; (5) SAVE via CRYSTALS no-autosave — HONEST STATE: per-system serialize exists but live game uses volatile memoryStorage, no unified save/crystal/load-on-boot [ENGINE FEATURE, load-bearing]; (6) AUDIO as a system (tension states + scripted stings + leitmotifs; CC0 palette first) [free palette + engine state-machine]; (7) synthetic NARRATOR TTS first (Piper/Kokoro/Chatterbox/CosyVoice2; XTTS/F5/Fish weights BANNED non-commercial; no NPC voice till script frozen) [free local]; (8) NPC schedules [free] + smooth dusk/dawn tint lerp (no phase tints exist today) [engine, small]; (9) living world: time-skip transforms ONLY childhood-seen areas + ongoing changes [free, alt data+flags]; (10) TRICKSTER WIZARD quest — moving geography w/ mandatory CALLING CARD (never reads as a bug), shakedown economy hook, 4 morality-distinct resolutions [free design + small engine for swappable exits]; (11) accessibility/difficulty + colourblind-safe telegraphs [free/engine per item]; (12) PERFORMANCE BUDGET binding discipline (cull/pool; 4060 over-spec, jank=code) [free]; (13) DEPLOYMENT: static-hostable, saves LOCAL, concurrent players = static bandwidth not Van's PC, NOT multiplayer [free]. Build order: juice pass on Marsh first → wire Part 2.6 verify gates → skill tree → Marsh to bar → SaveManager. Proposed SSOT ids LISTED not added. Doc-only; npm test + verify green; no code/SSOT touched.
## [DONE 2026-06-07] DESIGN CAPTURE: docs/INTERACTABLES-DESIGN.md — write-once DATA-driven interactable world objects (Zelda-grade playground: cut/lift/throw/break/smash/push/bomb + tool-gated access). AUDIT (honest): the ONLY world-object interaction today is the press-E chest; tools are deed-flags gated at QUEST level (firefrost/grapple/hookshot/lantern — tool_dash does NOT exist, the dash is the dodge-roll); foundations reusable (Interaction, attack-arc, EnemyController projectiles, inv.add, deed memory). One Interactable DATA shape ({via, tool, hp, result[], respawn, fx}) + InteractableController (mirrors EnemyController). HONEST ENGINE SPLIT: FREE-NOW (bombable walls, cuttable bushes, breakable crates, tool-gated access, dropLoot — small/med) vs NEEDS-ENGINE (lift-carry, throw-arc+collision [reuse projectiles], pushable-block grid — med/large). Proposed Gate K (Part 2.6: ≥3 interactable kinds + breakable-gated secrets feed Gate J) as a verify.mjs check; binding asset policy (free/OGA/CC0 first, flag don't source). Build order: free-now playground first, pushables next, throw deferred; region set-piece deps flagged (Peaks grapple/dash, Coast hookshot). Proposed SSOT ids LISTED not added. QUALITY-BIBLE Part 2.6 cross-links Gate K. Doc-only; npm test + verify green; no code/SSOT touched.
## [DONE 2026-06-07] DESIGN CAPTURE: QUALITY-BIBLE Part 2.6 — SPATIAL / WORLD-COHESION DoD (peer to Part 2.5, under HARD RULE 9). Named, checkable gates A–J every region build is judged against (Greenhollow = reference): A organic-layout-not-grid, B paths-curve-&-wear, C diegetic-boundaries (the tree-fix reference, spillCount 0), D feathered-biome-edges, E ground/scenery-variety (≥4 types), F elevation/relief (cosmetic-free vs NEEDS-ENGINE flags), G caves/interiors (cave-mouth + interior-scene FLAGS), H signature set-pieces (underwater-layer etc. FLAGS), I landmarks/legibility, J secrets (≥3). Each tagged [OBJECTIVE] (measurable; the wireable ones flagged → verify.mjs for a later code session) or [EYE] (Van play-judges). Binding ASSET POLICY (free/OGA/CC0 first; spend only after working+feels-right+licence/anti-AI check; Mana Seed banned). PROJECT-STATE §8 #2 cross-links it. Doc-only; npm test + verify green; no code/SSOT touched.
## [DONE 2026-06-07] DESIGN CAPTURE: docs/PERSONALISATION-DESIGN.md — covert "it knows me" personalisation (Ultima-IV gypsy pattern, payoffs local/save-state) = profiler + PERSONAL-STAKES RESOLVER (refactors PH1/PH4/M16 onto affinity, deeds cart_sacrificed_known etc.) + earned epithets; belief fork across PD pantheons (Greek/Egyptian/Norse) + humanist empty-altar branch + miffed unchosen gods; false-godhood comedic long-con; the RIVAL metafiction protagonist (RivalController later; Marvellous Murupara/Gore booster gag); cameo roster (Diogenes spit, Sagan→pie→cop chain w/ apple_pie, science-advice strut pool, Josh/Yeshua + jolly Buddha, cheerful Joseph-Smith pastiche w/ Salamander-Letter wink, Scientology tomato e-meter, historical/mythic slate); NZ gag pack (togs/undies, ghost chips, Pavlov, etc.); 2 NEW thought experiments (Descartes ties to Rival, Mary's Room) atop existing PH2/3/5/6; late-game seer REVEAL closes it as warmth. Safety rails inline (clearly-fictional/PD only, never minors, witty-not-explicit, NEVER a faith-is-fraud punchline). Proposed SSOT ids LISTED ONLY (not added — verify stays green). Build AFTER region/HUD; doc-only session (npm test + verify green; no code touched).
## [DONE 2026-06-07] HUD pass (shared in RegionScene, every region): STATS (HP/Gold/Morality+Purity-tiers/Time-phase — all real systems; MP reserved+greyed, no mana system yet) + MINIMAP + FULL-MAP (rendered from real ground/props/POIs) + QUEST TRACKER (QuestEngine active title+objective, T cycles detail) + OBJECTIVE arrow (toward the quest-giver; glowing-path flagged). Toggles in the control-map SSOT (O/T/Y/H, remappable). TimeOfDay now instantiated + ticked in RegionScene. Verified live (HP/gold/time/karma/tracker update; toggles work); npm test + verify green; 0 errors. Doc: docs/HUD-DESIGN.md. Proof: proof/hud-*. FLAGS: mana, glowing-path, toggle persistence (save plumbing).
## [DONE 2026-06-07] GREENHOLLOW naturalism + tree root-cause: diegetic BOUNDARY treeline walls all 4 edges (W marsh-gloom / N foothill-grey ridge / road gaps + gate posts), EDGE-SCATTER decals soften grass↔path seams (no transition tiles licence-loaded), village re-clustered + winding tracks, secrets/cast/quests/social re-verified. TREE CLIPPING root-caused = camera bounds extended 3 tiles past the play bound into empty void (defect a, NOT the sprite); fixed by the boundary (every sprite kept in-bounds, spillCount 0, rightmost tree 1646≤1664). npm test + verify green; 0 errors. RegionScene gained _edgeScatter(). Proof: proof/greenhollow-v2-*. (HUD = the next session.)
## CURRENT STEP: PHASE 2 BUILD — [DONE] engines + Greenhollow M1-M7 + Ashen Marsh COMPLETE (M8-M10 + SA1-SA5), AND the GREENHOLLOW HUB side quests + childhood callbacks (src/data/quests/greenhollow.side.js): SG1 Fatley marker-onboarding (skill_see_markers; the sanctioned Gate-D exception), SG2 Pem hunt (cross-region clue assembly -> pem_found; closes pem_clue_marsh), SG3 Hodge job (repair skill), SG4 Mara's Letters (reacts to drowned_letter; expose -> mara_exposed), SG5 Tankard Songs, SG7 Orchard Thief (locked-out if chicken_kicked), + a reactive GHUB vignette where the village remembers chicken_kicked/coin_returned/cave_lore/grief_vow. Self-tested (`npm test`, 37 checks; the Act-1 seeds visibly pay off). AND SUNDERED PEAKS COMPLETE (region 2): M11 arrival (miners-vs-owner dispute) + M12 Cinder Keep (tool_grapple + SHARD #2 = shard_2 [2/5] + order_chose_lie truth, reactive to the M10 decision) + SP1-SP5 (The Order's Records + pem_clue_peaks, Crag Beast bounty, The Stubborn Miner/Mike Hunt faction fork, grapple-gated High-Pass Climb, and the SIGNATURE morality-reactive TRICK 'A Stranger's Plea'). Files: sunderedpeaks.js + sunderedpeaks.side.js. Self-tested (`npm test`, 45 checks; shard 2/5). AND TIDEWRECK COAST COMPLETE (region 3): M13 arrival at Saltbreak (lean_smugglers/lean_authority) + M14 The Drowned Vault (tool_hookshot + SHARD #3 = shard_3 [3/5] + vault_betrayal truth, reactive to the M10 decision, foreshadows M17) + ST1-ST5/ST8 (Smuggler's Bargain/Hugh Jass faction fork ET5, hookshot-gated Tide-Cave Treasures, the sad Lighthouse Keeper/Holden McGroin ET2, Wreck Wraith bounty, the SEEDED Saltbreak Betrayal reactive-to-faction ET7, Message in a Bottle -> pem_clue_coast). Files: tidewreck.js + tidewreck.side.js. Self-tested (`npm test`, 54 checks; shard 3/5; 3 Pem clues of 4). AND EMBERWOOD COMPLETE (region 4, the god's fever): M15 arrival (the burning/freezing settlement) + M16 Ember Hollow [PERMANENT DECISION 2] (tool_firefrost + SHARD #4 = shard_4 [4/5] + the god's-agony beat + mercy/seize/take — god_mercy[+mercy_shown] opens Liberator/secret, god_seized LOCKS Liberator) + SE1-SE5 (Fire and Frost + pem_clue_emberwood [4th clue -> SG2 Pem hunt now completable -> pem_found -> secret Ashbearer reachable], the HEAVY no-win Caught Settlement [ET4 + lost half], firefrost-gated Ashen Relics, the heartfelt Weeping Tree [weeping_tree +P, ET11], Cinder Stag bounty). Files: emberwood.js + emberwood.side.js. Self-tested (`npm test`, 63 checks; shard 4/5; all 5 endings now gateable). AND THE SPIRE ENDGAME COMPLETE (src/data/quests/spire.js): M17 'Sela's Design' [PERMANENT 3 — trust(W,locks L)/oppose(sela_opposed,L)/alone; reactive betrayal on hagga_reported/corrupt; OPPOSE gated on Hagga allied] + M18 The Long Ascent [gated on all 4 tools; deed-memory echoes allies/enemies by Morality; the kicked-chicken cameo iff chicken_kicked; mercy/cruelty] + M19 The Binding Chamber [the over-built-up brutal boss + SHARD #5 = shard_5, 5/5] + M20 'The Choice' [resolves via Karma.reachableEndings(): Warden/Saint/Tyrant/Liberator + secret Ashbearer; offeredEndings() presents only reachable; epilogueCards() layers ET1-ET12 by deed]. Self-tested (`npm test`, 71 checks across 9 suites). AND a set of 6 diegetic PHILOSOPHY / thought-experiment side quests PH1-PH6 (spread across the regions, the dilemma lived never named): PH1 The Runaway Cart (Peaks; trolley, no-win), PH2 The Barber of Saltbreak (Coast; Russell paradox), PH3 Grandfather's Axe (Greenhollow; ship of Theseus), PH4 The Veil (Emberwood; Rawls, ties to SE2), PH5 The Experience Stone (Marsh; Nozick), PH6 Buridan's Mule (Greenhollow; comic). In each region's side file; self-tested (`npm test`, 79 checks across 10 suites). AND a set of CAMEO / EASTER-EGG / EDGE-TONE encounters (src/data/quests/cameos.js, the full wholesome-to-edgy mix): CAM1 The Cynic in the Marble Hall (Diogenes), CAM2 The Stone That Won't Stay (Sisyphus), CAM3 The Short General (Napoleon height myth-bust), CAM4 Stardust (Sagan wonder, +P), CAM5 The Wanderer Out of Time (rare-spawn time-traveller), CAM6 The Chill Prophet (a clearly-FICTIONAL party-prophet pastiche + gladness buff), CAM7 Blow on the Pie (NZ easter-egg + meat_pie heal); EDG1 The Midnight Raid (adult farce, innuendo never explicit) + EDG2 Flowers for Someone (wholesome +M counterpoint); GAG1 Roadside Wit. All clearly-fictional, witty-not-explicit, never minors; self-tested (`npm test`, 88 checks across 11 suites). === THE ENTIRE DESIGNED NARRATIVE IS NOW BUILT: the full main line M1-M20 + all 5 endings + epilogue twists + every region's side quests, all as DATA on the write-once engines. ===
## ALSO BUILT (systems, this session): ECONOMY + ITEMS + LIFE-SIM as write-once data-driven engines — src/systems/Inventory.js (player holdings: gold/items/equip/skills/food-body-state/property) + src/systems/Economy.js (regional buy/sell, level/karma-gated stock, jobs, books-teach-skills, property+rent, food->HP+body-state, capped death penalty) over src/data/items/ + src/data/economy.js. Balance: docs/ECONOMY-BALANCE.md. Self-tested (`npm test`, Economy suite 11 checks; 12 suites green). Gold lives in Inventory, not Karma (kept Karma single-purpose).
## ALSO BUILT (systems): DAY/NIGHT cycle (src/systems/TimeOfDay.js + src/data/time.js — phases dawn/day/dusk/night, phase/day callbacks, the requires-pattern gains a time axis via a FLAGGED QuestEngine extension so quests/shops gate on req.phase; example hooks: NIGHT1 night-only encounter + hodge_forge day-only) AND the MONSTER BEHAVIOUR engine (src/systems/Monsters.js + src/data/monsters/ — 9 telegraphed archetypes as behaviour FSMs, each with a 'what beats it' counter, + bosses = phased archetype + trick + twist; combat-queryable state; serializable). Self-tested (`npm test`, TimeOfDay 5 + Monsters 10; 14 suites green). Backlog captured in docs/IDEAS-BACKLOG.md (SLICE vs LATER). ALL DESIGNED SYSTEMS ARE NOW BUILT + TESTED HEADLESS.
## >>> THE FIRST PLAYABLE VERTICAL SLICE (HARD GATE) — IN PROGRESS <<<
## [DONE] STAGE 1 — Greenhollow walkable on LPC art + ONE NPC (Mara) wired to the REAL M1 dialogue (src/scenes/GreenhollowScene.js): the data-driven Dialogue + QuestEngine + Karma run live on screen — a clean dialogue box (LPC face portrait, name, body, selectable choices), and talking starts M1 -> the greet/ignore choice applies karma + a deed + completes M1 + unlocks M2. Verified live, 0 console errors (proof/slice1-*).
## [DONE] STAGE 1.5 — PRESENTATION PASS (Van flagged Stage 1 looked broken/unreadable). Fixed: (1) TEXT now on solid dark panels in a crisp 3x-resolution font (HUD + dialogue) — readable at normal zoom; (2) WATER = a 9-sliced grass-banked POND (real banks/shore, not a blue box) baked from the LPC terrain atlas; (3) TREES render whole + depth-sort correctly (repositioned into the green); (4) raw dirt/path STRIPES removed -> clean blended grass; (5) LETTERBOX fixed (ENVELOP scale fills the window); (7) MARA given a distinct RED shirt (player-vs-NPC clear). Reproducible bake: scripts/build_slice_art.py. Logic untouched; 14 suites green; 0 console errors (proof/slice1-greenhollow-fixed-*). STILL OPEN: (6) BUILDINGS — no licence-confirmed LPC building tileset is in the project; per "a fake is worse than none" I did NOT fake one. A cottage + forge need a quick OGA-BY LPC building-asset fetch (like the parked MUSIC pass) — do it at the start of Stage 2. Also still: judge the WALK cadence + camera feel by actually playing.
## [DONE] STAGE 2a — first FIGHT: sword attack + a CHARGER (Monsters FSM) with meaty feel (hit-pause/shake/SFX/flash/knockback, readable telegraph charge-line + punish window). [DONE] STAGE 2b — combat MECHANICS as a tested system: PlayerCombat (DODGE i-frames / BLOCK / PARRY), pure + unit-tested (5 checks), wired into the slice (dodge beats the charger; block/parry reduce/negate). ALL combat FEEL deferred to Van (PLAYTEST-LOG morning TODO; tuning in standards.js COMBAT). Design rule recorded: NO combat in childhood (M1-M6); first combat in Ashen Marsh — the Greenhollow charger is a flagged DEV TEST.
## [DONE] STAGE 2b — ENEMY ARCHETYPE VARIETY + ASHEN MARSH as a real combat region. Generic EnemyController renders ANY tested Monsters archetype (FSM-flag-driven telegraph language + counter resolution); 7 distinct archetype encounters placed in the marsh (charger/ranged/swarm/shielded/brute/caster/electric); M9 wired (shrine -> lantern -> the DROWNED GUARDIAN boss: lantern strips the shroud + half-HP brute enrage twist -> shard_1). All counters verified LIVE (flank↔shielded, interrupt↔caster, wait-window↔electric, AoE↔swarm, dodge↔charger/brute, lantern↔boss). Feel values unchanged. Proof: proof/slice2b-marsh-*.
## [DONE] REAL MONSTER SPRITES wired — the Ashen Marsh archetypes render as real LPC creatures (snake/bat/worm/eyeball/ghost/slime/pumpking; boss = man-eater maw), not tinted humanoids. Behaviour/counters unchanged. Art was already in the repo (prior LPC-monsters fetch); copied to public/art/monsters + loaded/animated/wired into EnemyController. AUDIT finding: the Downloads packs (Ultimate Monsters, Easy Animated Enemy Pack, KayKit, etc.) are ALL 3D (.blend/.fbx/.gltf) — unusable for the 2D LPC game; they stay in the local hauls (not git). FLAG: shielded=pumpking + boss=man-eater are closest-available; Greenhollow's dev-charger still uses its legacy humanoid path (unifies when it moves onto EnemyController in the refactor).
## [DONE] RegionScene base refactor (docs/REGIONSCENE-SPEC.md): src/scenes/RegionScene.js owns rendering/dialogue/interaction/camera/combat/quest-orchestration; Greenhollow + Marsh are thin DATA/config subclasses (the template for all 4 remaining regions). Both verified live + identical (M1/M8/M9/boss/combat/big-head/options), 0 console errors. Bonus: the Greenhollow dev charger (was a flagged HUMANOID on a legacy path — Van's reported bug) moved onto EnemyController -> now a real mon_snake; combat behaviour unchanged.
## [SUPERSEDED] RegionScene base refactor (deferred note): now DONE — see above. The COMBAT is already a shared system (EnemyController/PlayerCombat/InputMap); the remaining rendering/dialogue/orchestration dedup is a clean-template refactor of two diverged combat scenes, deferred to a focused session because it needs full re-verification of both (M1/M8/M9 + both combat flows) and a botched version would regress working code (Regression Rule). The spec sequences it safely (convert one scene at a time, verify after each).
## [STARTED] REGION RENDERING — Ashen Marsh arrival (src/scenes/MarshScene.js + src/data/marsh.js) renders on the proven Greenhollow pattern (bog mood via tints — FLAG: needs real bog/mud/black-water tiles + boardwalk + hut + dead-tree art) and M8 plays on-screen (talk to Elder Yssa -> warm/cold choice -> karma -> complete), Hagga placed. Reach it from the slice with the M key (G returns). FLAG: dedupe Marsh/Greenhollow into a shared RegionScene base.
## [DONE] STAGE 2c — controls + combat polish + framework: DODGE-ROLL/DASH split, SHIELD-SCALED block (shields as items), guard-arc block pose, KID/ADULT combat gate, canonical CONTROL MAP (SSOT + InputMap, remappable, Xbox+kbd), OPTIONS MENU (rebind/audio/aim-invert/modifiers), MODIFIERS registry (big-head etc.) + walled-off 18+ adult-mode architecture (no art; minor-safe, unit-tested). FLAGS: proper LPC roll + block frames; full Gate-N glyphs/auto-detect; controller menu nav.
## NEXT: (1) Van confirms control bindings + feel-tunes combat (morning); (2) art: LPC roll/block frames + bog tiles; (3) finish the marsh (M9 shrine) + dedupe RegionScene; full Gate-N (glyphs/auto-detect); persist settings to save; then regions region-by-region; quest-log + ending-screen UI.
## [DONE] STAGE 1.6 — ART FOUNDATION (assets only; no quests/scenes). AUDIT corrected the diagnosis: the hero was ALREADY fully clothed (body+pants+shoes+shirt render); the real defects were a sparse cowlick hair (looked balding), Mara being a hue-rotate hack, and NO building. Fixed with REAL licence-confirmed LPC art: hero gets full 'Natural' hair; MARA is now a real feminine villager (feminine body/head + blonde bob + forest shirt — distinct at a glance); BRAM is a real bearded gray smith (portrait); and Greenhollow now has a BRICK FORGE assembled from the LPC house-exterior kit (Sharm, CC-BY-SA/GPL/OGA-BY). All ElizaWy chars OGA-BY 3.0; all AI-safe (no anti-AI clause); recorded in ART-LICENSE-NOTE.md; reproducible via scripts/fetch_eliza_npcs.sh. hue-hack shirt_red removed. DoD self-check PASS on P1(text)/P2(no clipping, real tiles)/P3(no placeholders — forge is real art)/P4(licence)/P5(entity clarity: hero/Mara/Bram all distinct). 14 suites green, 0 console errors. Proof: proof/slice1-art-foundation-*. FEEL items still owner-judged (movement/camera/walking behind the forge+trees). NOTE: ElizaWy/LPC is characters-only now (no terrain/buildings there) — buildings come from OpenGameArt/LiberatedPixelCup.
## GOVERNANCE (installed 2026-06-06, ENFORCED): a SSOT for ids — src/constants/{deeds,flags,items,endings}.js (every canonical id; Karma + Monsters import them; god_mercy reconciled into mercy_shown). An automated VERIFY GATE — scripts/verify.mjs (`npm run verify`) FAILS on: a failing test suite, a deed/flag id referenced in data but absent from the SSOT (orphan check — demoed catching a typo'd id), an item defined≠SSOT or unknown economy item, an art file with no ASSET-LEDGER entry, or localStorage/sessionStorage outside the storage adapter — wired as a git pre-commit hook (core.hooksPath=.githooks) so a violating commit is REJECTED. Governance docs: ADR.md (settled decisions), STYLE-GUIDE.md (objective asset-fit values), ASSET-LEDGER.md (machine-read licence ledger), PLAYTEST-LOG.md (Van's feel-judgments). QUALITY-BIBLE gained the REGRESSION RULE (broken suite blocks; every bug gets a test) + SAVE VERSIONING note. CLAUDE.md requires `npm run verify` before every commit. 14 suites green; verify passes.
## GOVERNANCE+ (2026-06-06): standards SSOT — src/constants/standards.js (INTERACTION_RADIUS canonical; Interaction reads it, scene no longer hardcodes 72); verify.mjs gained a CONSISTENCY lint (flags bare interaction-radius / tile-size literals bypassing the SSOT; demoed catching one). CLAUDE.md HARD RULE 10 = SESSION COMPLETION (one finishable objective; per-item ✅/❌ checklist with proof; no silent drops). docs/STANDARDS.md + ADR-009/010 added. 14 suites + 6 verify checks green.
## PROCESS (in force from 2026-06-06): the PRESENTATION & FEEL DoD (QUALITY-BIBLE Part 2.5) now governs ALL visual work — a green `npm test` proves LOGIC only, never presentation. Every render/scene/UI/art session must run it live, SCREENSHOT at normal zoom, self-check pass/FAIL per item (text readable / no flat-box placeholders / autotiled transitions / whole sprites / correct depth / no letterbox / licence-confirmed art / entities distinct at a glance), and hand the FEEL items (movement/camera/combat feel) to Van to play-judge. No placeholder/colour-box assets — omit + flag. Enforced via CLAUDE.md HARD RULE 9 (RENDER VERIFICATION). Also recorded: the immortal treasure-guard FROG (S-A4) is now a LATE-game encounter guarding real treasure (QUEST-WEB-FULL design rule), not the early marsh.
## INFRA (set up 2026-06-06, separate from the region build progress): the SHARED BRAIN — CLAUDE.md (auto-read index + hard rules) + docs/WORKING-AGREEMENT.md; and the ASSET LIBRARY — asset-library/ (CC0-vetted; usable 2D/audio = Kenney UI + audio; ~30 Quaternius/KayKit/Kenney 3D kits catalogued + parked in 3d-future, binaries not committed; nothing quarantined). Index: asset-library/_INDEX.md, summary docs/ASSET-LIBRARY.md. LPC pipeline untouched.
## PHASE 1 - DESIGN (all on paper/docs first, to QUALITY-BIBLE) === COMPLETE ===
  [x] Quality Bible / DoD
  [x] World Map
  [x] Story & Quests - BLUEPRINT (main spine + side web + endings)
  [x] Systems Design (weapons ~24, armour ~12+, magic 5-10, monsters, skill tree, stats) - as data specs
  [x] FULL QUEST FLESH - every main quest written in full (beats, dialogue, choices, rewards, deed hooks)
  [x] FULL QUEST FLESH - every side quest written in full, per region
  [x] COHERENCE REVIEW - read whole design end-to-end: every quest links, choices pay off, map holds
      story, no contradictions, morality forks consistent
## PHASE 2 - BUILD (only after Phase 1 signed off): region-by-region, each a shippable increment,
##   each piece passes its QUALITY-BIBLE gates to Van's eye before "done".
## DOC RULE: Claude Code writes/updates all design docs directly in docs\ and commits+pushes.
##   Van does NOT download/move docs manually.

EMBERFALL 2 - faces. The LPC face parts match the bodies and work fine. We kept adding only a
partial set. Just get the FULL set in. Read docs\QUALITY-BIBLE.md. Van's eye is ground truth.

DO:
1. Get the full AI-safe LPC face layer set (CC0/CC-BY-SA/OGA-BY, no anti-AI): eyes + eyebrows +
   nose + ears, and the expression set (neutral/happy/sad/angry). Record attribution in
   ART-LICENSE-NOTE.md.
2. Composite the FULL face on hero + all 3 NPCs, all directions, all animation frames. Data-driven
   (a face = data: eye colour, brow, nose, expression).
3. Verify: close-up screenshot of each character's face showing eyes+brows+nose clearly. No blank
   heads. Then a normal-zoom screenshot so Van sees them in context.

CONSTRAINTS: AI-safe only; write-once + data-driven; per-file commit. Do not report done without
the close-up screenshots showing visible matched faces.